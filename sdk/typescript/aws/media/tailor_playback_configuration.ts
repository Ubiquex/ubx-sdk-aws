// Code generated by `ubx sdk gen` from aws_media_tailor_playback_configuration. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface TailorPlaybackConfiguration_AdConditioningConfiguration {
  streamingMediaFileConditioning: string | Computed<string>;
}

export interface TailorPlaybackConfiguration_AdDecisionServerConfiguration_HttpRequest {
  /** The body of the request to the Ad Decision Server URL. The maximum length is 100,000 characters. */
  body?: string | Computed<string>;
  /** The compression type of the request sent to the Ad Decision Server URL. Only the POST HTTP Method permits compression other than NONE. */
  compressRequest?: string | Computed<string>;
  /** The headers in the request sent to the Ad Decision Server URL. The max length is 10,000 characters. */
  headers?: unknown | Computed<unknown>;
  /** Supported HTTP Methods for the request to the Ad Decision Server URL. */
  httpMethod?: string | Computed<string>;
}

export interface TailorPlaybackConfiguration_AdDecisionServerConfiguration {
  /** The configuration for the request to the Ad Decision Server URL. */
  httpRequest: TailorPlaybackConfiguration_AdDecisionServerConfiguration_HttpRequest | Computed<TailorPlaybackConfiguration_AdDecisionServerConfiguration_HttpRequest>;
}

export interface TailorPlaybackConfiguration_AdsPersonalizationConcurrency {
  /** Enables parallel processing of ADS requests in VOD workflows when the ADS returns VAST responses. The default is false. */
  enableVodVastParallelization?: boolean | Computed<boolean>;
  /** The maximum number of simultaneous requests that MediaTailor makes to the ADS for each manifest request. The default is 1. */
  maxConcurrentAdsRequests?: number | Computed<number>;
}

export interface TailorPlaybackConfiguration_AdsPersonalizationTimeouts {
  /** The maximum time, in milliseconds, that MediaTailor waits for a single ADS response during live or VOD playback. The default is 3000. */
  adsRequestTimeoutMilliseconds?: number | Computed<number>;
  /** The maximum total time, in milliseconds, that MediaTailor spends on ADS activity for live manifests. The default is 10000. */
  liveMaximumAdsPersonalizationTimeMilliseconds?: number | Computed<number>;
  /** The maximum time, in milliseconds, that MediaTailor waits for a single ADS response during prefetch retrieval. If not set, MediaTailor uses the AdsRequestTimeoutMilliseconds value. */
  prefetchAdsRequestTimeoutMilliseconds?: number | Computed<number>;
  /** The maximum total time, in milliseconds, that MediaTailor spends on ADS activity during prefetch retrieval. */
  prefetchMaximumAdsPersonalizationTimeMilliseconds?: number | Computed<number>;
  /** The maximum total time, in milliseconds, that MediaTailor spends on ADS activity for VOD manifests. The default is 10000. */
  vodMaximumAdsPersonalizationTimeMilliseconds?: number | Computed<number>;
}

export interface TailorPlaybackConfiguration_AvailSuppression {
  /** Defines the policy to apply to the avail suppression mode. BEHIND_LIVE_EDGE will always use the full avail suppression policy. AFTER_LIVE_EDGE mode can be used to invoke partial ad break fills when a session starts mid-break. Valid values are FULL_AVAIL_ONLY and PARTIAL_AVAIL */
  fillPolicy?: string | Computed<string>;
  /** Sets the ad suppression mode. By default, ad suppression is off and all ad breaks are filled with ads or slate. When Mode is set to BEHIND_LIVE_EDGE, ad suppression is active and MediaTailor won't fill ad breaks on or behind the ad suppression Value time in the manifest lookback window. When Mode is set to AFTER_LIVE_EDGE, ad suppression is active and MediaTailor won't fill ad breaks that are within the live edge plus the avail suppression value. */
  mode?: string | Computed<string>;
  /** A live edge offset time in HH:MM:SS. MediaTailor won't fill ad breaks on or behind this time in the manifest lookback window. If Value is set to 00:00:00, it is in sync with the live edge, and MediaTailor won't fill any ad breaks on or behind the live edge. If you set a Value time, MediaTailor won't fill any ad breaks on or behind this time in the manifest lookback window. For example, if you set 00:45:00, then MediaTailor will fill ad breaks that occur within 45 minutes behind the live edge, but won't fill ad breaks on or behind 45 minutes behind the live edge. */
  value?: string | Computed<string>;
}

export interface TailorPlaybackConfiguration_Bumper {
  /** The URL for the end bumper asset. */
  endUrl?: string | Computed<string>;
  /** The URL for the start bumper asset. */
  startUrl?: string | Computed<string>;
}

export interface TailorPlaybackConfiguration_CdnConfiguration {
  /** A non-default content delivery network (CDN) to serve ad segments. By default, AWS Elemental MediaTailor uses Amazon CloudFront with default cache settings as its CDN for ad segments. To set up an alternate CDN, create a rule in your CDN for the origin ads.mediatailor.&lt;region>.amazonaws.com. Then specify the rule's name in this AdSegmentUrlPrefix. When AWS Elemental MediaTailor serves a manifest, it reports your CDN as the source for ad segments. */
  adSegmentUrlPrefix?: string | Computed<string>;
  /** A content delivery network (CDN) to cache content segments, so that content requests don't always have to go to the origin server. First, create a rule in your CDN for the content segment origin server. Then specify the rule's name in this ContentSegmentUrlPrefix. When AWS Elemental MediaTailor serves a manifest, it reports your CDN as the source for content segments. */
  contentSegmentUrlPrefix?: string | Computed<string>;
}

export interface TailorPlaybackConfiguration_DashConfiguration {
  /** The URL generated by MediaTailor to initiate a DASH playback session. The session uses server-side reporting. */
  manifestEndpointPrefix?: string | Computed<string>;
  /** The setting that controls whether MediaTailor includes the Location tag in DASH manifests. MediaTailor populates the Location tag with the URL for manifest update requests, to be used by players that don't support sticky redirects. Disable this if you have CDN routing rules set up for accessing MediaTailor manifests, and you are either using client-side reporting or your players support sticky HTTP redirects. Valid values are DISABLED and EMT_DEFAULT. The EMT_DEFAULT setting enables the inclusion of the tag and is the default value. */
  mpdLocation?: string | Computed<string>;
  /** The setting that controls whether MediaTailor handles manifests from the origin server as multi-period manifests or single-period manifests. If your origin server produces single-period manifests, set this to SINGLE_PERIOD. The default setting is MULTI_PERIOD. For multi-period manifests, omit this setting or set it to MULTI_PERIOD. */
  originManifestType?: string | Computed<string>;
}

export interface TailorPlaybackConfiguration_HlsConfiguration {
  /** The URL that is used to initiate a playback session for devices that support Apple HLS. The session uses server-side reporting. */
  manifestEndpointPrefix?: string | Computed<string>;
}

export interface TailorPlaybackConfiguration_LivePreRollConfiguration {
  /** The URL for the ad decision server (ADS) for pre-roll ads. This includes the specification of static parameters and placeholders for dynamic parameters. AWS Elemental MediaTailor substitutes player-specific and session-specific parameters as needed when calling the ADS. Alternately, for testing, you can provide a static VAST URL. The maximum length is 25,000 characters. */
  adDecisionServerUrl?: string | Computed<string>;
  /** The maximum allowed duration for the pre-roll ad avail. AWS Elemental MediaTailor won't play pre-roll ads to exceed this duration, regardless of the total duration of ads that the ADS returns. */
  maxDurationSeconds?: number | Computed<number>;
}

export interface TailorPlaybackConfiguration_LogConfiguration_AdsInteractionLog {
  /** Indicates that MediaTailor won't emit the selected events in the logs for playback sessions that are initialized with this configuration. */
  excludeEventTypes?: string[] | Computed<string[]>;
  /** Indicates that MediaTailor emits RAW_ADS_RESPONSE logs for playback sessions that are initialized with this configuration. */
  publishOptInEventTypes?: string[] | Computed<string[]>;
}

export interface TailorPlaybackConfiguration_LogConfiguration_ManifestServiceInteractionLog {
  /** Indicates that MediaTailor won't emit the selected events in the logs for playback sessions that are initialized with this configuration. */
  excludeEventTypes?: string[] | Computed<string[]>;
}

export interface TailorPlaybackConfiguration_LogConfiguration {
  adsInteractionLog?: TailorPlaybackConfiguration_LogConfiguration_AdsInteractionLog | Computed<TailorPlaybackConfiguration_LogConfiguration_AdsInteractionLog>;
  /** The method used for collecting logs from AWS Elemental MediaTailor. To configure MediaTailor to send logs directly to Amazon CloudWatch Logs, choose LEGACY_CLOUDWATCH. To configure MediaTailor to send logs to CloudWatch, which then vends the logs to your destination of choice, choose VENDED_LOGS. Supported destinations are CloudWatch Logs log group, Amazon S3 bucket, and Amazon Data Firehose stream. To use vended logs, you must configure the delivery destination in Amazon CloudWatch */
  enabledLoggingStrategies?: string[] | Computed<string[]>;
  manifestServiceInteractionLog?: TailorPlaybackConfiguration_LogConfiguration_ManifestServiceInteractionLog | Computed<TailorPlaybackConfiguration_LogConfiguration_ManifestServiceInteractionLog>;
  /** The percentage of session logs that MediaTailor sends to your CloudWatch Logs account. For example, if your playback configuration has 1000 sessions and percentEnabled is set to 60, MediaTailor sends logs for 600 of the sessions to CloudWatch Logs. MediaTailor decides at random which of the playback configuration sessions to send logs for. If you want to view logs for a specific session, you can use the debug log mode. */
  percentEnabled: number | Computed<number>;
}

export interface TailorPlaybackConfiguration_ManifestProcessingRules_AdMarkerPassthrough {
  /** Enables ad marker passthrough for your configuration. */
  enabled?: boolean | Computed<boolean>;
}

export interface TailorPlaybackConfiguration_ManifestProcessingRules {
  /** For HLS, when set to true, MediaTailor passes through EXT-X-CUE-IN, EXT-X-CUE-OUT, and EXT-X-SPLICEPOINT-SCTE35 ad markers from the origin manifest to the MediaTailor personalized manifest. No logic is applied to these ad markers. For example, if EXT-X-CUE-OUT has a value of 60, but no ads are filled for that ad break, MediaTailor will not set the value to 0. */
  adMarkerPassthrough?: TailorPlaybackConfiguration_ManifestProcessingRules_AdMarkerPassthrough | Computed<TailorPlaybackConfiguration_ManifestProcessingRules_AdMarkerPassthrough>;
}

export interface TailorPlaybackConfiguration_Tags {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

const TailorPlaybackConfiguration_AdConditioningConfigurationFields: FieldMap = {
  streamingMediaFileConditioning: "streaming_media_file_conditioning",
};

const TailorPlaybackConfiguration_AdDecisionServerConfiguration_HttpRequestFields: FieldMap = {
  body: "body",
  compressRequest: "compress_request",
  headers: "headers",
  httpMethod: "http_method",
};

const TailorPlaybackConfiguration_AdDecisionServerConfigurationFields: FieldMap = {
  httpRequest: {
    wireName: "http_request",
    kind: "object",
    fields: TailorPlaybackConfiguration_AdDecisionServerConfiguration_HttpRequestFields,
  },
};

const TailorPlaybackConfiguration_AdsPersonalizationConcurrencyFields: FieldMap = {
  enableVodVastParallelization: "enable_vod_vast_parallelization",
  maxConcurrentAdsRequests: "max_concurrent_ads_requests",
};

const TailorPlaybackConfiguration_AdsPersonalizationTimeoutsFields: FieldMap = {
  adsRequestTimeoutMilliseconds: "ads_request_timeout_milliseconds",
  liveMaximumAdsPersonalizationTimeMilliseconds: "live_maximum_ads_personalization_time_milliseconds",
  prefetchAdsRequestTimeoutMilliseconds: "prefetch_ads_request_timeout_milliseconds",
  prefetchMaximumAdsPersonalizationTimeMilliseconds: "prefetch_maximum_ads_personalization_time_milliseconds",
  vodMaximumAdsPersonalizationTimeMilliseconds: "vod_maximum_ads_personalization_time_milliseconds",
};

const TailorPlaybackConfiguration_AvailSuppressionFields: FieldMap = {
  fillPolicy: "fill_policy",
  mode: "mode",
  value: "value",
};

const TailorPlaybackConfiguration_BumperFields: FieldMap = {
  endUrl: "end_url",
  startUrl: "start_url",
};

const TailorPlaybackConfiguration_CdnConfigurationFields: FieldMap = {
  adSegmentUrlPrefix: "ad_segment_url_prefix",
  contentSegmentUrlPrefix: "content_segment_url_prefix",
};

const TailorPlaybackConfiguration_LivePreRollConfigurationFields: FieldMap = {
  adDecisionServerUrl: "ad_decision_server_url",
  maxDurationSeconds: "max_duration_seconds",
};

const TailorPlaybackConfiguration_LogConfiguration_AdsInteractionLogFields: FieldMap = {
  excludeEventTypes: "exclude_event_types",
  publishOptInEventTypes: "publish_opt_in_event_types",
};

const TailorPlaybackConfiguration_LogConfiguration_ManifestServiceInteractionLogFields: FieldMap = {
  excludeEventTypes: "exclude_event_types",
};

const TailorPlaybackConfiguration_LogConfigurationFields: FieldMap = {
  adsInteractionLog: {
    wireName: "ads_interaction_log",
    kind: "object",
    fields: TailorPlaybackConfiguration_LogConfiguration_AdsInteractionLogFields,
  },
  enabledLoggingStrategies: "enabled_logging_strategies",
  manifestServiceInteractionLog: {
    wireName: "manifest_service_interaction_log",
    kind: "object",
    fields: TailorPlaybackConfiguration_LogConfiguration_ManifestServiceInteractionLogFields,
  },
  percentEnabled: "percent_enabled",
};

const TailorPlaybackConfiguration_ManifestProcessingRules_AdMarkerPassthroughFields: FieldMap = {
  enabled: "enabled",
};

const TailorPlaybackConfiguration_ManifestProcessingRulesFields: FieldMap = {
  adMarkerPassthrough: {
    wireName: "ad_marker_passthrough",
    kind: "object",
    fields: TailorPlaybackConfiguration_ManifestProcessingRules_AdMarkerPassthroughFields,
  },
};

const TailorPlaybackConfiguration_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface TailorPlaybackConfigurationConfig {
  /** <p>The setting that indicates what conditioning MediaTailor will perform on ads that the ad decision server (ADS) returns.</p> */
  adConditioningConfiguration?: TailorPlaybackConfiguration_AdConditioningConfiguration | Computed<TailorPlaybackConfiguration_AdConditioningConfiguration>;
  /** The configuration for the request to the specified Ad Decision Server URL. */
  adDecisionServerConfiguration?: TailorPlaybackConfiguration_AdDecisionServerConfiguration | Computed<TailorPlaybackConfiguration_AdDecisionServerConfiguration>;
  /** The URL for the ad decision server (ADS). This includes the specification of static parameters and placeholders for dynamic parameters. AWS Elemental MediaTailor substitutes player-specific and session-specific parameters as needed when calling the ADS. Alternately, for testing you can provide a static VAST URL. The maximum length is 25,000 characters. */
  adDecisionServerUrl: string | Computed<string>;
  /** The settings that control how many concurrent requests MediaTailor makes to the ad decision server (ADS). */
  adsPersonalizationConcurrency?: TailorPlaybackConfiguration_AdsPersonalizationConcurrency | Computed<TailorPlaybackConfiguration_AdsPersonalizationConcurrency>;
  /** The ad decision server (ADS) request timeouts and personalization time budgets for live, VOD, and prefetch workflows. */
  adsPersonalizationTimeouts?: TailorPlaybackConfiguration_AdsPersonalizationTimeouts | Computed<TailorPlaybackConfiguration_AdsPersonalizationTimeouts>;
  /** The configuration for avail suppression, also known as ad suppression. For more information about ad suppression, see Ad Suppression (https://docs.aws.amazon.com/mediatailor/latest/ug/ad-behavior.html). */
  availSuppression?: TailorPlaybackConfiguration_AvailSuppression | Computed<TailorPlaybackConfiguration_AvailSuppression>;
  /** The configuration for bumpers. Bumpers are short audio or video clips that play at the start or before the end of an ad break. To learn more about bumpers, see Bumpers (https://docs.aws.amazon.com/mediatailor/latest/ug/bumpers.html). */
  bumper?: TailorPlaybackConfiguration_Bumper | Computed<TailorPlaybackConfiguration_Bumper>;
  /** The configuration for using a content delivery network (CDN), like Amazon CloudFront, for content and ad segment management. */
  cdnConfiguration?: TailorPlaybackConfiguration_CdnConfiguration | Computed<TailorPlaybackConfiguration_CdnConfiguration>;
  /** The predefined aliases for dynamic variables. */
  configurationAliases?: unknown | Computed<unknown>;
  /** A map of event names to function identifiers for custom processing during session lifecycle events. */
  functionMapping?: unknown | Computed<unknown>;
  insertionMode?: string | Computed<string>;
  /** The configuration for pre-roll ad insertion. */
  livePreRollConfiguration?: TailorPlaybackConfiguration_LivePreRollConfiguration | Computed<TailorPlaybackConfiguration_LivePreRollConfiguration>;
  logConfiguration?: TailorPlaybackConfiguration_LogConfiguration | Computed<TailorPlaybackConfiguration_LogConfiguration>;
  /** The configuration for manifest processing rules. Manifest processing rules enable customization of the personalized manifests created by MediaTailor. */
  manifestProcessingRules?: TailorPlaybackConfiguration_ManifestProcessingRules | Computed<TailorPlaybackConfiguration_ManifestProcessingRules>;
  /** The identifier for the playback configuration. */
  name: string | Computed<string>;
  /** Defines the maximum duration of underfilled ad time (in seconds) allowed in an ad break. If the duration of underfilled ad time exceeds the personalization threshold, then the personalization of the ad break is abandoned and the underlying content is shown. This feature applies to ad replacement in live and VOD streams, rather than ad insertion, because it relies on an underlying content stream. For more information about ad break behavior, including ad replacement and insertion, see Ad Behavior in AWS Elemental MediaTailor (https://docs.aws.amazon.com/mediatailor/latest/ug/ad-behavior.html). */
  personalizationThresholdSeconds?: number | Computed<number>;
  /** The URL for a high-quality video asset to transcode and use to fill in time that's not used by ads. AWS Elemental MediaTailor shows the slate to fill in gaps in media content. Configuring the slate is optional for non-VPAID configurations. For VPAID, the slate is required because MediaTailor provides it in the slots that are designated for dynamic ad content. The slate must be a high-quality asset that contains both audio and video. */
  slateAdUrl?: string | Computed<string>;
  /** The tags to assign to the playback configuration. */
  tags?: TailorPlaybackConfiguration_Tags[] | Computed<TailorPlaybackConfiguration_Tags[]>;
  /** The name that is used to associate this playback configuration with a custom transcode profile. This overrides the dynamic transcoding defaults of MediaTailor. Use this only if you have already set up custom profiles with the help of AWS Support. */
  transcodeProfileName?: string | Computed<string>;
  /** The URL prefix for the parent manifest for the stream, minus the asset ID. The maximum length is 512 characters. */
  videoContentSourceUrl: string | Computed<string>;
}

export interface TailorPlaybackConfigurationAttrs {
  /** <p>The setting that indicates what conditioning MediaTailor will perform on ads that the ad decision server (ADS) returns.</p> */
  adConditioningConfiguration: TailorPlaybackConfiguration_AdConditioningConfiguration;
  /** The configuration for the request to the specified Ad Decision Server URL. */
  adDecisionServerConfiguration: TailorPlaybackConfiguration_AdDecisionServerConfiguration;
  /** The URL for the ad decision server (ADS). This includes the specification of static parameters and placeholders for dynamic parameters. AWS Elemental MediaTailor substitutes player-specific and session-specific parameters as needed when calling the ADS. Alternately, for testing you can provide a static VAST URL. The maximum length is 25,000 characters. */
  adDecisionServerUrl: string;
  /** The settings that control how many concurrent requests MediaTailor makes to the ad decision server (ADS). */
  adsPersonalizationConcurrency: TailorPlaybackConfiguration_AdsPersonalizationConcurrency;
  /** The ad decision server (ADS) request timeouts and personalization time budgets for live, VOD, and prefetch workflows. */
  adsPersonalizationTimeouts: TailorPlaybackConfiguration_AdsPersonalizationTimeouts;
  /** The configuration for avail suppression, also known as ad suppression. For more information about ad suppression, see Ad Suppression (https://docs.aws.amazon.com/mediatailor/latest/ug/ad-behavior.html). */
  availSuppression: TailorPlaybackConfiguration_AvailSuppression;
  /** The configuration for bumpers. Bumpers are short audio or video clips that play at the start or before the end of an ad break. To learn more about bumpers, see Bumpers (https://docs.aws.amazon.com/mediatailor/latest/ug/bumpers.html). */
  bumper: TailorPlaybackConfiguration_Bumper;
  /** The configuration for using a content delivery network (CDN), like Amazon CloudFront, for content and ad segment management. */
  cdnConfiguration: TailorPlaybackConfiguration_CdnConfiguration;
  /** The predefined aliases for dynamic variables. */
  configurationAliases: unknown;
  /** The configuration for DASH PUT operations. */
  dashConfiguration: TailorPlaybackConfiguration_DashConfiguration;
  /** A map of event names to function identifiers for custom processing during session lifecycle events. */
  functionMapping: unknown;
  hlsConfiguration: TailorPlaybackConfiguration_HlsConfiguration;
  insertionMode: string;
  /** The configuration for pre-roll ad insertion. */
  livePreRollConfiguration: TailorPlaybackConfiguration_LivePreRollConfiguration;
  logConfiguration: TailorPlaybackConfiguration_LogConfiguration;
  /** The configuration for manifest processing rules. Manifest processing rules enable customization of the personalized manifests created by MediaTailor. */
  manifestProcessingRules: TailorPlaybackConfiguration_ManifestProcessingRules;
  /** The identifier for the playback configuration. */
  name: string;
  /** Defines the maximum duration of underfilled ad time (in seconds) allowed in an ad break. If the duration of underfilled ad time exceeds the personalization threshold, then the personalization of the ad break is abandoned and the underlying content is shown. This feature applies to ad replacement in live and VOD streams, rather than ad insertion, because it relies on an underlying content stream. For more information about ad break behavior, including ad replacement and insertion, see Ad Behavior in AWS Elemental MediaTailor (https://docs.aws.amazon.com/mediatailor/latest/ug/ad-behavior.html). */
  personalizationThresholdSeconds: number;
  /** The Amazon Resource Name (ARN) for the playback configuration. */
  playbackConfigurationArn: string;
  /** The URL that the player accesses to get a manifest from MediaTailor. This session will use server-side reporting. */
  playbackEndpointPrefix: string;
  /** The URL that the player uses to initialize a session that uses client-side reporting. */
  sessionInitializationEndpointPrefix: string;
  /** The URL for a high-quality video asset to transcode and use to fill in time that's not used by ads. AWS Elemental MediaTailor shows the slate to fill in gaps in media content. Configuring the slate is optional for non-VPAID configurations. For VPAID, the slate is required because MediaTailor provides it in the slots that are designated for dynamic ad content. The slate must be a high-quality asset that contains both audio and video. */
  slateAdUrl: string;
  /** The tags to assign to the playback configuration. */
  tags: TailorPlaybackConfiguration_Tags[];
  /** The name that is used to associate this playback configuration with a custom transcode profile. This overrides the dynamic transcoding defaults of MediaTailor. Use this only if you have already set up custom profiles with the help of AWS Support. */
  transcodeProfileName: string;
  /** The URL prefix for the parent manifest for the stream, minus the asset ID. The maximum length is 512 characters. */
  videoContentSourceUrl: string;
}

export const TailorPlaybackConfiguration: ResourceBinding<TailorPlaybackConfigurationConfig, TailorPlaybackConfigurationAttrs> = {
  wireType: "aws_media_tailor_playback_configuration",
  fields: {
    adConditioningConfiguration: {
      wireName: "ad_conditioning_configuration",
      kind: "object",
      fields: TailorPlaybackConfiguration_AdConditioningConfigurationFields,
    },
    adDecisionServerConfiguration: {
      wireName: "ad_decision_server_configuration",
      kind: "object",
      fields: TailorPlaybackConfiguration_AdDecisionServerConfigurationFields,
    },
    adDecisionServerUrl: "ad_decision_server_url",
    adsPersonalizationConcurrency: {
      wireName: "ads_personalization_concurrency",
      kind: "object",
      fields: TailorPlaybackConfiguration_AdsPersonalizationConcurrencyFields,
    },
    adsPersonalizationTimeouts: {
      wireName: "ads_personalization_timeouts",
      kind: "object",
      fields: TailorPlaybackConfiguration_AdsPersonalizationTimeoutsFields,
    },
    availSuppression: {
      wireName: "avail_suppression",
      kind: "object",
      fields: TailorPlaybackConfiguration_AvailSuppressionFields,
    },
    bumper: {
      wireName: "bumper",
      kind: "object",
      fields: TailorPlaybackConfiguration_BumperFields,
    },
    cdnConfiguration: {
      wireName: "cdn_configuration",
      kind: "object",
      fields: TailorPlaybackConfiguration_CdnConfigurationFields,
    },
    configurationAliases: "configuration_aliases",
    functionMapping: "function_mapping",
    insertionMode: "insertion_mode",
    livePreRollConfiguration: {
      wireName: "live_pre_roll_configuration",
      kind: "object",
      fields: TailorPlaybackConfiguration_LivePreRollConfigurationFields,
    },
    logConfiguration: {
      wireName: "log_configuration",
      kind: "object",
      fields: TailorPlaybackConfiguration_LogConfigurationFields,
    },
    manifestProcessingRules: {
      wireName: "manifest_processing_rules",
      kind: "object",
      fields: TailorPlaybackConfiguration_ManifestProcessingRulesFields,
    },
    name: "name",
    personalizationThresholdSeconds: "personalization_threshold_seconds",
    slateAdUrl: "slate_ad_url",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: TailorPlaybackConfiguration_TagsFields,
    },
    transcodeProfileName: "transcode_profile_name",
    videoContentSourceUrl: "video_content_source_url",
  },
};
