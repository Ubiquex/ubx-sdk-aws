// Code generated by `ubx sdk gen` from aws_sage_maker_inference_experiment. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface InferenceExperiment_DataStorageConfig_ContentType {
  /** The list of all content type headers that SageMaker will treat as CSV and capture accordingly. */
  csvContentTypes?: string[] | Computed<string[]>;
  /** The list of all content type headers that SageMaker will treat as JSON and capture accordingly. */
  jsonContentTypes?: string[] | Computed<string[]>;
}

export interface InferenceExperiment_DataStorageConfig {
  /** Configuration specifying how to treat different headers. If no headers are specified SageMaker will by default base64 encode when capturing the data. */
  contentType?: InferenceExperiment_DataStorageConfig_ContentType | Computed<InferenceExperiment_DataStorageConfig_ContentType>;
  /** The Amazon S3 bucket where the inference request and response data is stored. */
  destination: string | Computed<string>;
  /** The AWS Key Management Service key that Amazon SageMaker uses to encrypt captured data at rest using Amazon S3 server-side encryption. */
  kmsKey?: string | Computed<string>;
}

export interface InferenceExperiment_EndpointMetadata {
  /** The name of the endpoint configuration. */
  endpointConfigName?: string | Computed<string>;
  /** The name of the endpoint used to run the inference experiment. */
  endpointName: string | Computed<string>;
  /** The status of the endpoint. For possible values of the status of an endpoint. */
  endpointStatus?: string | Computed<string>;
}

export interface InferenceExperiment_ModelVariants_InfrastructureConfig_RealTimeInferenceConfig {
  /** Specifies the initial number of ML compute instances to provision for the model variant when serving real-time inference in the SageMaker Inference Experiment. (AI-inferred) */
  instanceCount?: number | Computed<number>;
  /** The ML compute instance type (e.g., ml.m5.large, ml.c5.xlarge) that SageMaker provisions for the real-time inference endpoint of this inference experiment. (AI-inferred) */
  instanceType?: string | Computed<string>;
}

export interface InferenceExperiment_ModelVariants_InfrastructureConfig {
  /** Specifies whether the model variant is hosted on real-time infrastructure or asynchronous inference infrastructure, with allowed values of 'RealTime' or 'Async'. (AI-inferred) */
  infrastructureType?: string | Computed<string>;
  /** Specifies the Amazon SageMaker ML instance type and instance count to use for the model variant's real-time inference endpoint in the inference experiment. (AI-inferred) */
  realTimeInferenceConfig?: InferenceExperiment_ModelVariants_InfrastructureConfig_RealTimeInferenceConfig | Computed<InferenceExperiment_ModelVariants_InfrastructureConfig_RealTimeInferenceConfig>;
}

export interface InferenceExperiment_ModelVariants {
  /** Specifies the hosting infrastructure (such as the instance type and instance count) for serving this model variant in the inference experiment. (AI-inferred) */
  infrastructureConfig?: InferenceExperiment_ModelVariants_InfrastructureConfig | Computed<InferenceExperiment_ModelVariants_InfrastructureConfig>;
  /** Specifies the name of the SageMaker model that this model variant deploys in the inference experiment. (AI-inferred) */
  modelName?: string | Computed<string>;
  /** The name of the model variant in a SageMaker Inference Experiment, used to uniquely identify the variant within the experiment's configuration. (AI-inferred) */
  variantName?: string | Computed<string>;
}

export interface InferenceExperiment_Schedule {
  /** The timestamp at which the inference experiment ended or will end. */
  endTime?: string | Computed<string>;
  /** The timestamp at which the inference experiment started or will start. */
  startTime?: string | Computed<string>;
}

export interface InferenceExperiment_ShadowModeConfig_ShadowModelVariants {
  /** The percentage of inference requests that are routed to this shadow model variant for evaluation. (AI-inferred) */
  samplingPercentage?: number | Computed<number>;
  /** The name of the model variant that receives a copy of production traffic in shadow mode, allowing you to test its performance without impacting the primary endpoint. (AI-inferred) */
  shadowModelVariantName?: string | Computed<string>;
}

export interface InferenceExperiment_ShadowModeConfig {
  /** List of shadow variant configurations. */
  shadowModelVariants: InferenceExperiment_ShadowModeConfig_ShadowModelVariants[] | Computed<InferenceExperiment_ShadowModeConfig_ShadowModelVariants[]>;
  /** The name of the production variant, which takes all the inference requests. */
  sourceModelVariantName: string | Computed<string>;
}

export interface InferenceExperiment_Tags {
  /** The key of a tag attached to the SageMaker inference experiment. (AI-inferred) */
  key?: string | Computed<string>;
  /** Specifies the value portion of a key-value tag attached to the SageMaker inference experiment, used to organize, identify, and manage the experiment. (AI-inferred) */
  value?: string | Computed<string>;
}

const InferenceExperiment_DataStorageConfig_ContentTypeFields: FieldMap = {
  csvContentTypes: "csv_content_types",
  jsonContentTypes: "json_content_types",
};

const InferenceExperiment_DataStorageConfigFields: FieldMap = {
  contentType: {
    wireName: "content_type",
    kind: "object",
    fields: InferenceExperiment_DataStorageConfig_ContentTypeFields,
  },
  destination: "destination",
  kmsKey: "kms_key",
};

const InferenceExperiment_ModelVariants_InfrastructureConfig_RealTimeInferenceConfigFields: FieldMap = {
  instanceCount: "instance_count",
  instanceType: "instance_type",
};

const InferenceExperiment_ModelVariants_InfrastructureConfigFields: FieldMap = {
  infrastructureType: "infrastructure_type",
  realTimeInferenceConfig: {
    wireName: "real_time_inference_config",
    kind: "object",
    fields: InferenceExperiment_ModelVariants_InfrastructureConfig_RealTimeInferenceConfigFields,
  },
};

const InferenceExperiment_ModelVariantsFields: FieldMap = {
  infrastructureConfig: {
    wireName: "infrastructure_config",
    kind: "object",
    fields: InferenceExperiment_ModelVariants_InfrastructureConfigFields,
  },
  modelName: "model_name",
  variantName: "variant_name",
};

const InferenceExperiment_ScheduleFields: FieldMap = {
  endTime: "end_time",
  startTime: "start_time",
};

const InferenceExperiment_ShadowModeConfig_ShadowModelVariantsFields: FieldMap = {
  samplingPercentage: "sampling_percentage",
  shadowModelVariantName: "shadow_model_variant_name",
};

const InferenceExperiment_ShadowModeConfigFields: FieldMap = {
  shadowModelVariants: {
    wireName: "shadow_model_variants",
    kind: "list",
    fields: InferenceExperiment_ShadowModeConfig_ShadowModelVariantsFields,
  },
  sourceModelVariantName: "source_model_variant_name",
};

const InferenceExperiment_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface InferenceExperimentConfig {
  /** The Amazon S3 location and configuration for storing inference request and response data. */
  dataStorageConfig?: InferenceExperiment_DataStorageConfig | Computed<InferenceExperiment_DataStorageConfig>;
  /** The description of the inference experiment. */
  description?: string | Computed<string>;
  /** The desired state of the experiment after starting or stopping operation. */
  desiredState?: string | Computed<string>;
  /** The name of the endpoint used to run the inference experiment. */
  endpointName: string | Computed<string>;
  /** The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume attached to the ML compute instance that hosts the endpoint. */
  kmsKey?: string | Computed<string>;
  /** An array of ModelVariantConfig objects. Each ModelVariantConfig object in the array describes the infrastructure configuration for the corresponding variant. */
  modelVariants: InferenceExperiment_ModelVariants[] | Computed<InferenceExperiment_ModelVariants[]>;
  /** The name for the inference experiment. */
  name: string | Computed<string>;
  /** The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to access model artifacts and container images, and manage Amazon SageMaker Inference endpoints for model deployment. */
  roleArn: string | Computed<string>;
  /** The duration for which you want the inference experiment to run. */
  schedule?: InferenceExperiment_Schedule | Computed<InferenceExperiment_Schedule>;
  /** The configuration of ShadowMode inference experiment type. Use this field to specify a production variant which takes all the inference requests, and a shadow variant to which Amazon SageMaker replicates a percentage of the inference requests. For the shadow variant also specify the percentage of requests that Amazon SageMaker replicates. */
  shadowModeConfig?: InferenceExperiment_ShadowModeConfig | Computed<InferenceExperiment_ShadowModeConfig>;
  /** The error message or client-specified reason from the StopInferenceExperiment API, that explains the status of the inference experiment. */
  statusReason?: string | Computed<string>;
  /** An array of key-value pairs to apply to this resource. */
  tags?: InferenceExperiment_Tags[] | Computed<InferenceExperiment_Tags[]>;
  /** The type of the inference experiment that you want to run. */
  type: string | Computed<string>;
}

export interface InferenceExperimentAttrs {
  /** The Amazon Resource Name (ARN) of the inference experiment. */
  arn: string;
  /** The timestamp at which you created the inference experiment. */
  creationTime: string;
  /** The Amazon S3 location and configuration for storing inference request and response data. */
  dataStorageConfig: InferenceExperiment_DataStorageConfig;
  /** The description of the inference experiment. */
  description: string;
  /** The desired state of the experiment after starting or stopping operation. */
  desiredState: string;
  /** The metadata of the endpoint on which the inference experiment ran. */
  endpointMetadata: InferenceExperiment_EndpointMetadata;
  /** The name of the endpoint used to run the inference experiment. */
  endpointName: string;
  /** The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume attached to the ML compute instance that hosts the endpoint. */
  kmsKey: string;
  /** The timestamp at which you last modified the inference experiment. */
  lastModifiedTime: string;
  /** An array of ModelVariantConfig objects. Each ModelVariantConfig object in the array describes the infrastructure configuration for the corresponding variant. */
  modelVariants: InferenceExperiment_ModelVariants[];
  /** The name for the inference experiment. */
  name: string;
  /** The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to access model artifacts and container images, and manage Amazon SageMaker Inference endpoints for model deployment. */
  roleArn: string;
  /** The duration for which you want the inference experiment to run. */
  schedule: InferenceExperiment_Schedule;
  /** The configuration of ShadowMode inference experiment type. Use this field to specify a production variant which takes all the inference requests, and a shadow variant to which Amazon SageMaker replicates a percentage of the inference requests. For the shadow variant also specify the percentage of requests that Amazon SageMaker replicates. */
  shadowModeConfig: InferenceExperiment_ShadowModeConfig;
  /** The status of the inference experiment. */
  status: string;
  /** The error message or client-specified reason from the StopInferenceExperiment API, that explains the status of the inference experiment. */
  statusReason: string;
  /** An array of key-value pairs to apply to this resource. */
  tags: InferenceExperiment_Tags[];
  /** The type of the inference experiment that you want to run. */
  type: string;
}

export const InferenceExperiment: ResourceBinding<InferenceExperimentConfig, InferenceExperimentAttrs> = {
  wireType: "aws_sage_maker_inference_experiment",
  fields: {
    dataStorageConfig: {
      wireName: "data_storage_config",
      kind: "object",
      fields: InferenceExperiment_DataStorageConfigFields,
    },
    description: "description",
    desiredState: "desired_state",
    endpointName: "endpoint_name",
    kmsKey: "kms_key",
    modelVariants: {
      wireName: "model_variants",
      kind: "list",
      fields: InferenceExperiment_ModelVariantsFields,
    },
    name: "name",
    roleArn: "role_arn",
    schedule: {
      wireName: "schedule",
      kind: "object",
      fields: InferenceExperiment_ScheduleFields,
    },
    shadowModeConfig: {
      wireName: "shadow_mode_config",
      kind: "object",
      fields: InferenceExperiment_ShadowModeConfigFields,
    },
    statusReason: "status_reason",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: InferenceExperiment_TagsFields,
    },
    type: "type",
  },
};
