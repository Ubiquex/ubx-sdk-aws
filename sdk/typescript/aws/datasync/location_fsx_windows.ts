// Code generated by `ubx sdk gen` from aws_data_sync_location_fsx_windows. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface LocationFsxWindows_CmkSecretConfig {
  /** Specifies the ARN for the customer-managed AWS KMS key used to encrypt the secret specified for SecretArn. DataSync provides this key to AWS Secrets Manager. */
  kmsKeyArn?: string | Computed<string>;
  /** Specifies the ARN for an AWS Secrets Manager secret, managed by DataSync. */
  secretArn?: string | Computed<string>;
}

export interface LocationFsxWindows_CustomSecretConfig {
  /** Specifies the ARN for the AWS Identity and Access Management role that DataSync uses to access the secret specified for SecretArn. */
  secretAccessRoleArn: string | Computed<string>;
  /** Specifies the ARN for a customer created AWS Secrets Manager secret. */
  secretArn: string | Computed<string>;
}

export interface LocationFsxWindows_ManagedSecretConfig {
  /** Specifies the ARN for an AWS Secrets Manager secret. */
  secretArn: string | Computed<string>;
}

export interface LocationFsxWindows_Tags {
  /** The key of a tag that you can assign to the DataSync location for FSx for Windows File Server, used to identify and manage the resource. (AI-inferred) */
  key?: string | Computed<string>;
  /** Specifies the value of a user-defined tag applied to the FSx for Windows File Server DataSync location, used for cost allocation and resource management. (AI-inferred) */
  value?: string | Computed<string>;
}

const LocationFsxWindows_CustomSecretConfigFields: FieldMap = {
  secretAccessRoleArn: "secret_access_role_arn",
  secretArn: "secret_arn",
};

const LocationFsxWindows_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface LocationFsxWindowsConfig {
  /** Specifies configuration information for a customer-managed secret, such as an authentication token or set of credentials that DataSync uses to access a specific transfer location, and an IAM role that DataSync can assume and access the customer-managed secret. */
  customSecretConfig?: LocationFsxWindows_CustomSecretConfig | Computed<LocationFsxWindows_CustomSecretConfig>;
  /** The name of the Windows domain that the FSx for Windows server belongs to. */
  domain?: string | Computed<string>;
  /** The Amazon Resource Name (ARN) for the FSx for Windows file system. */
  fsxFilesystemArn?: string | Computed<string>;
  /** The password of the user who has the permissions to access files and folders in the FSx for Windows file system. */
  password?: string | Computed<string>;
  /** The ARNs of the security groups that are to use to configure the FSx for Windows file system. */
  securityGroupArns: string[] | Computed<string[]>;
  /** A subdirectory in the location's path. */
  subdirectory?: string | Computed<string>;
  /** An array of key-value pairs to apply to this resource. */
  tags?: LocationFsxWindows_Tags[] | Computed<LocationFsxWindows_Tags[]>;
  /** The user who has the permissions to access files and folders in the FSx for Windows file system. */
  user: string | Computed<string>;
}

export interface LocationFsxWindowsAttrs {
  /** Specifies configuration information for a DataSync-managed secret, such as an authentication token or set of credentials that DataSync uses to access a specific transfer location, and a customer-managed AWS KMS key. */
  cmkSecretConfig: LocationFsxWindows_CmkSecretConfig;
  /** Specifies configuration information for a customer-managed secret, such as an authentication token or set of credentials that DataSync uses to access a specific transfer location, and an IAM role that DataSync can assume and access the customer-managed secret. */
  customSecretConfig: LocationFsxWindows_CustomSecretConfig;
  /** The name of the Windows domain that the FSx for Windows server belongs to. */
  domain: string;
  /** The Amazon Resource Name (ARN) for the FSx for Windows file system. */
  fsxFilesystemArn: string;
  /** The Amazon Resource Name (ARN) of the Amazon FSx for Windows file system location that is created. */
  locationArn: string;
  /** The URL of the FSx for Windows location that was described. */
  locationUri: string;
  /** Specifies configuration information for a DataSync-managed secret, such as an authentication token or set of credentials that DataSync uses to access a specific transfer location. DataSync uses the default AWS-managed KMS key to encrypt this secret in AWS Secrets Manager. */
  managedSecretConfig: LocationFsxWindows_ManagedSecretConfig;
  /** The password of the user who has the permissions to access files and folders in the FSx for Windows file system. */
  password: string;
  /** The ARNs of the security groups that are to use to configure the FSx for Windows file system. */
  securityGroupArns: string[];
  /** A subdirectory in the location's path. */
  subdirectory: string;
  /** An array of key-value pairs to apply to this resource. */
  tags: LocationFsxWindows_Tags[];
  /** The user who has the permissions to access files and folders in the FSx for Windows file system. */
  user: string;
}

export const LocationFsxWindows: ResourceBinding<LocationFsxWindowsConfig, LocationFsxWindowsAttrs> = {
  wireType: "aws_data_sync_location_fsx_windows",
  fields: {
    customSecretConfig: {
      wireName: "custom_secret_config",
      kind: "object",
      fields: LocationFsxWindows_CustomSecretConfigFields,
    },
    domain: "domain",
    fsxFilesystemArn: "fsx_filesystem_arn",
    password: "password",
    securityGroupArns: "security_group_arns",
    subdirectory: "subdirectory",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: LocationFsxWindows_TagsFields,
    },
    user: "user",
  },
};
