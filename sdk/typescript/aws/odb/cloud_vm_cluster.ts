// Code generated by `ubx sdk gen` from aws_odb_cloud_vm_cluster. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface CloudVmCluster_DataCollectionOptions {
  /** Indicates whether diagnostic collection is enabled for the VM cluster. */
  isDiagnosticsEventsEnabled?: boolean | Computed<boolean>;
  /** Indicates whether health monitoring is enabled for the VM cluster. */
  isHealthMonitoringEnabled?: boolean | Computed<boolean>;
  /** Indicates whether incident logs are enabled for the cloud VM cluster. */
  isIncidentLogsEnabled?: boolean | Computed<boolean>;
}

export interface CloudVmCluster_DbNodes_Tags {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface CloudVmCluster_DbNodes {
  backupIpId?: string | Computed<string>;
  backupVnic2Id?: string | Computed<string>;
  cpuCoreCount?: number | Computed<number>;
  dbNodeArn?: string | Computed<string>;
  dbNodeId?: string | Computed<string>;
  dbNodeStorageSizeInGbs?: number | Computed<number>;
  dbServerId?: string | Computed<string>;
  dbSystemId?: string | Computed<string>;
  hostIpId?: string | Computed<string>;
  hostname?: string | Computed<string>;
  memorySizeInGbs?: number | Computed<number>;
  ocid?: string | Computed<string>;
  status?: string | Computed<string>;
  tags?: CloudVmCluster_DbNodes_Tags[] | Computed<CloudVmCluster_DbNodes_Tags[]>;
  vnic2Id?: string | Computed<string>;
  vnicId?: string | Computed<string>;
}

export interface CloudVmCluster_IamRoles {
  awsIntegration?: string | Computed<string>;
  iamRoleArn?: string | Computed<string>;
  status?: string | Computed<string>;
}

const CloudVmCluster_DataCollectionOptionsFields: FieldMap = {
  isDiagnosticsEventsEnabled: "is_diagnostics_events_enabled",
  isHealthMonitoringEnabled: "is_health_monitoring_enabled",
  isIncidentLogsEnabled: "is_incident_logs_enabled",
};

const CloudVmCluster_DbNodes_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface CloudVmClusterConfig {
  /** The unique identifier of the Exadata infrastructure that this VM cluster belongs to. */
  cloudExadataInfrastructureId?: string | Computed<string>;
  /** The name of the Grid Infrastructure (GI) cluster. */
  clusterName?: string | Computed<string>;
  /** The number of CPU cores enabled on the VM cluster. */
  cpuCoreCount?: number | Computed<number>;
  /** Information about the data collection options enabled for a VM cluster. */
  dataCollectionOptions?: CloudVmCluster_DataCollectionOptions | Computed<CloudVmCluster_DataCollectionOptions>;
  /** The size of the data disk group, in terabytes (TB), that's allocated for the VM cluster. */
  dataStorageSizeInTbs?: number | Computed<number>;
  /** The amount of local node storage, in gigabytes (GB), that's allocated for the VM cluster. */
  dbNodeStorageSizeInGbs?: number | Computed<number>;
  /** The list of database servers for the VM cluster. */
  dbServers?: string[] | Computed<string[]>;
  /** The user-friendly name for the VM cluster. */
  displayName?: string | Computed<string>;
  /** The software version of the Oracle Grid Infrastructure (GI) for the VM cluster. */
  giVersion?: string | Computed<string>;
  /** The host name for the VM cluster. */
  hostname?: string | Computed<string>;
  /** Indicates whether database backups to local Exadata storage is enabled for the VM cluster. */
  isLocalBackupEnabled?: boolean | Computed<boolean>;
  /** Indicates whether the VM cluster is configured with a sparse disk group. */
  isSparseDiskgroupEnabled?: boolean | Computed<boolean>;
  /** The Oracle license model applied to the VM cluster. */
  licenseModel?: string | Computed<string>;
  /** The amount of memory, in gigabytes (GB), that's allocated for the VM cluster. */
  memorySizeInGbs?: number | Computed<number>;
  /** The unique identifier of the ODB network for the VM cluster. */
  odbNetworkId?: string | Computed<string>;
  /** Property description not available. */
  scanListenerPortTcp?: number | Computed<number>;
  /** The public key portion of one or more key pairs used for SSH access to the VM cluster. */
  sshPublicKeys?: string[] | Computed<string[]>;
  /** The operating system version of the image chosen for the VM cluster. */
  systemVersion?: string | Computed<string>;
  /** Tags to assign to the Vm Cluster. */
  tags?: CloudVmCluster_DbNodes_Tags[] | Computed<CloudVmCluster_DbNodes_Tags[]>;
  /** The time zone of the VM cluster. */
  timeZone?: string | Computed<string>;
}

export interface CloudVmClusterAttrs {
  /** The unique identifier of the Exadata infrastructure that this VM cluster belongs to. */
  cloudExadataInfrastructureId: string;
  /** The Amazon Resource Name (ARN) of the VM cluster. */
  cloudVmClusterArn: string;
  /** The unique identifier of the VM cluster. */
  cloudVmClusterId: string;
  /** The name of the Grid Infrastructure (GI) cluster. */
  clusterName: string;
  /** The OCI model compute model used when you create or clone an instance: ECPU or OCPU. An ECPU is an abstracted measure of compute resources. ECPUs are based on the number of cores elastically allocated from a pool of compute and storage servers. An OCPU is a legacy physical measure of compute resources. OCPUs are based on the physical core of a processor with hyper-threading enabled. */
  computeModel: string;
  /** The number of CPU cores enabled on the VM cluster. */
  cpuCoreCount: number;
  /** Information about the data collection options enabled for a VM cluster. */
  dataCollectionOptions: CloudVmCluster_DataCollectionOptions;
  /** The size of the data disk group, in terabytes (TB), that's allocated for the VM cluster. */
  dataStorageSizeInTbs: number;
  /** The amount of local node storage, in gigabytes (GB), that's allocated for the VM cluster. */
  dbNodeStorageSizeInGbs: number;
  /** The DB nodes that are implicitly created and managed as part of this VM Cluster. */
  dbNodes: CloudVmCluster_DbNodes[];
  /** The list of database servers for the VM cluster. */
  dbServers: string[];
  /** The type of redundancy configured for the VM cluster. NORMAL is 2-way redundancy. HIGH is 3-way redundancy. */
  diskRedundancy: string;
  /** The user-friendly name for the VM cluster. */
  displayName: string;
  /** The domain of the VM cluster. */
  domain: string;
  /** The software version of the Oracle Grid Infrastructure (GI) for the VM cluster. */
  giVersion: string;
  /** The host name for the VM cluster. */
  hostname: string;
  /** The AWS Identity and Access Management (IAM) service roles associated with the VM cluster. */
  iamRoles: CloudVmCluster_IamRoles[];
  /** Indicates whether database backups to local Exadata storage is enabled for the VM cluster. */
  isLocalBackupEnabled: boolean;
  /** Indicates whether the VM cluster is configured with a sparse disk group. */
  isSparseDiskgroupEnabled: boolean;
  /** The Oracle license model applied to the VM cluster. */
  licenseModel: string;
  /** The port number configured for the listener on the VM cluster. */
  listenerPort: number;
  /** The amount of memory, in gigabytes (GB), that's allocated for the VM cluster. */
  memorySizeInGbs: number;
  /** The number of nodes in the VM cluster. */
  nodeCount: number;
  /** The name of the OCI resource anchor for the VM cluster. */
  ociResourceAnchorName: string;
  /** The HTTPS link to the VM cluster in OCI. */
  ociUrl: string;
  /** The OCID of the VM cluster. */
  ocid: string;
  /** The unique identifier of the ODB network for the VM cluster. */
  odbNetworkId: string;
  /** The FQDN of the DNS record for the Single Client Access Name (SCAN) IP addresses that are associated with the VM cluster. */
  scanDnsName: string;
  /** The OCID of the SCAN IP addresses that are associated with the VM cluster. */
  scanIpIds: string[];
  /** Property description not available. */
  scanListenerPortTcp: number;
  /** The hardware model name of the Exadata infrastructure that's running the VM cluster. */
  shape: string;
  /** The public key portion of one or more key pairs used for SSH access to the VM cluster. */
  sshPublicKeys: string[];
  /** The amount of local node storage, in gigabytes (GB), that's allocated to the VM cluster. */
  storageSizeInGbs: number;
  /** The operating system version of the image chosen for the VM cluster. */
  systemVersion: string;
  /** Tags to assign to the Vm Cluster. */
  tags: CloudVmCluster_DbNodes_Tags[];
  /** The time zone of the VM cluster. */
  timeZone: string;
  /** The virtual IP (VIP) addresses that are associated with the VM cluster. Oracle's Cluster Ready Services (CRS) creates and maintains one VIP address for each node in the VM cluster to enable failover. If one node fails, the VIP is reassigned to another active node in the cluster. */
  vipIds: string[];
}

export const CloudVmCluster: ResourceBinding<CloudVmClusterConfig, CloudVmClusterAttrs> = {
  wireType: "aws_odb_cloud_vm_cluster",
  fields: {
    cloudExadataInfrastructureId: "cloud_exadata_infrastructure_id",
    clusterName: "cluster_name",
    cpuCoreCount: "cpu_core_count",
    dataCollectionOptions: {
      wireName: "data_collection_options",
      kind: "object",
      fields: CloudVmCluster_DataCollectionOptionsFields,
    },
    dataStorageSizeInTbs: "data_storage_size_in_tbs",
    dbNodeStorageSizeInGbs: "db_node_storage_size_in_gbs",
    dbServers: "db_servers",
    displayName: "display_name",
    giVersion: "gi_version",
    hostname: "hostname",
    isLocalBackupEnabled: "is_local_backup_enabled",
    isSparseDiskgroupEnabled: "is_sparse_diskgroup_enabled",
    licenseModel: "license_model",
    memorySizeInGbs: "memory_size_in_gbs",
    odbNetworkId: "odb_network_id",
    scanListenerPortTcp: "scan_listener_port_tcp",
    sshPublicKeys: "ssh_public_keys",
    systemVersion: "system_version",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: CloudVmCluster_DbNodes_TagsFields,
    },
    timeZone: "time_zone",
  },
};
