// Code generated by `ubx sdk gen` from aws_secrets_manager_rotation_schedule. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface ManagerRotationSchedule_ExternalSecretRotationMetadata {
  /** The key of a key-value pair in the external_secret_rotation_metadata map, which is passed as metadata to an external rotation Lambda function to customize the rotation process. (AI-inferred) */
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface ManagerRotationSchedule_HostedRotationLambda {
  /** A string of the characters that you don't want in the password. */
  excludeCharacters?: string | Computed<string>;
  /** The ARN of the KMS key that Secrets Manager uses to encrypt the secret. If you don't specify this value, then Secrets Manager uses the key aws/secretsmanager. If aws/secretsmanager doesn't yet exist, then Secrets Manager creates it for you automatically the first time it encrypts the secret value. */
  kmsKeyArn?: string | Computed<string>;
  /** The ARN of the secret that contains superuser credentials, if you use the alternating users rotation strategy. CloudFormation grants the execution role for the Lambda rotation function GetSecretValue permission to the secret in this property. */
  masterSecretArn?: string | Computed<string>;
  /** The ARN of the KMS key that Secrets Manager used to encrypt the superuser secret, if you use the alternating users strategy and the superuser secret is encrypted with a customer managed key. You don't need to specify this property if the superuser secret is encrypted using the key aws/secretsmanager. CloudFormation grants the execution role for the Lambda rotation function Decrypt, DescribeKey, and GenerateDataKey permission to the key in this property. */
  masterSecretKmsKeyArn?: string | Computed<string>;
  /** The name of the Lambda rotation function. */
  rotationLambdaName?: string | Computed<string>;
  /** The type of rotation template to use */
  rotationType: string | Computed<string>;
  /** The python runtime associated with the Lambda function */
  runtime?: string | Computed<string>;
  /** The ARN of the secret that contains superuser credentials, if you use the alternating users rotation strategy. CloudFormation grants the execution role for the Lambda rotation function GetSecretValue permission to the secret in this property. */
  superuserSecretArn?: string | Computed<string>;
  /** The ARN of the KMS key that Secrets Manager used to encrypt the superuser secret, if you use the alternating users strategy and the superuser secret is encrypted with a customer managed key. You don't need to specify this property if the superuser secret is encrypted using the key aws/secretsmanager. CloudFormation grants the execution role for the Lambda rotation function Decrypt, DescribeKey, and GenerateDataKey permission to the key in this property. */
  superuserSecretKmsKeyArn?: string | Computed<string>;
  /** A comma-separated list of security group IDs applied to the target database. */
  vpcSecurityGroupIds?: string | Computed<string>;
  /** A comma separated list of VPC subnet IDs of the target database network. The Lambda rotation function is in the same subnet group. */
  vpcSubnetIds?: string | Computed<string>;
}

export interface ManagerRotationSchedule_RotationRules {
  /** The number of days between automatic scheduled rotations of the secret. You can use this value to check that your secret meets your compliance guidelines for how often secrets must be rotated. */
  automaticallyAfterDays?: number | Computed<number>;
  /** The length of the rotation window in hours, for example 3h for a three hour window. Secrets Manager rotates your secret at any time during this window. The window must not extend into the next rotation window or the next UTC day. The window starts according to the ScheduleExpression. If you don't specify a Duration, for a ScheduleExpression in hours, the window automatically closes after one hour. For a ScheduleExpression in days, the window automatically closes at the end of the UTC day. */
  duration?: string | Computed<string>;
  /** A cron() or rate() expression that defines the schedule for rotating your secret. Secrets Manager rotation schedules use UTC time zone. */
  scheduleExpression?: string | Computed<string>;
}

const ManagerRotationSchedule_ExternalSecretRotationMetadataFields: FieldMap = {
  key: "key",
  value: "value",
};

const ManagerRotationSchedule_HostedRotationLambdaFields: FieldMap = {
  excludeCharacters: "exclude_characters",
  kmsKeyArn: "kms_key_arn",
  masterSecretArn: "master_secret_arn",
  masterSecretKmsKeyArn: "master_secret_kms_key_arn",
  rotationLambdaName: "rotation_lambda_name",
  rotationType: "rotation_type",
  runtime: "runtime",
  superuserSecretArn: "superuser_secret_arn",
  superuserSecretKmsKeyArn: "superuser_secret_kms_key_arn",
  vpcSecurityGroupIds: "vpc_security_group_ids",
  vpcSubnetIds: "vpc_subnet_ids",
};

const ManagerRotationSchedule_RotationRulesFields: FieldMap = {
  automaticallyAfterDays: "automatically_after_days",
  duration: "duration",
  scheduleExpression: "schedule_expression",
};

export interface ManagerRotationScheduleConfig {
  /** The list of metadata needed to successfully rotate a managed external secret. */
  externalSecretRotationMetadata?: ManagerRotationSchedule_ExternalSecretRotationMetadata[] | Computed<ManagerRotationSchedule_ExternalSecretRotationMetadata[]>;
  /** The ARN of the IAM role that is used by Secrets Manager to rotate a managed external secret. */
  externalSecretRotationRoleArn?: string | Computed<string>;
  /** Configuration for a hosted AWS Secrets Manager rotation Lambda function, including the rotation type (e.g., MySQLSingleUser) and optional settings such as VPC configuration and a custom function name, which AWS Secrets Manager provisions and manages automatically to rotate the secret. (AI-inferred) */
  hostedRotationLambda?: ManagerRotationSchedule_HostedRotationLambda | Computed<ManagerRotationSchedule_HostedRotationLambda>;
  /** Specifies whether to rotate the secret immediately or wait until the next scheduled rotation window. */
  rotateImmediatelyOnUpdate?: boolean | Computed<boolean>;
  /** The ARN of an existing Lambda rotation function. To specify a rotation function that is also defined in this template, use the Ref function. */
  rotationLambdaArn?: string | Computed<string>;
  /** Defines the rotation schedule for the secret, including how often it rotates (via automatically_after_days or a cron/rate expression) and optionally the rotation window duration. (AI-inferred) */
  rotationRules?: ManagerRotationSchedule_RotationRules | Computed<ManagerRotationSchedule_RotationRules>;
  /** The ARN or name of the secret to rotate. */
  secretId: string | Computed<string>;
}

export interface ManagerRotationScheduleAttrs {
  /** The list of metadata needed to successfully rotate a managed external secret. */
  externalSecretRotationMetadata: ManagerRotationSchedule_ExternalSecretRotationMetadata[];
  /** The ARN of the IAM role that is used by Secrets Manager to rotate a managed external secret. */
  externalSecretRotationRoleArn: string;
  /** Configuration for a hosted AWS Secrets Manager rotation Lambda function, including the rotation type (e.g., MySQLSingleUser) and optional settings such as VPC configuration and a custom function name, which AWS Secrets Manager provisions and manages automatically to rotate the secret. (AI-inferred) */
  hostedRotationLambda: ManagerRotationSchedule_HostedRotationLambda;
  /** The ARN of the secret. */
  id: string;
  /** Specifies whether to rotate the secret immediately or wait until the next scheduled rotation window. */
  rotateImmediatelyOnUpdate: boolean;
  /** The ARN of an existing Lambda rotation function. To specify a rotation function that is also defined in this template, use the Ref function. */
  rotationLambdaArn: string;
  /** Defines the rotation schedule for the secret, including how often it rotates (via automatically_after_days or a cron/rate expression) and optionally the rotation window duration. (AI-inferred) */
  rotationRules: ManagerRotationSchedule_RotationRules;
  /** The ARN or name of the secret to rotate. */
  secretId: string;
}

export const ManagerRotationSchedule: ResourceBinding<ManagerRotationScheduleConfig, ManagerRotationScheduleAttrs> = {
  wireType: "aws_secrets_manager_rotation_schedule",
  fields: {
    externalSecretRotationMetadata: {
      wireName: "external_secret_rotation_metadata",
      kind: "list",
      fields: ManagerRotationSchedule_ExternalSecretRotationMetadataFields,
    },
    externalSecretRotationRoleArn: "external_secret_rotation_role_arn",
    hostedRotationLambda: {
      wireName: "hosted_rotation_lambda",
      kind: "object",
      fields: ManagerRotationSchedule_HostedRotationLambdaFields,
    },
    rotateImmediatelyOnUpdate: "rotate_immediately_on_update",
    rotationLambdaArn: "rotation_lambda_arn",
    rotationRules: {
      wireName: "rotation_rules",
      kind: "object",
      fields: ManagerRotationSchedule_RotationRulesFields,
    },
    secretId: "secret_id",
  },
};
