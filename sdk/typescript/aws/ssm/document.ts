// Code generated by `ubx sdk gen` from aws_ssm_document. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Document_Attachments {
  key?: string | Computed<string>;
  name?: string | Computed<string>;
  values?: string[] | Computed<string[]>;
}

export interface Document_Requires {
  name?: string | Computed<string>;
  version?: string | Computed<string>;
}

export interface Document_Tags {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

const Document_AttachmentsFields: FieldMap = {
  key: "key",
  name: "name",
  values: "values",
};

const Document_RequiresFields: FieldMap = {
  name: "name",
  version: "version",
};

const Document_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface DocumentConfig {
  /** A list of key and value pairs that describe attachments to a version of a document. */
  attachments?: Document_Attachments[] | Computed<Document_Attachments[]>;
  /** The content for the Systems Manager document in JSON, YAML or String format. */
  content: unknown | Computed<unknown>;
  /** Specify the document format for the request. The document format can be either JSON or YAML. JSON is the default format. */
  documentFormat?: string | Computed<string>;
  /** The type of document to create. */
  documentType?: string | Computed<string>;
  /** A name for the Systems Manager document. */
  name?: string | Computed<string>;
  /** A list of SSM documents required by a document. For example, an ApplicationConfiguration document requires an ApplicationConfigurationSchema document. */
  requires?: Document_Requires[] | Computed<Document_Requires[]>;
  /** Optional metadata that you assign to a resource. Tags enable you to categorize a resource in different ways, such as by purpose, owner, or environment. */
  tags?: Document_Tags[] | Computed<Document_Tags[]>;
  /** Specify a target type to define the kinds of resources the document can run on. */
  targetType?: string | Computed<string>;
  /** Update method - when set to 'Replace', the update will replace the existing document; when set to 'NewVersion', the update will create a new version. */
  updateMethod?: string | Computed<string>;
  /** An optional field specifying the version of the artifact you are creating with the document. This value is unique across all versions of a document, and cannot be changed. */
  versionName?: string | Computed<string>;
}

export interface DocumentAttrs {
  /** A list of key and value pairs that describe attachments to a version of a document. */
  attachments: Document_Attachments[];
  /** The content for the Systems Manager document in JSON, YAML or String format. */
  content: unknown;
  /** Specify the document format for the request. The document format can be either JSON or YAML. JSON is the default format. */
  documentFormat: string;
  /** The type of document to create. */
  documentType: string;
  /** A name for the Systems Manager document. */
  name: string;
  /** A list of SSM documents required by a document. For example, an ApplicationConfiguration document requires an ApplicationConfigurationSchema document. */
  requires: Document_Requires[];
  /** Optional metadata that you assign to a resource. Tags enable you to categorize a resource in different ways, such as by purpose, owner, or environment. */
  tags: Document_Tags[];
  /** Specify a target type to define the kinds of resources the document can run on. */
  targetType: string;
  /** Update method - when set to 'Replace', the update will replace the existing document; when set to 'NewVersion', the update will create a new version. */
  updateMethod: string;
  /** An optional field specifying the version of the artifact you are creating with the document. This value is unique across all versions of a document, and cannot be changed. */
  versionName: string;
}

export const Document: ResourceBinding<DocumentConfig, DocumentAttrs> = {
  wireType: "aws_ssm_document",
  fields: {
    attachments: {
      wireName: "attachments",
      kind: "list",
      fields: Document_AttachmentsFields,
    },
    content: "content",
    documentFormat: "document_format",
    documentType: "document_type",
    name: "name",
    requires: {
      wireName: "requires",
      kind: "list",
      fields: Document_RequiresFields,
    },
    tags: {
      wireName: "tags",
      kind: "list",
      fields: Document_TagsFields,
    },
    targetType: "target_type",
    updateMethod: "update_method",
    versionName: "version_name",
  },
};
