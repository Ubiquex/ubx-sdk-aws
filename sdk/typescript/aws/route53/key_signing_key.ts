// Code generated by `ubx sdk gen` from aws_route53_key_signing_key. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface KeySigningKeyConfig {
  /** The unique string (ID) used to identify a hosted zone. */
  hostedZoneId: string | Computed<string>;
  /** The Amazon resource name (ARN) for a customer managed key (CMK) in AWS Key Management Service (KMS). The KeyManagementServiceArn must be unique for each key signing key (KSK) in a single hosted zone. */
  keyManagementServiceArn: string | Computed<string>;
  /** An alphanumeric string used to identify a key signing key (KSK). Name must be unique for each key signing key in the same hosted zone. */
  name: string | Computed<string>;
  /** A string specifying the initial status of the key signing key (KSK). You can set the value to ACTIVE or INACTIVE. */
  status: string | Computed<string>;
}

export interface KeySigningKeyAttrs {
  /** The unique string (ID) used to identify a hosted zone. */
  hostedZoneId: string;
  /** The Amazon resource name (ARN) for a customer managed key (CMK) in AWS Key Management Service (KMS). The KeyManagementServiceArn must be unique for each key signing key (KSK) in a single hosted zone. */
  keyManagementServiceArn: string;
  /** An alphanumeric string used to identify a key signing key (KSK). Name must be unique for each key signing key in the same hosted zone. */
  name: string;
  /** A string specifying the initial status of the key signing key (KSK). You can set the value to ACTIVE or INACTIVE. */
  status: string;
}

export const KeySigningKey: ResourceBinding<KeySigningKeyConfig, KeySigningKeyAttrs> = {
  wireType: "aws_route53_key_signing_key",
  fields: {
    hostedZoneId: "hosted_zone_id",
    keyManagementServiceArn: "key_management_service_arn",
    name: "name",
    status: "status",
  },
};
