// Code generated by `ubx sdk gen` from aws_route53_hosted_zone. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface HostedZone_HostedZoneConfig {
  /** Any comments that you want to include about the hosted zone. */
  comment?: string | Computed<string>;
}

export interface HostedZone_HostedZoneFeatures {
  enableAcceleratedRecovery?: boolean | Computed<boolean>;
}

export interface HostedZone_HostedZoneTags {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface HostedZone_QueryLoggingConfig {
  /** The Amazon Resource Name (ARN) of the CloudWatch Logs log group that Amazon Route 53 is publishing logs to. */
  cloudWatchLogsLogGroupArn: string | Computed<string>;
}

export interface HostedZone_Vpcs {
  vpcid?: string | Computed<string>;
  vpcregion?: string | Computed<string>;
}

const HostedZone_HostedZoneConfigFields: FieldMap = {
  comment: "comment",
};

const HostedZone_HostedZoneFeaturesFields: FieldMap = {
  enableAcceleratedRecovery: "enable_accelerated_recovery",
};

const HostedZone_HostedZoneTagsFields: FieldMap = {
  key: "key",
  value: "value",
};

const HostedZone_QueryLoggingConfigFields: FieldMap = {
  cloudWatchLogsLogGroupArn: "cloud_watch_logs_log_group_arn",
};

const HostedZone_VpcsFields: FieldMap = {
  vpcid: "vpcid",
  vpcregion: "vpcregion",
};

export interface HostedZoneConfig {
  /** A complex type that contains an optional comment about your hosted zone. If you don't want to specify a comment, omit both the ``HostedZoneConfig`` and ``Comment`` elements. */
  hostedZoneConfig?: HostedZone_HostedZoneConfig | Computed<HostedZone_HostedZoneConfig>;
  /** Represents the features configuration for a hosted zone, including the status of various features and any associated failure reasons. */
  hostedZoneFeatures?: HostedZone_HostedZoneFeatures | Computed<HostedZone_HostedZoneFeatures>;
  /** Adds, edits, or deletes tags for a health check or a hosted zone. For information about using tags for cost allocation, see [Using Cost Allocation Tags](https://docs.aws.amazon.com/awsaccountbilling/latest/aboutv2/cost-alloc-tags.html) in the *User Guide*. */
  hostedZoneTags?: HostedZone_HostedZoneTags[] | Computed<HostedZone_HostedZoneTags[]>;
  /** The name of the domain. Specify a fully qualified domain name, for example, *www.example.com*. The trailing dot is optional; Amazon Route 53 assumes that the domain name is fully qualified. This means that Route 53 treats *www.example.com* (without a trailing dot) and *www.example.com.* (with a trailing dot) as identical. If you're creating a public hosted zone, this is the name you have registered with your DNS registrar. If your domain name is registered with a registrar other than Route 53, change the name servers for your domain to the set of ``NameServers`` that are returned by the ``Fn::GetAtt`` intrinsic function. */
  name?: string | Computed<string>;
  /** A complex type that contains information about a configuration for DNS query logging. */
  queryLoggingConfig?: HostedZone_QueryLoggingConfig | Computed<HostedZone_QueryLoggingConfig>;
  /** *Private hosted zones:* A complex type that contains information about the VPCs that are associated with the specified hosted zone. For public hosted zones, omit ``VPCs``, ``VPCId``, and ``VPCRegion``. */
  vpcs?: HostedZone_Vpcs[] | Computed<HostedZone_Vpcs[]>;
}

export interface HostedZoneAttrs {
  /** A complex type that contains an optional comment about your hosted zone. If you don't want to specify a comment, omit both the ``HostedZoneConfig`` and ``Comment`` elements. */
  hostedZoneConfig: HostedZone_HostedZoneConfig;
  /** Represents the features configuration for a hosted zone, including the status of various features and any associated failure reasons. */
  hostedZoneFeatures: HostedZone_HostedZoneFeatures;
  /** Adds, edits, or deletes tags for a health check or a hosted zone. For information about using tags for cost allocation, see [Using Cost Allocation Tags](https://docs.aws.amazon.com/awsaccountbilling/latest/aboutv2/cost-alloc-tags.html) in the *User Guide*. */
  hostedZoneTags: HostedZone_HostedZoneTags[];
  id: string;
  /** The name of the domain. Specify a fully qualified domain name, for example, *www.example.com*. The trailing dot is optional; Amazon Route 53 assumes that the domain name is fully qualified. This means that Route 53 treats *www.example.com* (without a trailing dot) and *www.example.com.* (with a trailing dot) as identical. If you're creating a public hosted zone, this is the name you have registered with your DNS registrar. If your domain name is registered with a registrar other than Route 53, change the name servers for your domain to the set of ``NameServers`` that are returned by the ``Fn::GetAtt`` intrinsic function. */
  name: string;
  nameServers: string[];
  /** A complex type that contains information about a configuration for DNS query logging. */
  queryLoggingConfig: HostedZone_QueryLoggingConfig;
  /** *Private hosted zones:* A complex type that contains information about the VPCs that are associated with the specified hosted zone. For public hosted zones, omit ``VPCs``, ``VPCId``, and ``VPCRegion``. */
  vpcs: HostedZone_Vpcs[];
}

export const HostedZone: ResourceBinding<HostedZoneConfig, HostedZoneAttrs> = {
  wireType: "aws_route53_hosted_zone",
  fields: {
    hostedZoneConfig: {
      wireName: "hosted_zone_config",
      kind: "object",
      fields: HostedZone_HostedZoneConfigFields,
    },
    hostedZoneFeatures: {
      wireName: "hosted_zone_features",
      kind: "object",
      fields: HostedZone_HostedZoneFeaturesFields,
    },
    hostedZoneTags: {
      wireName: "hosted_zone_tags",
      kind: "list",
      fields: HostedZone_HostedZoneTagsFields,
    },
    name: "name",
    queryLoggingConfig: {
      wireName: "query_logging_config",
      kind: "object",
      fields: HostedZone_QueryLoggingConfigFields,
    },
    vpcs: {
      wireName: "vpcs",
      kind: "list",
      fields: HostedZone_VpcsFields,
    },
  },
};
