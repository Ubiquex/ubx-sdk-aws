// Code generated by `ubx sdk gen` from aws_route53_resolver_resolver_endpoint. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface ResolverResolverEndpoint_IpAddresses {
  /** The specific IP address to assign to the resolver endpoint within the referenced subnet; when not provided, AWS automatically chooses an available IP from that subnet. (AI-inferred) */
  ip?: string | Computed<string>;
  /** Specifies the IPv6 address to assign to the Resolver endpoint's elastic network interface (ENI) for this IP address entry, within the given subnet, enabling IPv6-only or dual-stack resolver endpoints. (AI-inferred) */
  ipv6?: string | Computed<string>;
  /** The subnet ID where the Route 53 Resolver endpoint's elastic network interface is deployed, defining the VPC subnet from which an IP address is allocated for this resolver endpoint. (AI-inferred) */
  subnetId?: string | Computed<string>;
}

export interface ResolverResolverEndpoint_Tags {
  /** The key of a tag attached to the Route53 Resolver endpoint, enabling you to label the endpoint for filtering, cost tracking, and access control. (AI-inferred) */
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

const ResolverResolverEndpoint_IpAddressesFields: FieldMap = {
  ip: "ip",
  ipv6: "ipv6",
  subnetId: "subnet_id",
};

const ResolverResolverEndpoint_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface ResolverResolverEndpointConfig {
  /** Indicates whether the Resolver endpoint allows inbound or outbound DNS queries: - INBOUND: allows DNS queries to your VPC from your network - OUTBOUND: allows DNS queries from your VPC to your network - INBOUND_DELEGATION: allows DNS queries to your VPC from your network with authoritative answers from private hosted zones */
  direction: string | Computed<string>;
  /** Specifies whether DNS64 is enabled for the Inbound Resolver Endpoint. When set to true, if a DNS AAAA query is made for a domain that has only an A (IPv4) record, the resolver automatically synthesizes an AAAA (IPv6) response by embedding the IPv4 address into the well-known prefix 64:ff9b::/96. Default is false. */
  dns64Enabled?: boolean | Computed<boolean>;
  /** The subnets and IP addresses in your VPC that DNS queries originate from (for outbound endpoints) or that you forward DNS queries to (for inbound endpoints). The subnet ID uniquely identifies a VPC. */
  ipAddresses: ResolverResolverEndpoint_IpAddresses[] | Computed<ResolverResolverEndpoint_IpAddresses[]>;
  /** Specifies whether IPv6 Internet Gateway access is enabled through the Outbound Resolver Endpoint. When set to true, this property allows your Endpoint ENIs to reach public IPv6 target nameservers through an internet gateway. Default is false. */
  ipv6InternetAccessEnabled?: boolean | Computed<boolean>;
  /** A friendly name that lets you easily find a configuration in the Resolver dashboard in the Route 53 console. */
  name?: string | Computed<string>;
  /** The ARN (Amazon Resource Name) for the Outpost. */
  outpostArn?: string | Computed<string>;
  /** The Amazon EC2 instance type. */
  preferredInstanceType?: string | Computed<string>;
  /** Protocols used for the endpoint. DoH-FIPS is applicable for inbound endpoints only. */
  protocols?: string[] | Computed<string[]>;
  /** The Resolver endpoint IP address type. */
  resolverEndpointType?: string | Computed<string>;
  /** Specifies whether RNI enhanced metrics are enabled for the Resolver Endpoints. When set to true, one-minute granular metrics are published in CloudWatch for each RNI associated with this endpoint. When set to false, metrics are not published. Default is false. */
  rniEnhancedMetricsEnabled?: boolean | Computed<boolean>;
  /** The ID of one or more security groups that control access to this VPC. The security group must include one or more inbound rules (for inbound endpoints) or outbound rules (for outbound endpoints). Inbound and outbound rules must allow TCP and UDP access. For inbound access, open port 53. For outbound access, open the port that you're using for DNS queries on your network. */
  securityGroupIds: string[] | Computed<string[]>;
  /** An array of key-value pairs to apply to this resource. */
  tags?: ResolverResolverEndpoint_Tags[] | Computed<ResolverResolverEndpoint_Tags[]>;
  /** Specifies whether target name server metrics are enabled for the Outbound Resolver Endpoint. When set to true, one-minute granular metrics are published in CloudWatch for each target name server associated with this endpoint. When set to false, metrics are not published. Default is false. */
  targetNameServerMetricsEnabled?: boolean | Computed<boolean>;
}

export interface ResolverResolverEndpointAttrs {
  /** The Amazon Resource Name (ARN) of the resolver endpoint, such as arn:aws:route53resolver:us-east-1:123456789012:resolver-endpoint/resolver-endpoint-a1bzhi. */
  arn: string;
  /** Indicates whether the Resolver endpoint allows inbound or outbound DNS queries: - INBOUND: allows DNS queries to your VPC from your network - OUTBOUND: allows DNS queries from your VPC to your network - INBOUND_DELEGATION: allows DNS queries to your VPC from your network with authoritative answers from private hosted zones */
  direction: string;
  /** Specifies whether DNS64 is enabled for the Inbound Resolver Endpoint. When set to true, if a DNS AAAA query is made for a domain that has only an A (IPv4) record, the resolver automatically synthesizes an AAAA (IPv6) response by embedding the IPv4 address into the well-known prefix 64:ff9b::/96. Default is false. */
  dns64Enabled: boolean;
  /** The ID of the VPC that you want to create the resolver endpoint in. */
  hostVpcid: string;
  /** The number of IP addresses that the resolver endpoint can use for DNS queries. */
  ipAddressCount: string;
  /** The subnets and IP addresses in your VPC that DNS queries originate from (for outbound endpoints) or that you forward DNS queries to (for inbound endpoints). The subnet ID uniquely identifies a VPC. */
  ipAddresses: ResolverResolverEndpoint_IpAddresses[];
  /** Specifies whether IPv6 Internet Gateway access is enabled through the Outbound Resolver Endpoint. When set to true, this property allows your Endpoint ENIs to reach public IPv6 target nameservers through an internet gateway. Default is false. */
  ipv6InternetAccessEnabled: boolean;
  /** A friendly name that lets you easily find a configuration in the Resolver dashboard in the Route 53 console. */
  name: string;
  /** The ARN (Amazon Resource Name) for the Outpost. */
  outpostArn: string;
  /** The Amazon EC2 instance type. */
  preferredInstanceType: string;
  /** Protocols used for the endpoint. DoH-FIPS is applicable for inbound endpoints only. */
  protocols: string[];
  /** The ID of the resolver endpoint. */
  resolverEndpointId: string;
  /** The Resolver endpoint IP address type. */
  resolverEndpointType: string;
  /** Specifies whether RNI enhanced metrics are enabled for the Resolver Endpoints. When set to true, one-minute granular metrics are published in CloudWatch for each RNI associated with this endpoint. When set to false, metrics are not published. Default is false. */
  rniEnhancedMetricsEnabled: boolean;
  /** The ID of one or more security groups that control access to this VPC. The security group must include one or more inbound rules (for inbound endpoints) or outbound rules (for outbound endpoints). Inbound and outbound rules must allow TCP and UDP access. For inbound access, open port 53. For outbound access, open the port that you're using for DNS queries on your network. */
  securityGroupIds: string[];
  /** An array of key-value pairs to apply to this resource. */
  tags: ResolverResolverEndpoint_Tags[];
  /** Specifies whether target name server metrics are enabled for the Outbound Resolver Endpoint. When set to true, one-minute granular metrics are published in CloudWatch for each target name server associated with this endpoint. When set to false, metrics are not published. Default is false. */
  targetNameServerMetricsEnabled: boolean;
}

export const ResolverResolverEndpoint: ResourceBinding<ResolverResolverEndpointConfig, ResolverResolverEndpointAttrs> = {
  wireType: "aws_route53_resolver_resolver_endpoint",
  fields: {
    direction: "direction",
    dns64Enabled: "dns64_enabled",
    ipAddresses: {
      wireName: "ip_addresses",
      kind: "list",
      fields: ResolverResolverEndpoint_IpAddressesFields,
    },
    ipv6InternetAccessEnabled: "ipv6_internet_access_enabled",
    name: "name",
    outpostArn: "outpost_arn",
    preferredInstanceType: "preferred_instance_type",
    protocols: "protocols",
    resolverEndpointType: "resolver_endpoint_type",
    rniEnhancedMetricsEnabled: "rni_enhanced_metrics_enabled",
    securityGroupIds: "security_group_ids",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: ResolverResolverEndpoint_TagsFields,
    },
    targetNameServerMetricsEnabled: "target_name_server_metrics_enabled",
  },
};
