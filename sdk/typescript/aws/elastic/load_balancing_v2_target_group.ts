// Code generated by `ubx sdk gen` from aws_elastic_load_balancing_v2_target_group. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface LoadBalancingV2TargetGroup_Matcher {
  /** You can specify values between 0 and 99. You can specify multiple values, or a range of values. The default value is 12. */
  grpcCode?: string | Computed<string>;
  /** For Application Load Balancers, you can specify values between 200 and 499, and the default value is 200. You can specify multiple values or a range of values. */
  httpCode?: string | Computed<string>;
}

export interface LoadBalancingV2TargetGroup_Tags {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface LoadBalancingV2TargetGroup_Targets {
  availabilityZone?: string | Computed<string>;
  id?: string | Computed<string>;
  port?: number | Computed<number>;
  quicServerId?: string | Computed<string>;
}

const LoadBalancingV2TargetGroup_MatcherFields: FieldMap = {
  grpcCode: "grpc_code",
  httpCode: "http_code",
};

const LoadBalancingV2TargetGroup_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

const LoadBalancingV2TargetGroup_TargetsFields: FieldMap = {
  availabilityZone: "availability_zone",
  id: "id",
  port: "port",
  quicServerId: "quic_server_id",
};

export interface LoadBalancingV2TargetGroupConfig {
  /** Indicates whether health checks are enabled. If the target type is lambda, health checks are disabled by default but can be enabled. If the target type is instance, ip, or alb, health checks are always enabled and cannot be disabled. */
  healthCheckEnabled?: boolean | Computed<boolean>;
  /** The approximate amount of time, in seconds, between health checks of an individual target. */
  healthCheckIntervalSeconds?: number | Computed<number>;
  /** [HTTP/HTTPS health checks] The destination for health checks on the targets. [HTTP1 or HTTP2 protocol version] The ping path. The default is /. [GRPC protocol version] The path of a custom health check method with the format /package.service/method. The default is /AWS.ALB/healthcheck. */
  healthCheckPath?: string | Computed<string>;
  /** The port the load balancer uses when performing health checks on targets. */
  healthCheckPort?: string | Computed<string>;
  /** The protocol the load balancer uses when performing health checks on targets. */
  healthCheckProtocol?: string | Computed<string>;
  /** The amount of time, in seconds, during which no response from a target means a failed health check. */
  healthCheckTimeoutSeconds?: number | Computed<number>;
  /** The number of consecutive health checks successes required before considering an unhealthy target healthy. */
  healthyThresholdCount?: number | Computed<number>;
  /** The type of IP address used for this target group. The possible values are ipv4 and ipv6. */
  ipAddressType?: string | Computed<string>;
  matcher?: LoadBalancingV2TargetGroup_Matcher | Computed<LoadBalancingV2TargetGroup_Matcher>;
  /** The name of the target group. */
  name?: string | Computed<string>;
  /** The port on which the targets receive traffic. This port is used unless you specify a port override when registering the target. If the target is a Lambda function, this parameter does not apply. If the protocol is GENEVE, the supported port is 6081. */
  port?: number | Computed<number>;
  /** The protocol to use for routing traffic to the targets. */
  protocol?: string | Computed<string>;
  /** [HTTP/HTTPS protocol] The protocol version. The possible values are GRPC, HTTP1, and HTTP2. */
  protocolVersion?: string | Computed<string>;
  /** The tags. */
  tags?: LoadBalancingV2TargetGroup_Tags[] | Computed<LoadBalancingV2TargetGroup_Tags[]>;
  /** The port that the target control agent uses to communicate the available capacity of targets to the load balancer. */
  targetControlPort?: number | Computed<number>;
  /** The attributes. */
  targetGroupAttributes?: LoadBalancingV2TargetGroup_Tags[] | Computed<LoadBalancingV2TargetGroup_Tags[]>;
  /** The type of target that you must specify when registering targets with this target group. You can't specify targets for a target group using more than one target type. */
  targetType?: string | Computed<string>;
  /** The targets. */
  targets?: LoadBalancingV2TargetGroup_Targets[] | Computed<LoadBalancingV2TargetGroup_Targets[]>;
  /** The number of consecutive health check failures required before considering a target unhealthy. */
  unhealthyThresholdCount?: number | Computed<number>;
  /** The identifier of the virtual private cloud (VPC). If the target is a Lambda function, this parameter does not apply. */
  vpcId?: string | Computed<string>;
}

export interface LoadBalancingV2TargetGroupAttrs {
  /** Indicates whether health checks are enabled. If the target type is lambda, health checks are disabled by default but can be enabled. If the target type is instance, ip, or alb, health checks are always enabled and cannot be disabled. */
  healthCheckEnabled: boolean;
  /** The approximate amount of time, in seconds, between health checks of an individual target. */
  healthCheckIntervalSeconds: number;
  /** [HTTP/HTTPS health checks] The destination for health checks on the targets. [HTTP1 or HTTP2 protocol version] The ping path. The default is /. [GRPC protocol version] The path of a custom health check method with the format /package.service/method. The default is /AWS.ALB/healthcheck. */
  healthCheckPath: string;
  /** The port the load balancer uses when performing health checks on targets. */
  healthCheckPort: string;
  /** The protocol the load balancer uses when performing health checks on targets. */
  healthCheckProtocol: string;
  /** The amount of time, in seconds, during which no response from a target means a failed health check. */
  healthCheckTimeoutSeconds: number;
  /** The number of consecutive health checks successes required before considering an unhealthy target healthy. */
  healthyThresholdCount: number;
  /** The type of IP address used for this target group. The possible values are ipv4 and ipv6. */
  ipAddressType: string;
  /** The Amazon Resource Names (ARNs) of the load balancers that route traffic to this target group. */
  loadBalancerArns: string[];
  matcher: LoadBalancingV2TargetGroup_Matcher;
  /** The name of the target group. */
  name: string;
  /** The port on which the targets receive traffic. This port is used unless you specify a port override when registering the target. If the target is a Lambda function, this parameter does not apply. If the protocol is GENEVE, the supported port is 6081. */
  port: number;
  /** The protocol to use for routing traffic to the targets. */
  protocol: string;
  /** [HTTP/HTTPS protocol] The protocol version. The possible values are GRPC, HTTP1, and HTTP2. */
  protocolVersion: string;
  /** The tags. */
  tags: LoadBalancingV2TargetGroup_Tags[];
  /** The port that the target control agent uses to communicate the available capacity of targets to the load balancer. */
  targetControlPort: number;
  /** The ARN of the Target Group */
  targetGroupArn: string;
  /** The attributes. */
  targetGroupAttributes: LoadBalancingV2TargetGroup_Tags[];
  /** The full name of the target group. */
  targetGroupFullName: string;
  /** The name of the target group. */
  targetGroupName: string;
  /** The type of target that you must specify when registering targets with this target group. You can't specify targets for a target group using more than one target type. */
  targetType: string;
  /** The targets. */
  targets: LoadBalancingV2TargetGroup_Targets[];
  /** The number of consecutive health check failures required before considering a target unhealthy. */
  unhealthyThresholdCount: number;
  /** The identifier of the virtual private cloud (VPC). If the target is a Lambda function, this parameter does not apply. */
  vpcId: string;
}

export const LoadBalancingV2TargetGroup: ResourceBinding<LoadBalancingV2TargetGroupConfig, LoadBalancingV2TargetGroupAttrs> = {
  wireType: "aws_elastic_load_balancing_v2_target_group",
  fields: {
    healthCheckEnabled: "health_check_enabled",
    healthCheckIntervalSeconds: "health_check_interval_seconds",
    healthCheckPath: "health_check_path",
    healthCheckPort: "health_check_port",
    healthCheckProtocol: "health_check_protocol",
    healthCheckTimeoutSeconds: "health_check_timeout_seconds",
    healthyThresholdCount: "healthy_threshold_count",
    ipAddressType: "ip_address_type",
    matcher: {
      wireName: "matcher",
      kind: "object",
      fields: LoadBalancingV2TargetGroup_MatcherFields,
    },
    name: "name",
    port: "port",
    protocol: "protocol",
    protocolVersion: "protocol_version",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: LoadBalancingV2TargetGroup_TagsFields,
    },
    targetControlPort: "target_control_port",
    targetGroupAttributes: {
      wireName: "target_group_attributes",
      kind: "list",
      fields: LoadBalancingV2TargetGroup_TagsFields,
    },
    targetType: "target_type",
    targets: {
      wireName: "targets",
      kind: "list",
      fields: LoadBalancingV2TargetGroup_TargetsFields,
    },
    unhealthyThresholdCount: "unhealthy_threshold_count",
    vpcId: "vpc_id",
  },
};
