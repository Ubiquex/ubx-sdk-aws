// Code generated by `ubx sdk gen` from aws_route53_recovery_control_safety_rule. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface SafetyRule_AssertionRule {
  /** The routing controls that are part of transactions that are evaluated to determine if a request to change a routing control state is allowed. For example, you might include three routing controls, one for each of three AWS Regions. */
  assertedControls: string[] | Computed<string[]>;
  /** An evaluation period, in milliseconds (ms), during which any request against the target routing controls will fail. This helps prevent "flapping" of state. The wait period is 5000 ms by default, but you can choose a custom value. */
  waitPeriodMs: number | Computed<number>;
}

export interface SafetyRule_GatingRule {
  /** The gating controls for the gating rule. That is, routing controls that are evaluated by the rule configuration that you specify. */
  gatingControls: string[] | Computed<string[]>;
  /** Routing controls that can only be set or unset if the specified RuleConfig evaluates to true for the specified GatingControls. For example, say you have three gating controls, one for each of three AWS Regions. Now you specify AtLeast 2 as your RuleConfig. With these settings, you can only change (set or unset) the routing controls that you have specified as TargetControls if that rule evaluates to true. In other words, your ability to change the routing controls that you have specified as TargetControls is gated by the rule that you set for the routing controls in GatingControls. */
  targetControls: string[] | Computed<string[]>;
  /** An evaluation period, in milliseconds (ms), during which any request against the target routing controls will fail. This helps prevent "flapping" of state. The wait period is 5000 ms by default, but you can choose a custom value. */
  waitPeriodMs: number | Computed<number>;
}

export interface SafetyRule_RuleConfig {
  /** Logical negation of the rule. If the rule would usually evaluate true, it's evaluated as false, and vice versa. */
  inverted: boolean | Computed<boolean>;
  /** The value of N, when you specify an ATLEAST rule type. That is, Threshold is the number of controls that must be set when you specify an ATLEAST type. */
  threshold: number | Computed<number>;
  /** A rule can be one of the following: ATLEAST, AND, or OR. */
  type: string | Computed<string>;
}

export interface SafetyRule_Tags {
  /** The key of a tag applied to the Route 53 Recovery Control safety rule, used to categorize and manage the resource. (AI-inferred) */
  key?: string | Computed<string>;
  /** The value of an individual tag assigned to the AWS Route53 Recovery Control safety rule, used for metadata-based categorization and management of the rule. (AI-inferred) */
  value?: string | Computed<string>;
}

const SafetyRule_AssertionRuleFields: FieldMap = {
  assertedControls: "asserted_controls",
  waitPeriodMs: "wait_period_ms",
};

const SafetyRule_GatingRuleFields: FieldMap = {
  gatingControls: "gating_controls",
  targetControls: "target_controls",
  waitPeriodMs: "wait_period_ms",
};

const SafetyRule_RuleConfigFields: FieldMap = {
  inverted: "inverted",
  threshold: "threshold",
  type: "type",
};

const SafetyRule_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface SafetyRuleConfig {
  /** An assertion rule enforces that, when a routing control state is changed, that the criteria set by the rule configuration is met. Otherwise, the change to the routing control is not accepted. */
  assertionRule?: SafetyRule_AssertionRule | Computed<SafetyRule_AssertionRule>;
  /** The Amazon Resource Name (ARN) of the control panel. */
  controlPanelArn?: string | Computed<string>;
  /** A gating rule verifies that a set of gating controls evaluates as true, based on a rule configuration that you specify. If the gating rule evaluates to true, Amazon Route 53 Application Recovery Controller allows a set of routing control state changes to run and complete against the set of target controls. */
  gatingRule?: SafetyRule_GatingRule | Computed<SafetyRule_GatingRule>;
  /** The name for the safety rule. */
  name?: string | Computed<string>;
  /** The rule configuration for an assertion rule or gating rule. This is the criteria that you set for specific assertion controls (routing controls) or gating controls. This configuration specifies how many controls must be enabled after a transaction completes. */
  ruleConfig?: SafetyRule_RuleConfig | Computed<SafetyRule_RuleConfig>;
  /** A collection of tags associated with a resource */
  tags?: SafetyRule_Tags[] | Computed<SafetyRule_Tags[]>;
}

export interface SafetyRuleAttrs {
  /** An assertion rule enforces that, when a routing control state is changed, that the criteria set by the rule configuration is met. Otherwise, the change to the routing control is not accepted. */
  assertionRule: SafetyRule_AssertionRule;
  /** The Amazon Resource Name (ARN) of the control panel. */
  controlPanelArn: string;
  /** A gating rule verifies that a set of gating controls evaluates as true, based on a rule configuration that you specify. If the gating rule evaluates to true, Amazon Route 53 Application Recovery Controller allows a set of routing control state changes to run and complete against the set of target controls. */
  gatingRule: SafetyRule_GatingRule;
  /** The name for the safety rule. */
  name: string;
  /** The rule configuration for an assertion rule or gating rule. This is the criteria that you set for specific assertion controls (routing controls) or gating controls. This configuration specifies how many controls must be enabled after a transaction completes. */
  ruleConfig: SafetyRule_RuleConfig;
  /** The Amazon Resource Name (ARN) of the safety rule. */
  safetyRuleArn: string;
  /** The deployment status of the routing control. Status can be one of the following: PENDING, DEPLOYED, PENDING_DELETION. */
  status: string;
  /** A collection of tags associated with a resource */
  tags: SafetyRule_Tags[];
}

export const SafetyRule: ResourceBinding<SafetyRuleConfig, SafetyRuleAttrs> = {
  wireType: "aws_route53_recovery_control_safety_rule",
  fields: {
    assertionRule: {
      wireName: "assertion_rule",
      kind: "object",
      fields: SafetyRule_AssertionRuleFields,
    },
    controlPanelArn: "control_panel_arn",
    gatingRule: {
      wireName: "gating_rule",
      kind: "object",
      fields: SafetyRule_GatingRuleFields,
    },
    name: "name",
    ruleConfig: {
      wireName: "rule_config",
      kind: "object",
      fields: SafetyRule_RuleConfigFields,
    },
    tags: {
      wireName: "tags",
      kind: "list",
      fields: SafetyRule_TagsFields,
    },
  },
};
