// Code generated by `ubx sdk gen` from aws_quick_sight_approval_policy. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface SightApprovalPolicy_ApplicableTo {
  /** Required when type = GROUP. One or more group ARNs. */
  groupArns?: string[] | Computed<string[]>;
  type: unknown | Computed<unknown>;
}

const SightApprovalPolicy_ApplicableToFields: FieldMap = {
  groupArns: "group_arns",
  type: "type",
};

export interface SightApprovalPolicyConfig {
  /** List of governed actions a policy applies to. */
  actions: unknown[] | Computed<unknown[]>;
  /** Scoping: who the policy applies to. GROUP: `groupArns` required (one or more group ARNs). */
  applicableTo: SightApprovalPolicy_ApplicableTo | Computed<SightApprovalPolicy_ApplicableTo>;
  /** List of approval group ARNs (e.g. QuickSight group ARNs). At least one approval group is required; the upper bound is enforced per-account at the service layer via the configurable approver-group limit. */
  approvalGroups: string[] | Computed<string[]>;
  /** List of asset types a policy applies to. At least one asset type is required. */
  assetTypes: unknown[] | Computed<unknown[]>;
  description?: string | Computed<string>;
  name: string | Computed<string>;
  /** The unique identifier for a QuickSight approval policy that governs whether asset publishing requires approval before the asset is shared. (AI-inferred) */
  policyId: string | Computed<string>;
}

export interface SightApprovalPolicyAttrs {
  /** List of governed actions a policy applies to. */
  actions: unknown[];
  /** Scoping: who the policy applies to. GROUP: `groupArns` required (one or more group ARNs). */
  applicableTo: SightApprovalPolicy_ApplicableTo;
  /** List of approval group ARNs (e.g. QuickSight group ARNs). At least one approval group is required; the upper bound is enforced per-account at the service layer via the configurable approver-group limit. */
  approvalGroups: string[];
  /** List of asset types a policy applies to. At least one asset type is required. */
  assetTypes: unknown[];
  createdAt: string;
  description: string;
  name: string;
  /** The Amazon Resource Name (ARN) that uniquely identifies the QuickSight approval policy, used as a reference in IAM policies and API operations. (AI-inferred) */
  policyArn: string;
  /** The unique identifier for a QuickSight approval policy that governs whether asset publishing requires approval before the asset is shared. (AI-inferred) */
  policyId: string;
  updatedAt: string;
}

export const SightApprovalPolicy: ResourceBinding<SightApprovalPolicyConfig, SightApprovalPolicyAttrs> = {
  wireType: "aws_quick_sight_approval_policy",
  fields: {
    actions: "actions",
    applicableTo: {
      wireName: "applicable_to",
      kind: "object",
      fields: SightApprovalPolicy_ApplicableToFields,
    },
    approvalGroups: "approval_groups",
    assetTypes: "asset_types",
    description: "description",
    name: "name",
    policyId: "policy_id",
  },
};
