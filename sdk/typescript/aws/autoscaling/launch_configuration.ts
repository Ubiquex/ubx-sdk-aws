// Code generated by `ubx sdk gen` from aws_launch_configuration. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface LaunchConfiguration_BlockDeviceMappings_Ebs {
  /** Controls whether the EBS volume defined in this block device mapping is automatically deleted when the EC2 instance terminates. (AI-inferred) */
  deleteOnTermination?: boolean | Computed<boolean>;
  /** Whether the EBS volume in this block device mapping is encrypted, using the default AWS KMS key or the one specified by kms_key_id. (AI-inferred) */
  encrypted?: boolean | Computed<boolean>;
  /** The number of I/O operations per second (IOPS) to provision for the EBS volume, which is required when the volume type is `io1` (provisioned IOPS SSD) in the launch configuration's block device mapping. (AI-inferred) */
  iops?: number | Computed<number>;
  /** The ID of the snapshot to use as the source for the EBS volume in this block device mapping. (AI-inferred) */
  snapshotId?: string | Computed<string>;
  /** The throughput (in MiB/s) to provision for the EBS volume, which is applicable only when the volume type is gp3. (AI-inferred) */
  throughput?: number | Computed<number>;
  /** The size of the Amazon EBS volume, in GiB, for the block device mapping in the Auto Scaling launch configuration. (AI-inferred) */
  volumeSize?: number | Computed<number>;
  /** Specifies the Amazon EBS volume type (e.g., gp2, gp3, io1, st1, sc1, standard) for the block device mapped in the launch configuration, affecting performance and cost. (AI-inferred) */
  volumeType?: string | Computed<string>;
}

export interface LaunchConfiguration_BlockDeviceMappings {
  /** The device name (e.g., /dev/xvda, /dev/sdb) exposed to the EC2 instance for this block device mapping in the launch configuration. (AI-inferred) */
  deviceName?: string | Computed<string>;
  /** The EBS-specific settings (e.g., volume size, type, IOPS, snapshot, encryption, delete-on-termination) for the volume attached via this block device mapping in the Auto Scaling launch configuration. (AI-inferred) */
  ebs?: LaunchConfiguration_BlockDeviceMappings_Ebs | Computed<LaunchConfiguration_BlockDeviceMappings_Ebs>;
  /** When set to true, suppresses the block device mapping for the specified device name, so the launch configuration does not attach that device to instances launched from it. (AI-inferred) */
  noDevice?: boolean | Computed<boolean>;
  /** Specifies the virtual device name (such as ephemeral0 or ephemeral1) for an ephemeral (instance store) block device in the launch configuration's block device mapping. (AI-inferred) */
  virtualName?: string | Computed<string>;
}

export interface LaunchConfiguration_MetadataOptions {
  /** This parameter enables or disables the HTTP metadata endpoint on your instances. */
  httpEndpoint?: string | Computed<string>;
  /** The desired HTTP PUT response hop limit for instance metadata requests. */
  httpPutResponseHopLimit?: number | Computed<number>;
  /** The state of token usage for your instance metadata requests. */
  httpTokens?: string | Computed<string>;
}

const LaunchConfiguration_BlockDeviceMappings_EbsFields: FieldMap = {
  deleteOnTermination: "delete_on_termination",
  encrypted: "encrypted",
  iops: "iops",
  snapshotId: "snapshot_id",
  throughput: "throughput",
  volumeSize: "volume_size",
  volumeType: "volume_type",
};

const LaunchConfiguration_BlockDeviceMappingsFields: FieldMap = {
  deviceName: "device_name",
  ebs: {
    wireName: "ebs",
    kind: "object",
    fields: LaunchConfiguration_BlockDeviceMappings_EbsFields,
  },
  noDevice: "no_device",
  virtualName: "virtual_name",
};

const LaunchConfiguration_MetadataOptionsFields: FieldMap = {
  httpEndpoint: "http_endpoint",
  httpPutResponseHopLimit: "http_put_response_hop_limit",
  httpTokens: "http_tokens",
};

export interface LaunchConfigurationConfig {
  /** For Auto Scaling groups that are running in a virtual private cloud (VPC), specifies whether to assign a public IP address to the group's instances. */
  associatePublicIpAddress?: boolean | Computed<boolean>;
  /** Specifies how block devices are exposed to the instance. You can specify virtual devices and EBS volumes. */
  blockDeviceMappings?: LaunchConfiguration_BlockDeviceMappings[] | Computed<LaunchConfiguration_BlockDeviceMappings[]>;
  /** The ID of a ClassicLink-enabled VPC to link your EC2-Classic instances to. */
  classicLinkVpcid?: string | Computed<string>;
  /** The IDs of one or more security groups for the VPC that you specified in the ClassicLinkVPCId property. */
  classicLinkVpcsecurityGroups?: string[] | Computed<string[]>;
  /** Specifies whether the launch configuration is optimized for EBS I/O (true) or not (false). */
  ebsOptimized?: boolean | Computed<boolean>;
  /** Provides the name or the Amazon Resource Name (ARN) of the instance profile associated with the IAM role for the instance. The instance profile contains the IAM role. */
  iamInstanceProfile?: string | Computed<string>;
  /** Provides the unique ID of the Amazon Machine Image (AMI) that was assigned during registration. */
  imageId: string | Computed<string>;
  /** The ID of the Amazon EC2 instance you want to use to create the launch configuration. */
  instanceId?: string | Computed<string>;
  /** Controls whether instances in this group are launched with detailed (true) or basic (false) monitoring. */
  instanceMonitoring?: boolean | Computed<boolean>;
  /** Specifies the instance type of the EC2 instance. */
  instanceType: string | Computed<string>;
  /** Provides the ID of the kernel associated with the EC2 AMI. */
  kernelId?: string | Computed<string>;
  /** Provides the name of the EC2 key pair. */
  keyName?: string | Computed<string>;
  /** The name of the launch configuration. This name must be unique per Region per account. */
  launchConfigurationName?: string | Computed<string>;
  /** MetadataOptions is a property of AWS::AutoScaling::LaunchConfiguration that describes metadata options for the instances. */
  metadataOptions?: LaunchConfiguration_MetadataOptions | Computed<LaunchConfiguration_MetadataOptions>;
  /** The tenancy of the instance, either default or dedicated. */
  placementTenancy?: string | Computed<string>;
  /** The ID of the RAM disk to select. */
  ramDiskId?: string | Computed<string>;
  /** A list that contains the security groups to assign to the instances in the Auto Scaling group. */
  securityGroups?: unknown[] | Computed<unknown[]>;
  /** The maximum hourly price you are willing to pay for any Spot Instances launched to fulfill the request. */
  spotPrice?: string | Computed<string>;
  /** The Base64-encoded user data to make available to the launched EC2 instances. */
  userData?: string | Computed<string>;
}

export interface LaunchConfigurationAttrs {
  /** For Auto Scaling groups that are running in a virtual private cloud (VPC), specifies whether to assign a public IP address to the group's instances. */
  associatePublicIpAddress: boolean;
  /** Specifies how block devices are exposed to the instance. You can specify virtual devices and EBS volumes. */
  blockDeviceMappings: LaunchConfiguration_BlockDeviceMappings[];
  /** The ID of a ClassicLink-enabled VPC to link your EC2-Classic instances to. */
  classicLinkVpcid: string;
  /** The IDs of one or more security groups for the VPC that you specified in the ClassicLinkVPCId property. */
  classicLinkVpcsecurityGroups: string[];
  /** Specifies whether the launch configuration is optimized for EBS I/O (true) or not (false). */
  ebsOptimized: boolean;
  /** Provides the name or the Amazon Resource Name (ARN) of the instance profile associated with the IAM role for the instance. The instance profile contains the IAM role. */
  iamInstanceProfile: string;
  /** Provides the unique ID of the Amazon Machine Image (AMI) that was assigned during registration. */
  imageId: string;
  /** The ID of the Amazon EC2 instance you want to use to create the launch configuration. */
  instanceId: string;
  /** Controls whether instances in this group are launched with detailed (true) or basic (false) monitoring. */
  instanceMonitoring: boolean;
  /** Specifies the instance type of the EC2 instance. */
  instanceType: string;
  /** Provides the ID of the kernel associated with the EC2 AMI. */
  kernelId: string;
  /** Provides the name of the EC2 key pair. */
  keyName: string;
  /** The name of the launch configuration. This name must be unique per Region per account. */
  launchConfigurationName: string;
  /** MetadataOptions is a property of AWS::AutoScaling::LaunchConfiguration that describes metadata options for the instances. */
  metadataOptions: LaunchConfiguration_MetadataOptions;
  /** The tenancy of the instance, either default or dedicated. */
  placementTenancy: string;
  /** The ID of the RAM disk to select. */
  ramDiskId: string;
  /** A list that contains the security groups to assign to the instances in the Auto Scaling group. */
  securityGroups: unknown[];
  /** The maximum hourly price you are willing to pay for any Spot Instances launched to fulfill the request. */
  spotPrice: string;
  /** The Base64-encoded user data to make available to the launched EC2 instances. */
  userData: string;
}

export const LaunchConfiguration: ResourceBinding<LaunchConfigurationConfig, LaunchConfigurationAttrs> = {
  wireType: "aws_launch_configuration",
  fields: {
    associatePublicIpAddress: "associate_public_ip_address",
    blockDeviceMappings: {
      wireName: "block_device_mappings",
      kind: "list",
      fields: LaunchConfiguration_BlockDeviceMappingsFields,
    },
    classicLinkVpcid: "classic_link_vpcid",
    classicLinkVpcsecurityGroups: "classic_link_vpcsecurity_groups",
    ebsOptimized: "ebs_optimized",
    iamInstanceProfile: "iam_instance_profile",
    imageId: "image_id",
    instanceId: "instance_id",
    instanceMonitoring: "instance_monitoring",
    instanceType: "instance_type",
    kernelId: "kernel_id",
    keyName: "key_name",
    launchConfigurationName: "launch_configuration_name",
    metadataOptions: {
      wireName: "metadata_options",
      kind: "object",
      fields: LaunchConfiguration_MetadataOptionsFields,
    },
    placementTenancy: "placement_tenancy",
    ramDiskId: "ram_disk_id",
    securityGroups: "security_groups",
    spotPrice: "spot_price",
    userData: "user_data",
  },
};
