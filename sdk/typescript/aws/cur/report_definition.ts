// Code generated by `ubx sdk gen` from aws_cur_report_definition. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface ReportDefinition_Tags {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

const ReportDefinition_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface ReportDefinitionConfig {
  /** A list of manifests that you want Amazon Web Services to create for this report. */
  additionalArtifacts?: string[] | Computed<string[]>;
  /** A list of strings that indicate additional content that Amazon Web Services includes in the report, such as individual resource IDs. */
  additionalSchemaElements?: string[] | Computed<string[]>;
  /** The Amazon resource name of the billing view. You can get this value by using the billing view service public APIs. */
  billingViewArn?: string | Computed<string>;
  /** The compression format that AWS uses for the report. */
  compression: string | Computed<string>;
  /** The format that AWS saves the report in. */
  format: string | Computed<string>;
  /** Whether you want Amazon Web Services to update your reports after they have been finalized if Amazon Web Services detects charges related to previous months. These charges can include refunds, credits, or support fees. */
  refreshClosedReports: boolean | Computed<boolean>;
  /** The name of the report that you want to create. The name must be unique, is case sensitive, and can't include spaces. */
  reportName: string | Computed<string>;
  /** Whether you want Amazon Web Services to overwrite the previous version of each report or to deliver the report in addition to the previous versions. */
  reportVersioning: string | Computed<string>;
  /** The S3 bucket where AWS delivers the report. */
  s3Bucket: string | Computed<string>;
  /** The prefix that AWS adds to the report name when AWS delivers the report. Your prefix can't include spaces. */
  s3Prefix: string | Computed<string>;
  /** The region of the S3 bucket that AWS delivers the report into. */
  s3Region: string | Computed<string>;
  tags?: ReportDefinition_Tags[] | Computed<ReportDefinition_Tags[]>;
  /** The granularity of the line items in the report. */
  timeUnit: string | Computed<string>;
}

export interface ReportDefinitionAttrs {
  /** A list of manifests that you want Amazon Web Services to create for this report. */
  additionalArtifacts: string[];
  /** A list of strings that indicate additional content that Amazon Web Services includes in the report, such as individual resource IDs. */
  additionalSchemaElements: string[];
  /** The Amazon resource name of the billing view. You can get this value by using the billing view service public APIs. */
  billingViewArn: string;
  /** The compression format that AWS uses for the report. */
  compression: string;
  /** The format that AWS saves the report in. */
  format: string;
  /** Whether you want Amazon Web Services to update your reports after they have been finalized if Amazon Web Services detects charges related to previous months. These charges can include refunds, credits, or support fees. */
  refreshClosedReports: boolean;
  /** The name of the report that you want to create. The name must be unique, is case sensitive, and can't include spaces. */
  reportName: string;
  /** Whether you want Amazon Web Services to overwrite the previous version of each report or to deliver the report in addition to the previous versions. */
  reportVersioning: string;
  /** The S3 bucket where AWS delivers the report. */
  s3Bucket: string;
  /** The prefix that AWS adds to the report name when AWS delivers the report. Your prefix can't include spaces. */
  s3Prefix: string;
  /** The region of the S3 bucket that AWS delivers the report into. */
  s3Region: string;
  tags: ReportDefinition_Tags[];
  /** The granularity of the line items in the report. */
  timeUnit: string;
}

export const ReportDefinition: ResourceBinding<ReportDefinitionConfig, ReportDefinitionAttrs> = {
  wireType: "aws_cur_report_definition",
  fields: {
    additionalArtifacts: "additional_artifacts",
    additionalSchemaElements: "additional_schema_elements",
    billingViewArn: "billing_view_arn",
    compression: "compression",
    format: "format",
    refreshClosedReports: "refresh_closed_reports",
    reportName: "report_name",
    reportVersioning: "report_versioning",
    s3Bucket: "s3_bucket",
    s3Prefix: "s3_prefix",
    s3Region: "s3_region",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: ReportDefinition_TagsFields,
    },
    timeUnit: "time_unit",
  },
};
