// Code generated by `ubx sdk gen` from aws_cases_template. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Template_LayoutConfiguration {
  /** The unique identifier of a layout. */
  defaultLayout?: string | Computed<string>;
}

export interface Template_RequiredFields {
  fieldId?: string | Computed<string>;
}

export interface Template_Rules {
  caseRuleId?: string | Computed<string>;
  fieldId?: string | Computed<string>;
}

export interface Template_Tags {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

const Template_LayoutConfigurationFields: FieldMap = {
  defaultLayout: "default_layout",
};

const Template_RequiredFieldsFields: FieldMap = {
  fieldId: "field_id",
};

const Template_RulesFields: FieldMap = {
  caseRuleId: "case_rule_id",
  fieldId: "field_id",
};

const Template_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface TemplateConfig {
  /** A description explaining the purpose and use case for this template. Should indicate what types of cases this template is designed for and any specific workflow it supports. */
  description?: string | Computed<string>;
  /** The unique identifier of the Cases domain. */
  domainId?: string | Computed<string>;
  /** Specifies the default layout to use when displaying cases created from this template. The layout determines which fields are visible and their arrangement in the agent interface. */
  layoutConfiguration?: Template_LayoutConfiguration | Computed<Template_LayoutConfiguration>;
  /** A name for the template. It must be unique per domain. */
  name: string | Computed<string>;
  /** A list of fields that must contain a value for a case to be successfully created with this template. */
  requiredFields?: Template_RequiredFields[] | Computed<Template_RequiredFields[]>;
  /** A list of case rules (also known as case field conditions) on a template. */
  rules?: Template_Rules[] | Computed<Template_Rules[]>;
  /** The current status of the template. Active templates can be used to create new cases, while Inactive templates are disabled but preserved for existing cases. */
  status?: string | Computed<string>;
  /** The tags that you attach to this template. */
  tags?: Template_Tags[] | Computed<Template_Tags[]>;
}

export interface TemplateAttrs {
  /** The time at which the template was created. */
  createdTime: string;
  /** A description explaining the purpose and use case for this template. Should indicate what types of cases this template is designed for and any specific workflow it supports. */
  description: string;
  /** The unique identifier of the Cases domain. */
  domainId: string;
  /** The time at which the template was created or last modified. */
  lastModifiedTime: string;
  /** Specifies the default layout to use when displaying cases created from this template. The layout determines which fields are visible and their arrangement in the agent interface. */
  layoutConfiguration: Template_LayoutConfiguration;
  /** A name for the template. It must be unique per domain. */
  name: string;
  /** A list of fields that must contain a value for a case to be successfully created with this template. */
  requiredFields: Template_RequiredFields[];
  /** A list of case rules (also known as case field conditions) on a template. */
  rules: Template_Rules[];
  /** The current status of the template. Active templates can be used to create new cases, while Inactive templates are disabled but preserved for existing cases. */
  status: string;
  /** The tags that you attach to this template. */
  tags: Template_Tags[];
  /** The Amazon Resource Name (ARN) of the template. */
  templateArn: string;
  /** The unique identifier of a template. */
  templateId: string;
}

export const Template: ResourceBinding<TemplateConfig, TemplateAttrs> = {
  wireType: "aws_cases_template",
  fields: {
    description: "description",
    domainId: "domain_id",
    layoutConfiguration: {
      wireName: "layout_configuration",
      kind: "object",
      fields: Template_LayoutConfigurationFields,
    },
    name: "name",
    requiredFields: {
      wireName: "required_fields",
      kind: "list",
      fields: Template_RequiredFieldsFields,
    },
    rules: {
      wireName: "rules",
      kind: "list",
      fields: Template_RulesFields,
    },
    status: "status",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: Template_TagsFields,
    },
  },
};
