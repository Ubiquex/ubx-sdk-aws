// Code generated by `ubx sdk gen` from aws_customer_profiles_domain. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Domain_DataStore_Readiness {
  /** A message describing the current progress. */
  message?: string | Computed<string>;
  /** The percentage of progress completed. */
  progressPercentage?: number | Computed<number>;
}

export interface Domain_DataStore {
  /** Whether the data store is enabled. */
  enabled?: boolean | Computed<boolean>;
  /** Progress information for data store setup. */
  readiness?: Domain_DataStore_Readiness | Computed<Domain_DataStore_Readiness>;
}

export interface Domain_Matching_AutoMerging_ConflictResolution {
  /** How the auto-merging process should resolve conflicts between different profiles. */
  conflictResolvingModel: string | Computed<string>;
  /** The ObjectType name that is used to resolve profile merging conflicts when choosing SOURCE as the ConflictResolvingModel. */
  sourceName?: string | Computed<string>;
}

export interface Domain_Matching_AutoMerging_Consolidation {
  /** A list of matching criteria. */
  matchingAttributesList: string[][] | Computed<string[][]>;
}

export interface Domain_Matching_AutoMerging {
  /** How the auto-merging process should resolve conflicts between different profiles. For example, if Profile A and Profile B have the same FirstName and LastName (and that is the matching criteria), which EmailAddress should be used? */
  conflictResolution?: Domain_Matching_AutoMerging_ConflictResolution | Computed<Domain_Matching_AutoMerging_ConflictResolution>;
  /** A list of matching attributes that represent matching criteria. If two profiles meet at least one of the requirements in the matching attributes list, they will be merged. */
  consolidation?: Domain_Matching_AutoMerging_Consolidation | Computed<Domain_Matching_AutoMerging_Consolidation>;
  /** The flag that enables the auto-merging of duplicate profiles. */
  enabled: boolean | Computed<boolean>;
  /** A number between 0 and 1 that represents the minimum confidence score required for profiles within a matching group to be merged during the auto-merge process. A higher score means higher similarity required to merge profiles. */
  minAllowedConfidenceScoreForMerging?: number | Computed<number>;
}

export interface Domain_Matching_ExportingConfig_S3Exporting {
  /** The name of the S3 bucket where Identity Resolution Jobs write result files. */
  s3BucketName: string | Computed<string>;
  /** The S3 key name of the location where Identity Resolution Jobs write result files. */
  s3KeyName?: string | Computed<string>;
}

export interface Domain_Matching_ExportingConfig {
  /** The S3 location where Identity Resolution Jobs write result files. */
  s3Exporting?: Domain_Matching_ExportingConfig_S3Exporting | Computed<Domain_Matching_ExportingConfig_S3Exporting>;
}

export interface Domain_Matching_JobSchedule {
  /** The day when the Identity Resolution Job should run every week. */
  dayOfTheWeek: string | Computed<string>;
  /** The time when the Identity Resolution Job should run every week. */
  time: string | Computed<string>;
}

export interface Domain_Matching {
  /** Configuration information about the auto-merging process. */
  autoMerging?: Domain_Matching_AutoMerging | Computed<Domain_Matching_AutoMerging>;
  /** The flag that enables the matching process of duplicate profiles. */
  enabled: boolean | Computed<boolean>;
  /** Configuration information for exporting Identity Resolution results, for example, to an S3 bucket. */
  exportingConfig?: Domain_Matching_ExportingConfig | Computed<Domain_Matching_ExportingConfig>;
  /** The day and time when do you want to start the Identity Resolution Job every week. */
  jobSchedule?: Domain_Matching_JobSchedule | Computed<Domain_Matching_JobSchedule>;
}

export interface Domain_RuleBasedMatching_AttributeTypesSelector {
  /** The Address type. You can choose from Address, BusinessAddress, MaillingAddress, and ShippingAddress. You only can use the Address type in the MatchingRule. For example, if you want to match profile based on BusinessAddress.City or MaillingAddress.City, you need to choose the BusinessAddress and the MaillingAddress to represent the Address type and specify the Address.City on the matching rule. */
  address?: string[] | Computed<string[]>;
  /** Configures the AttributeMatchingModel, you can either choose ONE_TO_ONE or MANY_TO_MANY. */
  attributeMatchingModel: string | Computed<string>;
  /** The Email type. You can choose from EmailAddress, BusinessEmailAddress and PersonalEmailAddress. You only can use the EmailAddress type in the MatchingRule. For example, if you want to match profile based on PersonalEmailAddress or BusinessEmailAddress, you need to choose the PersonalEmailAddress and the BusinessEmailAddress to represent the EmailAddress type and only specify the EmailAddress on the matching rule. */
  emailAddress?: string[] | Computed<string[]>;
  /** The PhoneNumber type. You can choose from PhoneNumber, HomePhoneNumber, and MobilePhoneNumber. You only can use the PhoneNumber type in the MatchingRule. For example, if you want to match a profile based on Phone or HomePhone, you need to choose the Phone and the HomePhone to represent the PhoneNumber type and only specify the PhoneNumber on the matching rule. */
  phoneNumber?: string[] | Computed<string[]>;
}

export interface Domain_RuleBasedMatching_MatchingRules {
  /** Defines a single matching rule by listing the profile attribute names (e.g., '_account.Phone') that must have identical values across profiles for them to be considered a match. (AI-inferred) */
  rule?: string[] | Computed<string[]>;
}

export interface Domain_RuleBasedMatching {
  /** Configures information about the AttributeTypesSelector where the rule-based identity resolution uses to match profiles. */
  attributeTypesSelector?: Domain_RuleBasedMatching_AttributeTypesSelector | Computed<Domain_RuleBasedMatching_AttributeTypesSelector>;
  /** How the auto-merging process should resolve conflicts between different profiles. For example, if Profile A and Profile B have the same FirstName and LastName (and that is the matching criteria), which EmailAddress should be used? */
  conflictResolution?: Domain_Matching_AutoMerging_ConflictResolution | Computed<Domain_Matching_AutoMerging_ConflictResolution>;
  /** The flag that enables the rule-based matching process of duplicate profiles. */
  enabled: boolean | Computed<boolean>;
  /** Configuration information for exporting Identity Resolution results, for example, to an S3 bucket. */
  exportingConfig?: Domain_Matching_ExportingConfig | Computed<Domain_Matching_ExportingConfig>;
  /** Configures how the rule-based matching process should match profiles. You can have up to 15 MatchingRule in the MatchingRules. */
  matchingRules?: Domain_RuleBasedMatching_MatchingRules[] | Computed<Domain_RuleBasedMatching_MatchingRules[]>;
  /** Indicates the maximum allowed rule level for matching. */
  maxAllowedRuleLevelForMatching?: number | Computed<number>;
  /** Indicates the maximum allowed rule level for merging. */
  maxAllowedRuleLevelForMerging?: number | Computed<number>;
  /** The current status of the rule-based matching configuration for the Customer Profiles domain, with values ACTIVE (enabled and running), IN_PROGRESS (being enabled or updated), or DISABLED (turned off). (AI-inferred) */
  status?: string | Computed<string>;
}

export interface Domain_Stats {
  /** The number of profiles that you are currently paying for in the domain. If you have more than 100 objects associated with a single profile, that profile counts as two profiles. If you have more than 200 objects, that profile counts as three, and so on. */
  meteringProfileCount?: number | Computed<number>;
  /** The total number of objects in domain. */
  objectCount?: number | Computed<number>;
  /** The total number of profiles currently in the domain. */
  profileCount?: number | Computed<number>;
  /** The total size, in bytes, of all objects in the domain. */
  totalSize?: number | Computed<number>;
}

export interface Domain_Tags {
  /** The key of a user-defined tag that can be attached to the Amazon Connect Customer Profiles domain. (AI-inferred) */
  key?: string | Computed<string>;
  /** The value of a tag attached to the Amazon Connect Customer Profiles domain, used to assign arbitrary metadata for resource organization and cost allocation. (AI-inferred) */
  value?: string | Computed<string>;
}

const Domain_Matching_AutoMerging_ConflictResolutionFields: FieldMap = {
  conflictResolvingModel: "conflict_resolving_model",
  sourceName: "source_name",
};

const Domain_Matching_AutoMerging_ConsolidationFields: FieldMap = {
  matchingAttributesList: "matching_attributes_list",
};

const Domain_Matching_AutoMergingFields: FieldMap = {
  conflictResolution: {
    wireName: "conflict_resolution",
    kind: "object",
    fields: Domain_Matching_AutoMerging_ConflictResolutionFields,
  },
  consolidation: {
    wireName: "consolidation",
    kind: "object",
    fields: Domain_Matching_AutoMerging_ConsolidationFields,
  },
  enabled: "enabled",
  minAllowedConfidenceScoreForMerging: "min_allowed_confidence_score_for_merging",
};

const Domain_Matching_ExportingConfig_S3ExportingFields: FieldMap = {
  s3BucketName: "s3_bucket_name",
  s3KeyName: "s3_key_name",
};

const Domain_Matching_ExportingConfigFields: FieldMap = {
  s3Exporting: {
    wireName: "s3_exporting",
    kind: "object",
    fields: Domain_Matching_ExportingConfig_S3ExportingFields,
  },
};

const Domain_Matching_JobScheduleFields: FieldMap = {
  dayOfTheWeek: "day_of_the_week",
  time: "time",
};

const Domain_MatchingFields: FieldMap = {
  autoMerging: {
    wireName: "auto_merging",
    kind: "object",
    fields: Domain_Matching_AutoMergingFields,
  },
  enabled: "enabled",
  exportingConfig: {
    wireName: "exporting_config",
    kind: "object",
    fields: Domain_Matching_ExportingConfigFields,
  },
  jobSchedule: {
    wireName: "job_schedule",
    kind: "object",
    fields: Domain_Matching_JobScheduleFields,
  },
};

const Domain_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface DomainConfig {
  /** The URL of the SQS dead letter queue */
  deadLetterQueueUrl?: string | Computed<string>;
  /** The default encryption key */
  defaultEncryptionKey?: string | Computed<string>;
  /** The default number of days until the data within the domain expires. */
  defaultExpirationDays: number | Computed<number>;
  /** The unique name of the domain. */
  domainName: string | Computed<string>;
  /** The process of matching duplicate profiles. If Matching = true, Amazon Connect Customer Profiles starts a weekly batch process called Identity Resolution Job. If you do not specify a date and time for Identity Resolution Job to run, by default it runs every Saturday at 12AM UTC to detect duplicate profiles in your domains. After the Identity Resolution Job completes, use the GetMatches API to return and review the results. Or, if you have configured ExportingConfig in the MatchingRequest, you can download the results from S3. */
  matching?: Domain_Matching | Computed<Domain_Matching>;
  /** The tags (keys and values) associated with the domain */
  tags?: Domain_Tags[] | Computed<Domain_Tags[]>;
}

export interface DomainAttrs {
  /** The time of this integration got created */
  createdAt: string;
  /** Configuration and status of the data store for the domain. */
  dataStore: Domain_DataStore;
  /** The URL of the SQS dead letter queue */
  deadLetterQueueUrl: string;
  /** The default encryption key */
  defaultEncryptionKey: string;
  /** The default number of days until the data within the domain expires. */
  defaultExpirationDays: number;
  /** The unique name of the domain. */
  domainName: string;
  /** The time of this integration got last updated at */
  lastUpdatedAt: string;
  /** The process of matching duplicate profiles. If Matching = true, Amazon Connect Customer Profiles starts a weekly batch process called Identity Resolution Job. If you do not specify a date and time for Identity Resolution Job to run, by default it runs every Saturday at 12AM UTC to detect duplicate profiles in your domains. After the Identity Resolution Job completes, use the GetMatches API to return and review the results. Or, if you have configured ExportingConfig in the MatchingRequest, you can download the results from S3. */
  matching: Domain_Matching;
  /** The process of matching duplicate profiles using the Rule-Based matching. If RuleBasedMatching = true, Amazon Connect Customer Profiles will start to match and merge your profiles according to your configuration in the RuleBasedMatchingRequest. You can use the ListRuleBasedMatches and GetSimilarProfiles API to return and review the results. Also, if you have configured ExportingConfig in the RuleBasedMatchingRequest, you can download the results from S3. */
  ruleBasedMatching: Domain_RuleBasedMatching;
  /** Usage-specific statistics about the domain. */
  stats: Domain_Stats;
  /** The tags (keys and values) associated with the domain */
  tags: Domain_Tags[];
}

export const Domain: ResourceBinding<DomainConfig, DomainAttrs> = {
  wireType: "aws_customer_profiles_domain",
  fields: {
    deadLetterQueueUrl: "dead_letter_queue_url",
    defaultEncryptionKey: "default_encryption_key",
    defaultExpirationDays: "default_expiration_days",
    domainName: "domain_name",
    matching: {
      wireName: "matching",
      kind: "object",
      fields: Domain_MatchingFields,
    },
    tags: {
      wireName: "tags",
      kind: "list",
      fields: Domain_TagsFields,
    },
  },
};
