// Code generated by `ubx sdk gen` from aws_ec2_application_status_check. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface ApplicationStatusCheck_HealthCheckPaths_Destinations {
  securityGroupId?: string | Computed<string>;
  subnetId?: string | Computed<string>;
}

export interface ApplicationStatusCheck_HealthCheckPaths {
  destinations?: ApplicationStatusCheck_HealthCheckPaths_Destinations[] | Computed<ApplicationStatusCheck_HealthCheckPaths_Destinations[]>;
  source?: ApplicationStatusCheck_HealthCheckPaths_Destinations | Computed<ApplicationStatusCheck_HealthCheckPaths_Destinations>;
}

export interface ApplicationStatusCheck_Tags {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

const ApplicationStatusCheck_HealthCheckPaths_DestinationsFields: FieldMap = {
  securityGroupId: "security_group_id",
  subnetId: "subnet_id",
};

const ApplicationStatusCheck_HealthCheckPathsFields: FieldMap = {
  destinations: {
    wireName: "destinations",
    kind: "list",
    fields: ApplicationStatusCheck_HealthCheckPaths_DestinationsFields,
  },
  source: {
    wireName: "source",
    kind: "object",
    fields: ApplicationStatusCheck_HealthCheckPaths_DestinationsFields,
  },
};

const ApplicationStatusCheck_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface ApplicationStatusCheckConfig {
  /** Whether this check is included in the rolled-up application status. */
  aggregation?: string | Computed<string>;
  /** The network interface device index used for the health check. */
  deviceIndex?: number | Computed<number>;
  /** The number of consecutive failed probes required to mark the instance unhealthy. */
  failureThreshold?: number | Computed<number>;
  /** The source/destination network paths used for the health check. */
  healthCheckPaths?: ApplicationStatusCheck_HealthCheckPaths[] | Computed<ApplicationStatusCheck_HealthCheckPaths[]>;
  /** Seconds to wait after instance launch before beginning health checks. */
  initializationGracePeriodSeconds?: number | Computed<number>;
  /** The interval, in seconds, between health check probes. */
  interval?: number | Computed<number>;
  /** The IP scope used for the health check. */
  ipScope?: string | Computed<string>;
  /** The IP version used for the health check. */
  ipVersion?: string | Computed<string>;
  /** The HTTP path used for the health check. */
  path?: string | Computed<string>;
  /** The port used for the health check. */
  port: number | Computed<number>;
  /** The network protocol used for the health check. */
  protocol: string | Computed<string>;
  /** The HTTP status codes considered successful (e.g., "200-299"). */
  statusCodeMatcher?: string | Computed<string>;
  /** The number of consecutive successful probes required to mark the instance healthy. */
  successThreshold?: number | Computed<number>;
  /** Tags to apply to the application status check. */
  tags?: ApplicationStatusCheck_Tags[] | Computed<ApplicationStatusCheck_Tags[]>;
  /** The timeout, in seconds, for each health check probe. */
  timeout?: number | Computed<number>;
}

export interface ApplicationStatusCheckAttrs {
  /** Whether this check is included in the rolled-up application status. */
  aggregation: string;
  /** The unique identifier of the application status check. */
  applicationStatusCheckId: string;
  /** The Amazon Resource Name (ARN) of the application status check. */
  arn: string;
  /** When the application status check was created (ISO 8601). */
  creationTime: string;
  /** The network interface device index used for the health check. */
  deviceIndex: number;
  /** The number of consecutive failed probes required to mark the instance unhealthy. */
  failureThreshold: number;
  /** The source/destination network paths used for the health check. */
  healthCheckPaths: ApplicationStatusCheck_HealthCheckPaths[];
  /** Seconds to wait after instance launch before beginning health checks. */
  initializationGracePeriodSeconds: number;
  /** The interval, in seconds, between health check probes. */
  interval: number;
  /** The IP scope used for the health check. */
  ipScope: string;
  /** The IP version used for the health check. */
  ipVersion: string;
  /** The HTTP path used for the health check. */
  path: string;
  /** The port used for the health check. */
  port: number;
  /** The network protocol used for the health check. */
  protocol: string;
  /** The HTTP status codes considered successful (e.g., "200-299"). */
  statusCodeMatcher: string;
  /** The number of consecutive successful probes required to mark the instance healthy. */
  successThreshold: number;
  /** Tags to apply to the application status check. */
  tags: ApplicationStatusCheck_Tags[];
  /** The timeout, in seconds, for each health check probe. */
  timeout: number;
}

export const ApplicationStatusCheck: ResourceBinding<ApplicationStatusCheckConfig, ApplicationStatusCheckAttrs> = {
  wireType: "aws_ec2_application_status_check",
  fields: {
    aggregation: "aggregation",
    deviceIndex: "device_index",
    failureThreshold: "failure_threshold",
    healthCheckPaths: {
      wireName: "health_check_paths",
      kind: "list",
      fields: ApplicationStatusCheck_HealthCheckPathsFields,
    },
    initializationGracePeriodSeconds: "initialization_grace_period_seconds",
    interval: "interval",
    ipScope: "ip_scope",
    ipVersion: "ip_version",
    path: "path",
    port: "port",
    protocol: "protocol",
    statusCodeMatcher: "status_code_matcher",
    successThreshold: "success_threshold",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: ApplicationStatusCheck_TagsFields,
    },
    timeout: "timeout",
  },
};
