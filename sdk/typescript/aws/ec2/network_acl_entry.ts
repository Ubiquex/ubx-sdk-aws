// Code generated by `ubx sdk gen` from aws_ec2_network_acl_entry. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface NetworkAclEntry_Icmp {
  code?: number | Computed<number>;
  type?: number | Computed<number>;
}

export interface NetworkAclEntry_PortRange {
  from?: number | Computed<number>;
  to?: number | Computed<number>;
}

const NetworkAclEntry_IcmpFields: FieldMap = {
  code: "code",
  type: "type",
};

const NetworkAclEntry_PortRangeFields: FieldMap = {
  from: "from",
  to: "to",
};

export interface NetworkAclEntryConfig {
  /** The IPv4 CIDR range to allow or deny, in CIDR notation (for example, 172.16.0.0/24). Requirement is conditional: You must specify the CidrBlock or Ipv6CidrBlock property */
  cidrBlock?: string | Computed<string>;
  /** Indicates whether this is an egress rule (rule is applied to traffic leaving the subnet) */
  egress?: boolean | Computed<boolean>;
  icmp?: NetworkAclEntry_Icmp | Computed<NetworkAclEntry_Icmp>;
  /** The IPv6 network range to allow or deny, in CIDR notation (for example 2001:db8:1234:1a00::/64) */
  ipv6CidrBlock?: string | Computed<string>;
  /** The ID of the network ACL */
  networkAclId: string | Computed<string>;
  portRange?: NetworkAclEntry_PortRange | Computed<NetworkAclEntry_PortRange>;
  /** The protocol number. A value of "-1" means all protocols. If you specify "-1" or a protocol number other than "6" (TCP), "17" (UDP), or "1" (ICMP), traffic on all ports is allowed, regardless of any ports or ICMP types or codes that you specify. If you specify protocol "58" (ICMPv6) and specify an IPv4 CIDR block, traffic for all ICMP types and codes allowed, regardless of any that you specify. If you specify protocol "58" (ICMPv6) and specify an IPv6 CIDR block, you must specify an ICMP type and code */
  protocol: number | Computed<number>;
  /** Indicates whether to allow or deny the traffic that matches the rule */
  ruleAction: string | Computed<string>;
  /** Rule number to assign to the entry, such as 100. ACL entries are processed in ascending order by rule number. Entries can't use the same rule number unless one is an egress rule and the other is an ingress rule */
  ruleNumber: number | Computed<number>;
}

export interface NetworkAclEntryAttrs {
  /** The IPv4 CIDR range to allow or deny, in CIDR notation (for example, 172.16.0.0/24). Requirement is conditional: You must specify the CidrBlock or Ipv6CidrBlock property */
  cidrBlock: string;
  /** Indicates whether this is an egress rule (rule is applied to traffic leaving the subnet) */
  egress: boolean;
  icmp: NetworkAclEntry_Icmp;
  id: string;
  /** The IPv6 network range to allow or deny, in CIDR notation (for example 2001:db8:1234:1a00::/64) */
  ipv6CidrBlock: string;
  /** The ID of the network ACL */
  networkAclId: string;
  portRange: NetworkAclEntry_PortRange;
  /** The protocol number. A value of "-1" means all protocols. If you specify "-1" or a protocol number other than "6" (TCP), "17" (UDP), or "1" (ICMP), traffic on all ports is allowed, regardless of any ports or ICMP types or codes that you specify. If you specify protocol "58" (ICMPv6) and specify an IPv4 CIDR block, traffic for all ICMP types and codes allowed, regardless of any that you specify. If you specify protocol "58" (ICMPv6) and specify an IPv6 CIDR block, you must specify an ICMP type and code */
  protocol: number;
  /** Indicates whether to allow or deny the traffic that matches the rule */
  ruleAction: string;
  /** Rule number to assign to the entry, such as 100. ACL entries are processed in ascending order by rule number. Entries can't use the same rule number unless one is an egress rule and the other is an ingress rule */
  ruleNumber: number;
}

export const NetworkAclEntry: ResourceBinding<NetworkAclEntryConfig, NetworkAclEntryAttrs> = {
  wireType: "aws_ec2_network_acl_entry",
  fields: {
    cidrBlock: "cidr_block",
    egress: "egress",
    icmp: {
      wireName: "icmp",
      kind: "object",
      fields: NetworkAclEntry_IcmpFields,
    },
    ipv6CidrBlock: "ipv6_cidr_block",
    networkAclId: "network_acl_id",
    portRange: {
      wireName: "port_range",
      kind: "object",
      fields: NetworkAclEntry_PortRangeFields,
    },
    protocol: "protocol",
    ruleAction: "rule_action",
    ruleNumber: "rule_number",
  },
};
