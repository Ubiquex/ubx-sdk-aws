// Code generated by `ubx sdk gen` from aws_ses_mail_manager_ingress_point. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface MailManagerIngressPoint_IngressPointConfiguration_TlsAuthConfiguration_TrustStore {
  /** Provides the PEM-encoded content of a CA certificate to be included in the trust store used for TLS client authentication for the ingress point. (AI-inferred) */
  cacontent?: string | Computed<string>;
  /** Specifies a PEM-encoded certificate revocation list (CRL) that the ingress point uses to check that client certificates presented during mutual TLS authentication have not been revoked. (AI-inferred) */
  crlContent?: string | Computed<string>;
  /** The ARN of the AWS KMS key used to encrypt the trust store that holds the CA certificates for TLS client authentication in the ingress point's configuration. (AI-inferred) */
  kmsKeyArn?: string | Computed<string>;
}

export interface MailManagerIngressPoint_IngressPointConfiguration_TlsAuthConfiguration {
  /** The `trust_store` block supplies the Amazon Resource Name of the AWS SES Mail Manager trust store that holds the CA certificates against which client certificates are verified for mutual TLS authentication on the ingress point. (AI-inferred) */
  trustStore?: MailManagerIngressPoint_IngressPointConfiguration_TlsAuthConfiguration_TrustStore | Computed<MailManagerIngressPoint_IngressPointConfiguration_TlsAuthConfiguration_TrustStore>;
}

export interface MailManagerIngressPoint_IngressPointConfiguration {
  /** The ARN of an AWS Secrets Manager secret that stores the SMTP password used to authenticate to the ingress point's SMTP endpoint. (AI-inferred) */
  secretArn?: string | Computed<string>;
  /** The SMTP password used for authenticated SMTP connections to the SES Mail Manager ingress point, provided directly in the configuration as an alternative to referencing a Secrets Manager secret ARN. (AI-inferred) */
  smtpPassword?: string | Computed<string>;
  /** Defines the mutual TLS authentication settings for the ingress point's TLS endpoint, such as the certificate authority used to verify client certificates. (AI-inferred) */
  tlsAuthConfiguration?: MailManagerIngressPoint_IngressPointConfiguration_TlsAuthConfiguration | Computed<MailManagerIngressPoint_IngressPointConfiguration_TlsAuthConfiguration>;
}

export interface MailManagerIngressPoint_NetworkConfiguration_PrivateNetworkConfiguration {
  /** The ID of the VPC interface endpoint that enables private access to the ingress point from within your VPC. (AI-inferred) */
  vpcEndpointId?: string | Computed<string>;
}

export interface MailManagerIngressPoint_NetworkConfiguration_PublicNetworkConfiguration_IpType {
}

export interface MailManagerIngressPoint_NetworkConfiguration_PublicNetworkConfiguration {
  /** Specifies whether the public ingest point uses IPv4 or IPv6 addressing, with allowed values 'IPV4' or 'IPV6'. (AI-inferred) */
  ipType?: MailManagerIngressPoint_NetworkConfiguration_PublicNetworkConfiguration_IpType | Computed<MailManagerIngressPoint_NetworkConfiguration_PublicNetworkConfiguration_IpType>;
}

export interface MailManagerIngressPoint_NetworkConfiguration {
  /** Specifies the VPC and subnet settings that enable the ingress point to receive email traffic over a private network, allowing mail to be delivered from within a private Amazon VPC rather than over the public internet. (AI-inferred) */
  privateNetworkConfiguration?: MailManagerIngressPoint_NetworkConfiguration_PrivateNetworkConfiguration | Computed<MailManagerIngressPoint_NetworkConfiguration_PrivateNetworkConfiguration>;
  /** Configures the ingress point to be publicly reachable over the internet, accepting incoming mail without requiring a VPC connection. (AI-inferred) */
  publicNetworkConfiguration?: MailManagerIngressPoint_NetworkConfiguration_PublicNetworkConfiguration | Computed<MailManagerIngressPoint_NetworkConfiguration_PublicNetworkConfiguration>;
}

export interface MailManagerIngressPoint_Tags {
  /** The key of a metadata tag that can be applied to this SES Mail Manager ingress point to help organize, identify, and manage the resource. (AI-inferred) */
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

const MailManagerIngressPoint_IngressPointConfiguration_TlsAuthConfiguration_TrustStoreFields: FieldMap = {
  cacontent: "cacontent",
  crlContent: "crl_content",
  kmsKeyArn: "kms_key_arn",
};

const MailManagerIngressPoint_IngressPointConfiguration_TlsAuthConfigurationFields: FieldMap = {
  trustStore: {
    wireName: "trust_store",
    kind: "object",
    fields: MailManagerIngressPoint_IngressPointConfiguration_TlsAuthConfiguration_TrustStoreFields,
  },
};

const MailManagerIngressPoint_IngressPointConfigurationFields: FieldMap = {
  secretArn: "secret_arn",
  smtpPassword: "smtp_password",
  tlsAuthConfiguration: {
    wireName: "tls_auth_configuration",
    kind: "object",
    fields: MailManagerIngressPoint_IngressPointConfiguration_TlsAuthConfigurationFields,
  },
};

const MailManagerIngressPoint_NetworkConfiguration_PrivateNetworkConfigurationFields: FieldMap = {
  vpcEndpointId: "vpc_endpoint_id",
};

const MailManagerIngressPoint_NetworkConfiguration_PublicNetworkConfiguration_IpTypeFields: FieldMap = {
};

const MailManagerIngressPoint_NetworkConfiguration_PublicNetworkConfigurationFields: FieldMap = {
  ipType: {
    wireName: "ip_type",
    kind: "object",
    fields: MailManagerIngressPoint_NetworkConfiguration_PublicNetworkConfiguration_IpTypeFields,
  },
};

const MailManagerIngressPoint_NetworkConfigurationFields: FieldMap = {
  privateNetworkConfiguration: {
    wireName: "private_network_configuration",
    kind: "object",
    fields: MailManagerIngressPoint_NetworkConfiguration_PrivateNetworkConfigurationFields,
  },
  publicNetworkConfiguration: {
    wireName: "public_network_configuration",
    kind: "object",
    fields: MailManagerIngressPoint_NetworkConfiguration_PublicNetworkConfigurationFields,
  },
};

const MailManagerIngressPoint_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface MailManagerIngressPointConfig {
  /** Specifies the SMTP authentication configuration for the ingress point, containing the ARN of the AWS Secrets Manager secret that stores the credentials used for SMTP client authentication. (AI-inferred) */
  ingressPointConfiguration?: MailManagerIngressPoint_IngressPointConfiguration | Computed<MailManagerIngressPoint_IngressPointConfiguration>;
  /** Specifies a friendly name for the ingress point, which is the endpoint through which incoming email is received for the SES mail manager. (AI-inferred) */
  ingressPointName?: string | Computed<string>;
  /** Specifies the network configuration for the ingress point, determining whether the endpoint is public or private (VPC-based) and including the relevant VPC and subnet details. (AI-inferred) */
  networkConfiguration?: MailManagerIngressPoint_NetworkConfiguration | Computed<MailManagerIngressPoint_NetworkConfiguration>;
  /** The ID of the rule set that is associated with this ingress point and is applied to incoming email. (AI-inferred) */
  ruleSetId: string | Computed<string>;
  /** Defines the desired operational status (e.g., ACTIVE or INACTIVE) that the ingress point should be updated to, enabling you to start or stop inbound email processing through this endpoint. (AI-inferred) */
  statusToUpdate?: string | Computed<string>;
  tags?: MailManagerIngressPoint_Tags[] | Computed<MailManagerIngressPoint_Tags[]>;
  /** Specifies the TLS enforcement for connections to the ingress point, with allowed values 'REQUIRE' (forces TLS) or 'OPTIONAL' (allows both TLS and non-TLS). (AI-inferred) */
  tlsPolicy?: string | Computed<string>;
  /** The identifier of the traffic policy attached to this ingress point, which defines the rules for processing and routing incoming email. (AI-inferred) */
  trafficPolicyId: string | Computed<string>;
  /** Determines the protocol for receiving incoming email at the ingress point, with valid values being SMTP (standard email over SMTP) or API (ingress via the SES API). (AI-inferred) */
  type: string | Computed<string>;
}

export interface MailManagerIngressPointAttrs {
  /** The A record (DNS A record) automatically provisioned for the SES Mail Manager ingress point, used as the endpoint for inbound email routing. (AI-inferred) */
  arecord: string;
  /** The Amazon Resource Name (ARN) that uniquely identifies this SES Mail Manager ingress point. (AI-inferred) */
  ingressPointArn: string;
  /** Specifies the SMTP authentication configuration for the ingress point, containing the ARN of the AWS Secrets Manager secret that stores the credentials used for SMTP client authentication. (AI-inferred) */
  ingressPointConfiguration: MailManagerIngressPoint_IngressPointConfiguration;
  /** The unique identifier assigned by AWS to this ingress point. (AI-inferred) */
  ingressPointId: string;
  /** Specifies a friendly name for the ingress point, which is the endpoint through which incoming email is received for the SES mail manager. (AI-inferred) */
  ingressPointName: string;
  /** Specifies the network configuration for the ingress point, determining whether the endpoint is public or private (VPC-based) and including the relevant VPC and subnet details. (AI-inferred) */
  networkConfiguration: MailManagerIngressPoint_NetworkConfiguration;
  /** The ID of the rule set that is associated with this ingress point and is applied to incoming email. (AI-inferred) */
  ruleSetId: string;
  /** The current provisioning status of the ingress point, such as ACTIVE, PROVISIONING, or DELETING. (AI-inferred) */
  status: string;
  /** Defines the desired operational status (e.g., ACTIVE or INACTIVE) that the ingress point should be updated to, enabling you to start or stop inbound email processing through this endpoint. (AI-inferred) */
  statusToUpdate: string;
  tags: MailManagerIngressPoint_Tags[];
  /** Specifies the TLS enforcement for connections to the ingress point, with allowed values 'REQUIRE' (forces TLS) or 'OPTIONAL' (allows both TLS and non-TLS). (AI-inferred) */
  tlsPolicy: string;
  /** The identifier of the traffic policy attached to this ingress point, which defines the rules for processing and routing incoming email. (AI-inferred) */
  trafficPolicyId: string;
  /** Determines the protocol for receiving incoming email at the ingress point, with valid values being SMTP (standard email over SMTP) or API (ingress via the SES API). (AI-inferred) */
  type: string;
}

export const MailManagerIngressPoint: ResourceBinding<MailManagerIngressPointConfig, MailManagerIngressPointAttrs> = {
  wireType: "aws_ses_mail_manager_ingress_point",
  fields: {
    ingressPointConfiguration: {
      wireName: "ingress_point_configuration",
      kind: "object",
      fields: MailManagerIngressPoint_IngressPointConfigurationFields,
    },
    ingressPointName: "ingress_point_name",
    networkConfiguration: {
      wireName: "network_configuration",
      kind: "object",
      fields: MailManagerIngressPoint_NetworkConfigurationFields,
    },
    ruleSetId: "rule_set_id",
    statusToUpdate: "status_to_update",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: MailManagerIngressPoint_TagsFields,
    },
    tlsPolicy: "tls_policy",
    trafficPolicyId: "traffic_policy_id",
    type: "type",
  },
};
