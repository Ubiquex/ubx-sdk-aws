// Code generated by `ubx sdk gen` from aws_ses_email_identity. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface EmailIdentity_ConfigurationSetAttributes {
  /** The configuration set to use by default when sending from this identity. Note that any configuration set defined in the email sending request takes precedence. */
  configurationSetName?: string | Computed<string>;
}

export interface EmailIdentity_DkimAttributes {
  /** Sets the DKIM signing configuration for the identity. When you set this value true, then the messages that are sent from the identity are signed using DKIM. If you set this value to false, your messages are sent without DKIM signing. */
  signingEnabled?: boolean | Computed<boolean>;
}

export interface EmailIdentity_DkimSigningAttributes {
  /** [Bring Your Own DKIM] A private key that's used to generate a DKIM signature. The private key must use 1024 or 2048-bit RSA encryption, and must be encoded using base64 encoding. */
  domainSigningPrivateKey?: string | Computed<string>;
  /** [Bring Your Own DKIM] A string that's used to identify a public key in the DNS configuration for a domain. */
  domainSigningSelector?: string | Computed<string>;
  /** [Easy DKIM] The key length of the future DKIM key pair to be generated. This can be changed at most once per day. */
  nextSigningKeyLength?: string | Computed<string>;
}

export interface EmailIdentity_FeedbackAttributes {
  /** If the value is true, you receive email notifications when bounce or complaint events occur */
  emailForwardingEnabled?: boolean | Computed<boolean>;
}

export interface EmailIdentity_MailFromAttributes {
  /** The action to take if the required MX record isn't found when you send an email. When you set this value to UseDefaultValue , the mail is sent using amazonses.com as the MAIL FROM domain. When you set this value to RejectMessage , the Amazon SES API v2 returns a MailFromDomainNotVerified error, and doesn't attempt to deliver the email. */
  behaviorOnMxFailure?: string | Computed<string>;
  /** The custom MAIL FROM domain that you want the verified identity to use */
  mailFromDomain?: string | Computed<string>;
}

export interface EmailIdentity_Tags {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

const EmailIdentity_ConfigurationSetAttributesFields: FieldMap = {
  configurationSetName: "configuration_set_name",
};

const EmailIdentity_DkimAttributesFields: FieldMap = {
  signingEnabled: "signing_enabled",
};

const EmailIdentity_DkimSigningAttributesFields: FieldMap = {
  domainSigningPrivateKey: "domain_signing_private_key",
  domainSigningSelector: "domain_signing_selector",
  nextSigningKeyLength: "next_signing_key_length",
};

const EmailIdentity_FeedbackAttributesFields: FieldMap = {
  emailForwardingEnabled: "email_forwarding_enabled",
};

const EmailIdentity_MailFromAttributesFields: FieldMap = {
  behaviorOnMxFailure: "behavior_on_mx_failure",
  mailFromDomain: "mail_from_domain",
};

const EmailIdentity_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface EmailIdentityConfig {
  /** Used to associate a configuration set with an email identity. */
  configurationSetAttributes?: EmailIdentity_ConfigurationSetAttributes | Computed<EmailIdentity_ConfigurationSetAttributes>;
  /** Used to enable or disable DKIM authentication for an email identity. */
  dkimAttributes?: EmailIdentity_DkimAttributes | Computed<EmailIdentity_DkimAttributes>;
  /** If your request includes this object, Amazon SES configures the identity to use Bring Your Own DKIM (BYODKIM) for DKIM authentication purposes, or, configures the key length to be used for Easy DKIM. */
  dkimSigningAttributes?: EmailIdentity_DkimSigningAttributes | Computed<EmailIdentity_DkimSigningAttributes>;
  /** The email address or domain to verify. */
  emailIdentity: string | Computed<string>;
  /** Used to enable or disable feedback forwarding for an identity. */
  feedbackAttributes?: EmailIdentity_FeedbackAttributes | Computed<EmailIdentity_FeedbackAttributes>;
  /** Used to enable or disable the custom Mail-From domain configuration for an email identity. */
  mailFromAttributes?: EmailIdentity_MailFromAttributes | Computed<EmailIdentity_MailFromAttributes>;
  /** The tags (keys and values) associated with the email identity. */
  tags?: EmailIdentity_Tags[] | Computed<EmailIdentity_Tags[]>;
}

export interface EmailIdentityAttrs {
  /** Used to associate a configuration set with an email identity. */
  configurationSetAttributes: EmailIdentity_ConfigurationSetAttributes;
  /** Used to enable or disable DKIM authentication for an email identity. */
  dkimAttributes: EmailIdentity_DkimAttributes;
  dkimDnstokenName1: string;
  dkimDnstokenName2: string;
  dkimDnstokenName3: string;
  dkimDnstokenValue1: string;
  dkimDnstokenValue2: string;
  dkimDnstokenValue3: string;
  /** If your request includes this object, Amazon SES configures the identity to use Bring Your Own DKIM (BYODKIM) for DKIM authentication purposes, or, configures the key length to be used for Easy DKIM. */
  dkimSigningAttributes: EmailIdentity_DkimSigningAttributes;
  /** The email address or domain to verify. */
  emailIdentity: string;
  /** Used to enable or disable feedback forwarding for an identity. */
  feedbackAttributes: EmailIdentity_FeedbackAttributes;
  /** Used to enable or disable the custom Mail-From domain configuration for an email identity. */
  mailFromAttributes: EmailIdentity_MailFromAttributes;
  /** The tags (keys and values) associated with the email identity. */
  tags: EmailIdentity_Tags[];
}

export const EmailIdentity: ResourceBinding<EmailIdentityConfig, EmailIdentityAttrs> = {
  wireType: "aws_ses_email_identity",
  fields: {
    configurationSetAttributes: {
      wireName: "configuration_set_attributes",
      kind: "object",
      fields: EmailIdentity_ConfigurationSetAttributesFields,
    },
    dkimAttributes: {
      wireName: "dkim_attributes",
      kind: "object",
      fields: EmailIdentity_DkimAttributesFields,
    },
    dkimSigningAttributes: {
      wireName: "dkim_signing_attributes",
      kind: "object",
      fields: EmailIdentity_DkimSigningAttributesFields,
    },
    emailIdentity: "email_identity",
    feedbackAttributes: {
      wireName: "feedback_attributes",
      kind: "object",
      fields: EmailIdentity_FeedbackAttributesFields,
    },
    mailFromAttributes: {
      wireName: "mail_from_attributes",
      kind: "object",
      fields: EmailIdentity_MailFromAttributesFields,
    },
    tags: {
      wireName: "tags",
      kind: "list",
      fields: EmailIdentity_TagsFields,
    },
  },
};
