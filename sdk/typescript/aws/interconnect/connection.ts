// Code generated by `ubx sdk gen` from aws_interconnect_connection. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Connection_AttachPoint {
  /** The ARN of the resource to attach to. */
  arn?: string | Computed<string>;
  /** The ID of the Direct Connect Gateway to attach to. */
  directConnectGateway?: string | Computed<string>;
}

export interface Connection_Provider {
  /** The name of the cloud service provider. */
  cloudServiceProvider?: string | Computed<string>;
  /** The name of the last mile provider. */
  lastMileProvider?: string | Computed<string>;
}

export interface Connection_RemoteAccount {
  /** The identifier of the remote account. */
  identifier: string | Computed<string>;
}

export interface Connection_Tags {
  key?: string | Computed<string>;
  /** The value part of a key-value tag applied to the AWS Direct Connect connection (interconnect) resource, used to store arbitrary metadata for organizing and managing the connection. (AI-inferred) */
  value?: string | Computed<string>;
}

const Connection_AttachPointFields: FieldMap = {
  arn: "arn",
  directConnectGateway: "direct_connect_gateway",
};

const Connection_RemoteAccountFields: FieldMap = {
  identifier: "identifier",
};

const Connection_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface ConnectionConfig {
  /** The activation key for accepting a connection proposal from a partner CSP. Mutually exclusive with EnvironmentId. */
  activationKey?: string | Computed<string>;
  /** The logical attachment point in your AWS network where the managed connection will be connected. */
  attachPoint: Connection_AttachPoint | Computed<Connection_AttachPoint>;
  /** The bandwidth of the connection (e.g., 50Mbps, 1Gbps). Required when creating a connection through AWS. */
  bandwidth?: string | Computed<string>;
  /** A description of the connection. */
  description?: string | Computed<string>;
  /** The ID of the environment for the connection. Required when creating a connection through AWS. Mutually exclusive with ActivationKey. */
  environmentId?: string | Computed<string>;
  /** The remote account identifier for the connection. Required when creating a connection through AWS. Replaces RemoteOwnerAccount. */
  remoteAccount?: Connection_RemoteAccount | Computed<Connection_RemoteAccount>;
  /** Deprecated. Use RemoteAccount instead. The account ID of the remote owner. Required when creating a connection through AWS. */
  remoteOwnerAccount?: string | Computed<string>;
  /** An array of key-value pairs to apply to this resource. */
  tags?: Connection_Tags[] | Computed<Connection_Tags[]>;
}

export interface ConnectionAttrs {
  /** The activation key for accepting a connection proposal from a partner CSP. Mutually exclusive with EnvironmentId. */
  activationKey: string;
  /** The Amazon Resource Name (ARN) of the connection. */
  arn: string;
  /** The logical attachment point in your AWS network where the managed connection will be connected. */
  attachPoint: Connection_AttachPoint;
  /** The bandwidth of the connection (e.g., 50Mbps, 1Gbps). Required when creating a connection through AWS. */
  bandwidth: string;
  /** The billing tier for the connection. */
  billingTier: number;
  /** The unique identifier for the connection. */
  connectionId: string;
  /** A description of the connection. */
  description: string;
  /** The ID of the environment for the connection. Required when creating a connection through AWS. Mutually exclusive with ActivationKey. */
  environmentId: string;
  /** The AWS account ID of the connection owner. */
  ownerAccount: string;
  /** The partner cloud service provider. */
  provider: Connection_Provider;
  /** The remote account identifier for the connection. Required when creating a connection through AWS. Replaces RemoteOwnerAccount. */
  remoteAccount: Connection_RemoteAccount;
  /** Deprecated. Use RemoteAccount instead. The account ID of the remote owner. Required when creating a connection through AWS. */
  remoteOwnerAccount: string;
  /** The shared identifier for the connection pairing. */
  sharedId: string;
  /** The current state of the connection. */
  state: string;
  /** An array of key-value pairs to apply to this resource. */
  tags: Connection_Tags[];
  /** The type of managed connection. */
  type: string;
}

export const Connection: ResourceBinding<ConnectionConfig, ConnectionAttrs> = {
  wireType: "aws_interconnect_connection",
  fields: {
    activationKey: "activation_key",
    attachPoint: {
      wireName: "attach_point",
      kind: "object",
      fields: Connection_AttachPointFields,
    },
    bandwidth: "bandwidth",
    description: "description",
    environmentId: "environment_id",
    remoteAccount: {
      wireName: "remote_account",
      kind: "object",
      fields: Connection_RemoteAccountFields,
    },
    remoteOwnerAccount: "remote_owner_account",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: Connection_TagsFields,
    },
  },
};
