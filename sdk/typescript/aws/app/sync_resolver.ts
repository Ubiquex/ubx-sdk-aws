// Code generated by `ubx sdk gen` from aws_app_sync_resolver. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface SyncResolver_CachingConfig {
  /** The caching keys for a resolver that has caching activated. Valid values are entries from the ``$context.arguments``, ``$context.source``, and ``$context.identity`` maps. */
  cachingKeys?: string[] | Computed<string[]>;
  /** The TTL in seconds for a resolver that has caching activated. Valid values are 1–3,600 seconds. */
  ttl: number | Computed<number>;
}

export interface SyncResolver_PipelineConfig {
  /** A list of ``Function`` objects. */
  functions?: string[] | Computed<string[]>;
}

export interface SyncResolver_Runtime {
  /** The ``name`` of the runtime to use. Currently, the only allowed value is ``APPSYNC_JS``. */
  name: string | Computed<string>;
  /** The ``version`` of the runtime to use. Currently, the only allowed version is ``1.0.0``. */
  runtimeVersion: string | Computed<string>;
}

export interface SyncResolver_SyncConfig_LambdaConflictHandlerConfig {
  /** The Amazon Resource Name (ARN) for the Lambda function to use as the Conflict Handler. */
  lambdaConflictHandlerArn?: string | Computed<string>;
}

export interface SyncResolver_SyncConfig {
  /** The Conflict Detection strategy to use. + *VERSION*: Detect conflicts based on object versions for this resolver. + *NONE*: Do not detect conflicts when invoking this resolver. */
  conflictDetection: string | Computed<string>;
  /** The Conflict Resolution strategy to perform in the event of a conflict. + *OPTIMISTIC_CONCURRENCY*: Resolve conflicts by rejecting mutations when versions don't match the latest version at the server. + *AUTOMERGE*: Resolve conflicts with the Automerge conflict resolution strategy. + *LAMBDA*: Resolve conflicts with an LAMlong function supplied in the ``LambdaConflictHandlerConfig``. */
  conflictHandler?: string | Computed<string>;
  /** The ``LambdaConflictHandlerConfig`` when configuring LAMBDA as the Conflict Handler. */
  lambdaConflictHandlerConfig?: SyncResolver_SyncConfig_LambdaConflictHandlerConfig | Computed<SyncResolver_SyncConfig_LambdaConflictHandlerConfig>;
}

const SyncResolver_CachingConfigFields: FieldMap = {
  cachingKeys: "caching_keys",
  ttl: "ttl",
};

const SyncResolver_PipelineConfigFields: FieldMap = {
  functions: "functions",
};

const SyncResolver_RuntimeFields: FieldMap = {
  name: "name",
  runtimeVersion: "runtime_version",
};

const SyncResolver_SyncConfig_LambdaConflictHandlerConfigFields: FieldMap = {
  lambdaConflictHandlerArn: "lambda_conflict_handler_arn",
};

const SyncResolver_SyncConfigFields: FieldMap = {
  conflictDetection: "conflict_detection",
  conflictHandler: "conflict_handler",
  lambdaConflictHandlerConfig: {
    wireName: "lambda_conflict_handler_config",
    kind: "object",
    fields: SyncResolver_SyncConfig_LambdaConflictHandlerConfigFields,
  },
};

export interface SyncResolverConfig {
  /** The APSYlong GraphQL API to which you want to attach this resolver. */
  apiId: string | Computed<string>;
  /** The caching configuration for a resolver that has caching activated. */
  cachingConfig?: SyncResolver_CachingConfig | Computed<SyncResolver_CachingConfig>;
  /** The ``resolver`` code that contains the request and response functions. When code is used, the ``runtime`` is required. The runtime value must be ``APPSYNC_JS``. */
  code?: string | Computed<string>;
  /** The Amazon S3 endpoint. */
  codeS3Location?: string | Computed<string>;
  /** The resolver data source name. */
  dataSourceName?: string | Computed<string>;
  /** The GraphQL field on a type that invokes the resolver. */
  fieldName: string | Computed<string>;
  /** The resolver type. + *UNIT*: A UNIT resolver type. A UNIT resolver is the default resolver type. You can use a UNIT resolver to run a GraphQL query against a single data source. + *PIPELINE*: A PIPELINE resolver type. You can use a PIPELINE resolver to invoke a series of ``Function`` objects in a serial manner. You can use a pipeline resolver to run a GraphQL query against multiple data sources. */
  kind?: string | Computed<string>;
  /** The maximum number of resolver request inputs that will be sent to a single LAMlong function in a ``BatchInvoke`` operation. */
  maxBatchSize?: number | Computed<number>;
  /** Enables or disables enhanced resolver metrics for specified resolvers. Note that ``MetricsConfig`` won't be used unless the ``resolverLevelMetricsBehavior`` value is set to ``PER_RESOLVER_METRICS``. If the ``resolverLevelMetricsBehavior`` is set to ``FULL_REQUEST_RESOLVER_METRICS`` instead, ``MetricsConfig`` will be ignored. However, you can still set its value. */
  metricsConfig?: string | Computed<string>;
  /** Use the ``PipelineConfig`` property type to specify ``PipelineConfig`` for an APSYlong resolver. ``PipelineConfig`` is a property of the [AWS::AppSync::Resolver](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-appsync-resolver.html) resource. */
  pipelineConfig?: SyncResolver_PipelineConfig | Computed<SyncResolver_PipelineConfig>;
  /** The request mapping template. Request mapping templates are optional when using a Lambda data source. For all other data sources, a request mapping template is required. */
  requestMappingTemplate?: string | Computed<string>;
  /** The location of a request mapping template in an S3 bucket. Use this if you want to provision with a template file in S3 rather than embedding it in your CFNshort template. */
  requestMappingTemplateS3Location?: string | Computed<string>;
  /** The response mapping template. */
  responseMappingTemplate?: string | Computed<string>;
  /** The location of a response mapping template in an S3 bucket. Use this if you want to provision with a template file in S3 rather than embedding it in your CFNshort template. */
  responseMappingTemplateS3Location?: string | Computed<string>;
  /** Describes a runtime used by an APSYlong resolver or APSYlong function. Specifies the name and version of the runtime to use. Note that if a runtime is specified, code must also be specified. */
  runtime?: SyncResolver_Runtime | Computed<SyncResolver_Runtime>;
  /** Describes a Sync configuration for a resolver. Specifies which Conflict Detection strategy and Resolution strategy to use when the resolver is invoked. */
  syncConfig?: SyncResolver_SyncConfig | Computed<SyncResolver_SyncConfig>;
  /** The GraphQL type that invokes this resolver. */
  typeName: string | Computed<string>;
}

export interface SyncResolverAttrs {
  /** The APSYlong GraphQL API to which you want to attach this resolver. */
  apiId: string;
  /** The caching configuration for a resolver that has caching activated. */
  cachingConfig: SyncResolver_CachingConfig;
  /** The ``resolver`` code that contains the request and response functions. When code is used, the ``runtime`` is required. The runtime value must be ``APPSYNC_JS``. */
  code: string;
  /** The Amazon S3 endpoint. */
  codeS3Location: string;
  /** The resolver data source name. */
  dataSourceName: string;
  /** The GraphQL field on a type that invokes the resolver. */
  fieldName: string;
  /** The resolver type. + *UNIT*: A UNIT resolver type. A UNIT resolver is the default resolver type. You can use a UNIT resolver to run a GraphQL query against a single data source. + *PIPELINE*: A PIPELINE resolver type. You can use a PIPELINE resolver to invoke a series of ``Function`` objects in a serial manner. You can use a pipeline resolver to run a GraphQL query against multiple data sources. */
  kind: string;
  /** The maximum number of resolver request inputs that will be sent to a single LAMlong function in a ``BatchInvoke`` operation. */
  maxBatchSize: number;
  /** Enables or disables enhanced resolver metrics for specified resolvers. Note that ``MetricsConfig`` won't be used unless the ``resolverLevelMetricsBehavior`` value is set to ``PER_RESOLVER_METRICS``. If the ``resolverLevelMetricsBehavior`` is set to ``FULL_REQUEST_RESOLVER_METRICS`` instead, ``MetricsConfig`` will be ignored. However, you can still set its value. */
  metricsConfig: string;
  /** Use the ``PipelineConfig`` property type to specify ``PipelineConfig`` for an APSYlong resolver. ``PipelineConfig`` is a property of the [AWS::AppSync::Resolver](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-appsync-resolver.html) resource. */
  pipelineConfig: SyncResolver_PipelineConfig;
  /** The request mapping template. Request mapping templates are optional when using a Lambda data source. For all other data sources, a request mapping template is required. */
  requestMappingTemplate: string;
  /** The location of a request mapping template in an S3 bucket. Use this if you want to provision with a template file in S3 rather than embedding it in your CFNshort template. */
  requestMappingTemplateS3Location: string;
  resolverArn: string;
  /** The response mapping template. */
  responseMappingTemplate: string;
  /** The location of a response mapping template in an S3 bucket. Use this if you want to provision with a template file in S3 rather than embedding it in your CFNshort template. */
  responseMappingTemplateS3Location: string;
  /** Describes a runtime used by an APSYlong resolver or APSYlong function. Specifies the name and version of the runtime to use. Note that if a runtime is specified, code must also be specified. */
  runtime: SyncResolver_Runtime;
  /** Describes a Sync configuration for a resolver. Specifies which Conflict Detection strategy and Resolution strategy to use when the resolver is invoked. */
  syncConfig: SyncResolver_SyncConfig;
  /** The GraphQL type that invokes this resolver. */
  typeName: string;
}

export const SyncResolver: ResourceBinding<SyncResolverConfig, SyncResolverAttrs> = {
  wireType: "aws_app_sync_resolver",
  fields: {
    apiId: "api_id",
    cachingConfig: {
      wireName: "caching_config",
      kind: "object",
      fields: SyncResolver_CachingConfigFields,
    },
    code: "code",
    codeS3Location: "code_s3_location",
    dataSourceName: "data_source_name",
    fieldName: "field_name",
    kind: "kind",
    maxBatchSize: "max_batch_size",
    metricsConfig: "metrics_config",
    pipelineConfig: {
      wireName: "pipeline_config",
      kind: "object",
      fields: SyncResolver_PipelineConfigFields,
    },
    requestMappingTemplate: "request_mapping_template",
    requestMappingTemplateS3Location: "request_mapping_template_s3_location",
    responseMappingTemplate: "response_mapping_template",
    responseMappingTemplateS3Location: "response_mapping_template_s3_location",
    runtime: {
      wireName: "runtime",
      kind: "object",
      fields: SyncResolver_RuntimeFields,
    },
    syncConfig: {
      wireName: "sync_config",
      kind: "object",
      fields: SyncResolver_SyncConfigFields,
    },
    typeName: "type_name",
  },
};
