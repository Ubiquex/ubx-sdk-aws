// Code generated by `ubx sdk gen` from aws_kinesis_stream. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Stream_StreamEncryption {
  /** The encryption type to use. The only valid value is KMS. */
  encryptionType: string | Computed<string>;
  /** The GUID for the customer-managed AWS KMS key to use for encryption. This value can be a globally unique identifier, a fully specified Amazon Resource Name (ARN) to either an alias or a key, or an alias name prefixed by "alias/".You can also use a master key owned by Kinesis Data Streams by specifying the alias aws/kinesis. */
  keyId: unknown | Computed<unknown>;
}

export interface Stream_StreamModeDetails {
  /** The mode of the stream */
  streamMode: string | Computed<string>;
}

export interface Stream_Tags {
  /** The key of a tag attached to the Kinesis stream, used to organize, identify, and manage access to the stream. (AI-inferred) */
  key?: string | Computed<string>;
  /** Specifies the value of a user-defined tag applied to the Kinesis data stream, allowing you to associate arbitrary metadata with the resource. (AI-inferred) */
  value?: string | Computed<string>;
}

export interface Stream_WarmThroughputObject {
  /** Current warm throughput in MiB/s */
  currentMiBps?: number | Computed<number>;
  /** Target warm throughput in MiB/s that a customer can write to a stream at any given time */
  targetMiBps?: number | Computed<number>;
}

const Stream_StreamEncryptionFields: FieldMap = {
  encryptionType: "encryption_type",
  keyId: "key_id",
};

const Stream_StreamModeDetailsFields: FieldMap = {
  streamMode: "stream_mode",
};

const Stream_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface StreamConfig {
  /** The final list of shard-level metrics */
  desiredShardLevelMetrics?: string[] | Computed<string[]>;
  /** Maximum size of a data record in KiB allowed to be put into Kinesis stream. */
  maxRecordSizeInKiB?: number | Computed<number>;
  /** The name of the Kinesis stream. */
  name?: string | Computed<string>;
  /** The number of hours for the data records that are stored in shards to remain accessible. */
  retentionPeriodHours?: number | Computed<number>;
  /** The number of shards that the stream uses. Required when StreamMode = PROVISIONED is passed. */
  shardCount?: number | Computed<number>;
  /** When specified, enables or updates server-side encryption using an AWS KMS key for a specified stream. Removing this property from your stack template and updating your stack disables encryption. */
  streamEncryption?: Stream_StreamEncryption | Computed<Stream_StreamEncryption>;
  /** When specified, enables or updates the mode of stream. Default is PROVISIONED. */
  streamModeDetails?: Stream_StreamModeDetails | Computed<Stream_StreamModeDetails>;
  /** An arbitrary set of tags (key-value pairs) to associate with the Kinesis stream. */
  tags?: Stream_Tags[] | Computed<Stream_Tags[]>;
  /** Target warm throughput in MiB/s for the stream. This property can ONLY be set when StreamMode is ON_DEMAND. */
  warmThroughputMiBps?: number | Computed<number>;
}

export interface StreamAttrs {
  /** The Amazon resource name (ARN) of the Kinesis stream */
  arn: string;
  /** The final list of shard-level metrics */
  desiredShardLevelMetrics: string[];
  /** Maximum size of a data record in KiB allowed to be put into Kinesis stream. */
  maxRecordSizeInKiB: number;
  /** The name of the Kinesis stream. */
  name: string;
  /** The number of hours for the data records that are stored in shards to remain accessible. */
  retentionPeriodHours: number;
  /** The number of shards that the stream uses. Required when StreamMode = PROVISIONED is passed. */
  shardCount: number;
  /** When specified, enables or updates server-side encryption using an AWS KMS key for a specified stream. Removing this property from your stack template and updating your stack disables encryption. */
  streamEncryption: Stream_StreamEncryption;
  /** When specified, enables or updates the mode of stream. Default is PROVISIONED. */
  streamModeDetails: Stream_StreamModeDetails;
  /** An arbitrary set of tags (key-value pairs) to associate with the Kinesis stream. */
  tags: Stream_Tags[];
  /** Target warm throughput in MiB/s for the stream. This property can ONLY be set when StreamMode is ON_DEMAND. */
  warmThroughputMiBps: number;
  /** Warm throughput configuration details for the stream. Only present for ON_DEMAND streams. */
  warmThroughputObject: Stream_WarmThroughputObject;
}

export const Stream: ResourceBinding<StreamConfig, StreamAttrs> = {
  wireType: "aws_kinesis_stream",
  fields: {
    desiredShardLevelMetrics: "desired_shard_level_metrics",
    maxRecordSizeInKiB: "max_record_size_in_ki_b",
    name: "name",
    retentionPeriodHours: "retention_period_hours",
    shardCount: "shard_count",
    streamEncryption: {
      wireName: "stream_encryption",
      kind: "object",
      fields: Stream_StreamEncryptionFields,
    },
    streamModeDetails: {
      wireName: "stream_mode_details",
      kind: "object",
      fields: Stream_StreamModeDetailsFields,
    },
    tags: {
      wireName: "tags",
      kind: "list",
      fields: Stream_TagsFields,
    },
    warmThroughputMiBps: "warm_throughput_mi_bps",
  },
};
