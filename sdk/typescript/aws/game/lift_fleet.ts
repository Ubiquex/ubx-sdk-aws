// Code generated by `ubx sdk gen` from aws_game_lift_fleet. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface LiftFleet_CertificateConfiguration {
  certificateType: string | Computed<string>;
}

export interface LiftFleet_Ec2InboundPermissions {
  fromPort?: number | Computed<number>;
  ipRange?: string | Computed<string>;
  protocol?: string | Computed<string>;
  toPort?: number | Computed<number>;
}

export interface LiftFleet_Locations_LocationCapacity_ManagedCapacityConfiguration {
  scaleInAfterInactivityMinutes?: number | Computed<number>;
  zeroCapacityStrategy?: string | Computed<string>;
}

export interface LiftFleet_Locations_LocationCapacity {
  desiredEc2Instances?: number | Computed<number>;
  managedCapacityConfiguration?: LiftFleet_Locations_LocationCapacity_ManagedCapacityConfiguration | Computed<LiftFleet_Locations_LocationCapacity_ManagedCapacityConfiguration>;
  maxSize?: number | Computed<number>;
  minSize?: number | Computed<number>;
}

export interface LiftFleet_Locations {
  location?: string | Computed<string>;
  locationCapacity?: LiftFleet_Locations_LocationCapacity | Computed<LiftFleet_Locations_LocationCapacity>;
  playerGatewayStatus?: string | Computed<string>;
}

export interface LiftFleet_PlayerGatewayConfiguration {
  /** The IP protocol supported by the game server. */
  gameServerIpProtocolSupported?: string | Computed<string>;
}

export interface LiftFleet_ResourceCreationLimitPolicy {
  /** The maximum number of game sessions that an individual can create during the policy period. */
  newGameSessionsPerCreator?: number | Computed<number>;
  /** The time span used in evaluating the resource creation limit policy. */
  policyPeriodInMinutes?: number | Computed<number>;
}

export interface LiftFleet_RuntimeConfiguration_ServerProcesses {
  concurrentExecutions?: number | Computed<number>;
  launchPath?: string | Computed<string>;
  parameters?: string | Computed<string>;
}

export interface LiftFleet_RuntimeConfiguration {
  /** The maximum amount of time (in seconds) that a game session can remain in status ACTIVATING. If the game session is not active before the timeout, activation is terminated and the game session status is changed to TERMINATED. */
  gameSessionActivationTimeoutSeconds?: number | Computed<number>;
  /** The maximum number of game sessions with status ACTIVATING to allow on an instance simultaneously. This setting limits the amount of instance resources that can be used for new game activations at any one time. */
  maxConcurrentGameSessionActivations?: number | Computed<number>;
  /** A collection of server process configurations that describe which server processes to run on each instance in a fleet. */
  serverProcesses?: LiftFleet_RuntimeConfiguration_ServerProcesses[] | Computed<LiftFleet_RuntimeConfiguration_ServerProcesses[]>;
}

export interface LiftFleet_ScalingPolicies_TargetConfiguration {
  targetValue?: number | Computed<number>;
}

export interface LiftFleet_ScalingPolicies {
  comparisonOperator?: string | Computed<string>;
  evaluationPeriods?: number | Computed<number>;
  location?: string | Computed<string>;
  metricName?: string | Computed<string>;
  name?: string | Computed<string>;
  policyType?: string | Computed<string>;
  scalingAdjustment?: number | Computed<number>;
  scalingAdjustmentType?: string | Computed<string>;
  status?: string | Computed<string>;
  targetConfiguration?: LiftFleet_ScalingPolicies_TargetConfiguration | Computed<LiftFleet_ScalingPolicies_TargetConfiguration>;
  threshold?: number | Computed<number>;
  updateStatus?: string | Computed<string>;
}

export interface LiftFleet_Tags {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

const LiftFleet_CertificateConfigurationFields: FieldMap = {
  certificateType: "certificate_type",
};

const LiftFleet_Ec2InboundPermissionsFields: FieldMap = {
  fromPort: "from_port",
  ipRange: "ip_range",
  protocol: "protocol",
  toPort: "to_port",
};

const LiftFleet_PlayerGatewayConfigurationFields: FieldMap = {
  gameServerIpProtocolSupported: "game_server_ip_protocol_supported",
};

const LiftFleet_ResourceCreationLimitPolicyFields: FieldMap = {
  newGameSessionsPerCreator: "new_game_sessions_per_creator",
  policyPeriodInMinutes: "policy_period_in_minutes",
};

const LiftFleet_RuntimeConfiguration_ServerProcessesFields: FieldMap = {
  concurrentExecutions: "concurrent_executions",
  launchPath: "launch_path",
  parameters: "parameters",
};

const LiftFleet_RuntimeConfigurationFields: FieldMap = {
  gameSessionActivationTimeoutSeconds: "game_session_activation_timeout_seconds",
  maxConcurrentGameSessionActivations: "max_concurrent_game_session_activations",
  serverProcesses: {
    wireName: "server_processes",
    kind: "list",
    fields: LiftFleet_RuntimeConfiguration_ServerProcessesFields,
  },
};

const LiftFleet_ScalingPolicies_TargetConfigurationFields: FieldMap = {
  targetValue: "target_value",
};

const LiftFleet_ScalingPoliciesFields: FieldMap = {
  comparisonOperator: "comparison_operator",
  evaluationPeriods: "evaluation_periods",
  location: "location",
  metricName: "metric_name",
  name: "name",
  policyType: "policy_type",
  scalingAdjustment: "scaling_adjustment",
  scalingAdjustmentType: "scaling_adjustment_type",
  status: "status",
  targetConfiguration: {
    wireName: "target_configuration",
    kind: "object",
    fields: LiftFleet_ScalingPolicies_TargetConfigurationFields,
  },
  threshold: "threshold",
  updateStatus: "update_status",
};

const LiftFleet_TagsFields: FieldMap = {
  key: "key",
  value: "value",
};

export interface LiftFleetConfig {
  /** Configuration for Anywhere fleet. */
  anywhereConfiguration?: unknown | Computed<unknown>;
  /** Determines when and how to apply fleet or location capacities. Allowed options are ON_UPDATE (default), ON_CREATE_AND_UPDATE and ON_CREATE_AND_UPDATE_WITH_AUTOSCALING. If you choose ON_CREATE_AND_UPDATE_WITH_AUTOSCALING, MinSize and MaxSize will still be applied on creation and on updates, but DesiredEC2Instances will only be applied once on fleet creation and will be ignored during updates to prevent conflicts with auto-scaling. During updates with ON_CREATE_AND_UPDATE_WITH_AUTOSCALING chosen, if current desired instance is lower than the new MinSize, it will be increased to the new MinSize; if current desired instance is larger than the new MaxSize, it will be decreased to the new MaxSize. */
  applyCapacity?: string | Computed<string>;
  /** A unique identifier for a build to be deployed on the new fleet. If you are deploying the fleet with a custom game build, you must specify this property. The build must have been successfully uploaded to Amazon GameLift and be in a READY status. This fleet setting cannot be changed once the fleet is created. */
  buildId?: string | Computed<string>;
  /** Information about the use of a TLS/SSL certificate for a fleet. TLS certificate generation is enabled at the fleet level, with one certificate generated for the fleet. When this feature is enabled, the certificate can be retrieved using the GameLift Server SDK call GetInstanceCertificate. All instances in a fleet share the same certificate. */
  certificateConfiguration?: LiftFleet_CertificateConfiguration | Computed<LiftFleet_CertificateConfiguration>;
  /** ComputeType to differentiate EC2 hardware managed by GameLift and Anywhere hardware managed by the customer. */
  computeType?: string | Computed<string>;
  /** A human-readable description of a fleet. */
  description?: string | Computed<string>;
  /** [DEPRECATED] The number of EC2 instances that you want this fleet to host. When creating a new fleet, GameLift automatically sets this value to "1" and initiates a single instance. Once the fleet is active, update this value to trigger GameLift to add or remove instances from the fleet. */
  desiredEc2Instances?: number | Computed<number>;
  /** A range of IP addresses and port settings that allow inbound traffic to connect to server processes on an Amazon GameLift server. */
  ec2InboundPermissions?: LiftFleet_Ec2InboundPermissions[] | Computed<LiftFleet_Ec2InboundPermissions[]>;
  /** The name of an EC2 instance type that is supported in Amazon GameLift. A fleet instance type determines the computing resources of each instance in the fleet, including CPU, memory, storage, and networking capacity. Amazon GameLift supports the following EC2 instance types. See Amazon EC2 Instance Types for detailed descriptions. */
  ec2InstanceType?: string | Computed<string>;
  /** Indicates whether to use On-Demand instances or Spot instances for this fleet. If empty, the default is ON_DEMAND. Both categories of instances use identical hardware and configurations based on the instance type selected for this fleet. */
  fleetType?: string | Computed<string>;
  /** A unique identifier for an AWS IAM role that manages access to your AWS services. With an instance role ARN set, any application that runs on an instance in this fleet can assume the role, including install scripts, server processes, and daemons (background processes). Create a role or look up a role's ARN from the IAM dashboard in the AWS Management Console. */
  instanceRoleArn?: string | Computed<string>;
  /** Credentials provider implementation that loads credentials from the Amazon EC2 Instance Metadata Service. */
  instanceRoleCredentialsProvider?: string | Computed<string>;
  /** This parameter is no longer used. When hosting a custom game build, specify where Amazon GameLift should store log files using the Amazon GameLift server API call ProcessReady() */
  logPaths?: string[] | Computed<string[]>;
  /** [DEPRECATED] The maximum value that is allowed for the fleet's instance count. When creating a new fleet, GameLift automatically sets this value to "1". Once the fleet is active, you can change this value. */
  maxSize?: number | Computed<number>;
  /** The name of an Amazon CloudWatch metric group. A metric group aggregates the metrics for all fleets in the group. Specify a string containing the metric group name. You can use an existing name or use a new name to create a new metric group. Currently, this parameter can have only one string. */
  metricGroups?: string[] | Computed<string[]>;
  /** [DEPRECATED] The minimum value allowed for the fleet's instance count. When creating a new fleet, GameLift automatically sets this value to "0". After the fleet is active, you can change this value. */
  minSize?: number | Computed<number>;
  /** A descriptive label that is associated with a fleet. Fleet names do not need to be unique. */
  name: string | Computed<string>;
  /** A game session protection policy to apply to all game sessions hosted on instances in this fleet. When protected, active game sessions cannot be terminated during a scale-down event. If this parameter is not set, instances in this fleet default to no protection. You can change a fleet's protection policy to affect future game sessions on the fleet. You can also set protection for individual game sessions. */
  newGameSessionProtectionPolicy?: string | Computed<string>;
  /** A unique identifier for the AWS account with the VPC that you want to peer your Amazon GameLift fleet with. You can find your account ID in the AWS Management Console under account settings. */
  peerVpcAwsAccountId?: string | Computed<string>;
  /** A unique identifier for a VPC with resources to be accessed by your Amazon GameLift fleet. The VPC must be in the same Region as your fleet. To look up a VPC ID, use the VPC Dashboard in the AWS Management Console. */
  peerVpcId?: string | Computed<string>;
  /** Configuration for player gateway. */
  playerGatewayConfiguration?: LiftFleet_PlayerGatewayConfiguration | Computed<LiftFleet_PlayerGatewayConfiguration>;
  /** The player gateway mode for the fleet. */
  playerGatewayMode?: string | Computed<string>;
  /** A policy that limits the number of game sessions a player can create on the same fleet. This optional policy gives game owners control over how players can consume available game server resources. A resource creation policy makes the following statement: "An individual player can create a maximum number of new game sessions within a specified time period". The policy is evaluated when a player tries to create a new game session. For example, assume you have a policy of 10 new game sessions and a time period of 60 minutes. On receiving a CreateGameSession request, Amazon GameLift checks that the player (identified by CreatorId) has created fewer than 10 game sessions in the past 60 minutes. */
  resourceCreationLimitPolicy?: LiftFleet_ResourceCreationLimitPolicy | Computed<LiftFleet_ResourceCreationLimitPolicy>;
  /** A collection of server process configurations that describe the processes to run on each instance in a fleet. All fleets must have a runtime configuration. Each instance in the fleet maintains server processes as specified in the runtime configuration, launching new ones as existing processes end. Each instance regularly checks for an updated runtime configuration makes adjustments as called for. The runtime configuration enables the instances in a fleet to run multiple processes simultaneously. Potential scenarios are as follows: (1) Run multiple processes of a single game server executable to maximize usage of your hosting resources. (2) Run one or more processes of different executables, such as your game server and a metrics tracking program. (3) Run multiple processes of a single game server but with different launch parameters, for example to run one process on each instance in debug mode. An Amazon GameLift instance is limited to 50 processes running simultaneously. A runtime configuration must specify fewer than this limit. To calculate the total number of processes specified in a runtime configuration, add the values of the ConcurrentExecutions parameter for each ServerProcess object in the runtime configuration. */
  runtimeConfiguration?: LiftFleet_RuntimeConfiguration | Computed<LiftFleet_RuntimeConfiguration>;
  /** A list of rules that control how a fleet is scaled. */
  scalingPolicies?: LiftFleet_ScalingPolicies[] | Computed<LiftFleet_ScalingPolicies[]>;
  /** A unique identifier for a Realtime script to be deployed on a new Realtime Servers fleet. The script must have been successfully uploaded to Amazon GameLift. This fleet setting cannot be changed once the fleet is created. Note: It is not currently possible to use the !Ref command to reference a script created with a CloudFormation template for the fleet property ScriptId. Instead, use Fn::GetAtt Script.Arn or Fn::GetAtt Script.Id to retrieve either of these properties as input for ScriptId. Alternatively, enter a ScriptId string manually. */
  scriptId?: string | Computed<string>;
  /** This parameter is no longer used but is retained for backward compatibility. Instead, specify server launch parameters in the RuntimeConfiguration parameter. A request must specify either a runtime configuration or values for both ServerLaunchParameters and ServerLaunchPath. */
  serverLaunchParameters?: string | Computed<string>;
  /** This parameter is no longer used. Instead, specify a server launch path using the RuntimeConfiguration parameter. Requests that specify a server launch path and launch parameters instead of a runtime configuration will continue to work. */
  serverLaunchPath?: string | Computed<string>;
  /** An array of key-value pairs to apply to this resource. */
  tags?: LiftFleet_Tags[] | Computed<LiftFleet_Tags[]>;
}

export interface LiftFleetAttrs {
  /** Configuration for Anywhere fleet. */
  anywhereConfiguration: unknown;
  /** Determines when and how to apply fleet or location capacities. Allowed options are ON_UPDATE (default), ON_CREATE_AND_UPDATE and ON_CREATE_AND_UPDATE_WITH_AUTOSCALING. If you choose ON_CREATE_AND_UPDATE_WITH_AUTOSCALING, MinSize and MaxSize will still be applied on creation and on updates, but DesiredEC2Instances will only be applied once on fleet creation and will be ignored during updates to prevent conflicts with auto-scaling. During updates with ON_CREATE_AND_UPDATE_WITH_AUTOSCALING chosen, if current desired instance is lower than the new MinSize, it will be increased to the new MinSize; if current desired instance is larger than the new MaxSize, it will be decreased to the new MaxSize. */
  applyCapacity: string;
  /** A unique identifier for a build to be deployed on the new fleet. If you are deploying the fleet with a custom game build, you must specify this property. The build must have been successfully uploaded to Amazon GameLift and be in a READY status. This fleet setting cannot be changed once the fleet is created. */
  buildId: string;
  /** Information about the use of a TLS/SSL certificate for a fleet. TLS certificate generation is enabled at the fleet level, with one certificate generated for the fleet. When this feature is enabled, the certificate can be retrieved using the GameLift Server SDK call GetInstanceCertificate. All instances in a fleet share the same certificate. */
  certificateConfiguration: LiftFleet_CertificateConfiguration;
  /** ComputeType to differentiate EC2 hardware managed by GameLift and Anywhere hardware managed by the customer. */
  computeType: string;
  /** A human-readable description of a fleet. */
  description: string;
  /** [DEPRECATED] The number of EC2 instances that you want this fleet to host. When creating a new fleet, GameLift automatically sets this value to "1" and initiates a single instance. Once the fleet is active, update this value to trigger GameLift to add or remove instances from the fleet. */
  desiredEc2Instances: number;
  /** A range of IP addresses and port settings that allow inbound traffic to connect to server processes on an Amazon GameLift server. */
  ec2InboundPermissions: LiftFleet_Ec2InboundPermissions[];
  /** The name of an EC2 instance type that is supported in Amazon GameLift. A fleet instance type determines the computing resources of each instance in the fleet, including CPU, memory, storage, and networking capacity. Amazon GameLift supports the following EC2 instance types. See Amazon EC2 Instance Types for detailed descriptions. */
  ec2InstanceType: string;
  /** The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers Fleet resource and uniquely identifies it. ARNs are unique across all Regions. In a GameLift Fleet ARN, the resource ID matches the FleetId value. */
  fleetArn: string;
  /** Unique fleet ID */
  fleetId: string;
  /** Indicates whether to use On-Demand instances or Spot instances for this fleet. If empty, the default is ON_DEMAND. Both categories of instances use identical hardware and configurations based on the instance type selected for this fleet. */
  fleetType: string;
  /** A unique identifier for an AWS IAM role that manages access to your AWS services. With an instance role ARN set, any application that runs on an instance in this fleet can assume the role, including install scripts, server processes, and daemons (background processes). Create a role or look up a role's ARN from the IAM dashboard in the AWS Management Console. */
  instanceRoleArn: string;
  /** Credentials provider implementation that loads credentials from the Amazon EC2 Instance Metadata Service. */
  instanceRoleCredentialsProvider: string;
  locations: LiftFleet_Locations[];
  /** This parameter is no longer used. When hosting a custom game build, specify where Amazon GameLift should store log files using the Amazon GameLift server API call ProcessReady() */
  logPaths: string[];
  /** [DEPRECATED] The maximum value that is allowed for the fleet's instance count. When creating a new fleet, GameLift automatically sets this value to "1". Once the fleet is active, you can change this value. */
  maxSize: number;
  /** The name of an Amazon CloudWatch metric group. A metric group aggregates the metrics for all fleets in the group. Specify a string containing the metric group name. You can use an existing name or use a new name to create a new metric group. Currently, this parameter can have only one string. */
  metricGroups: string[];
  /** [DEPRECATED] The minimum value allowed for the fleet's instance count. When creating a new fleet, GameLift automatically sets this value to "0". After the fleet is active, you can change this value. */
  minSize: number;
  /** A descriptive label that is associated with a fleet. Fleet names do not need to be unique. */
  name: string;
  /** A game session protection policy to apply to all game sessions hosted on instances in this fleet. When protected, active game sessions cannot be terminated during a scale-down event. If this parameter is not set, instances in this fleet default to no protection. You can change a fleet's protection policy to affect future game sessions on the fleet. You can also set protection for individual game sessions. */
  newGameSessionProtectionPolicy: string;
  /** A unique identifier for the AWS account with the VPC that you want to peer your Amazon GameLift fleet with. You can find your account ID in the AWS Management Console under account settings. */
  peerVpcAwsAccountId: string;
  /** A unique identifier for a VPC with resources to be accessed by your Amazon GameLift fleet. The VPC must be in the same Region as your fleet. To look up a VPC ID, use the VPC Dashboard in the AWS Management Console. */
  peerVpcId: string;
  /** Configuration for player gateway. */
  playerGatewayConfiguration: LiftFleet_PlayerGatewayConfiguration;
  /** The player gateway mode for the fleet. */
  playerGatewayMode: string;
  /** A policy that limits the number of game sessions a player can create on the same fleet. This optional policy gives game owners control over how players can consume available game server resources. A resource creation policy makes the following statement: "An individual player can create a maximum number of new game sessions within a specified time period". The policy is evaluated when a player tries to create a new game session. For example, assume you have a policy of 10 new game sessions and a time period of 60 minutes. On receiving a CreateGameSession request, Amazon GameLift checks that the player (identified by CreatorId) has created fewer than 10 game sessions in the past 60 minutes. */
  resourceCreationLimitPolicy: LiftFleet_ResourceCreationLimitPolicy;
  /** A collection of server process configurations that describe the processes to run on each instance in a fleet. All fleets must have a runtime configuration. Each instance in the fleet maintains server processes as specified in the runtime configuration, launching new ones as existing processes end. Each instance regularly checks for an updated runtime configuration makes adjustments as called for. The runtime configuration enables the instances in a fleet to run multiple processes simultaneously. Potential scenarios are as follows: (1) Run multiple processes of a single game server executable to maximize usage of your hosting resources. (2) Run one or more processes of different executables, such as your game server and a metrics tracking program. (3) Run multiple processes of a single game server but with different launch parameters, for example to run one process on each instance in debug mode. An Amazon GameLift instance is limited to 50 processes running simultaneously. A runtime configuration must specify fewer than this limit. To calculate the total number of processes specified in a runtime configuration, add the values of the ConcurrentExecutions parameter for each ServerProcess object in the runtime configuration. */
  runtimeConfiguration: LiftFleet_RuntimeConfiguration;
  /** A list of rules that control how a fleet is scaled. */
  scalingPolicies: LiftFleet_ScalingPolicies[];
  /** A unique identifier for a Realtime script to be deployed on a new Realtime Servers fleet. The script must have been successfully uploaded to Amazon GameLift. This fleet setting cannot be changed once the fleet is created. Note: It is not currently possible to use the !Ref command to reference a script created with a CloudFormation template for the fleet property ScriptId. Instead, use Fn::GetAtt Script.Arn or Fn::GetAtt Script.Id to retrieve either of these properties as input for ScriptId. Alternatively, enter a ScriptId string manually. */
  scriptId: string;
  /** This parameter is no longer used but is retained for backward compatibility. Instead, specify server launch parameters in the RuntimeConfiguration parameter. A request must specify either a runtime configuration or values for both ServerLaunchParameters and ServerLaunchPath. */
  serverLaunchParameters: string;
  /** This parameter is no longer used. Instead, specify a server launch path using the RuntimeConfiguration parameter. Requests that specify a server launch path and launch parameters instead of a runtime configuration will continue to work. */
  serverLaunchPath: string;
  /** An array of key-value pairs to apply to this resource. */
  tags: LiftFleet_Tags[];
}

export const LiftFleet: ResourceBinding<LiftFleetConfig, LiftFleetAttrs> = {
  wireType: "aws_game_lift_fleet",
  fields: {
    anywhereConfiguration: "anywhere_configuration",
    applyCapacity: "apply_capacity",
    buildId: "build_id",
    certificateConfiguration: {
      wireName: "certificate_configuration",
      kind: "object",
      fields: LiftFleet_CertificateConfigurationFields,
    },
    computeType: "compute_type",
    description: "description",
    desiredEc2Instances: "desired_ec2_instances",
    ec2InboundPermissions: {
      wireName: "ec2_inbound_permissions",
      kind: "list",
      fields: LiftFleet_Ec2InboundPermissionsFields,
    },
    ec2InstanceType: "ec2_instance_type",
    fleetType: "fleet_type",
    instanceRoleArn: "instance_role_arn",
    instanceRoleCredentialsProvider: "instance_role_credentials_provider",
    logPaths: "log_paths",
    maxSize: "max_size",
    metricGroups: "metric_groups",
    minSize: "min_size",
    name: "name",
    newGameSessionProtectionPolicy: "new_game_session_protection_policy",
    peerVpcAwsAccountId: "peer_vpc_aws_account_id",
    peerVpcId: "peer_vpc_id",
    playerGatewayConfiguration: {
      wireName: "player_gateway_configuration",
      kind: "object",
      fields: LiftFleet_PlayerGatewayConfigurationFields,
    },
    playerGatewayMode: "player_gateway_mode",
    resourceCreationLimitPolicy: {
      wireName: "resource_creation_limit_policy",
      kind: "object",
      fields: LiftFleet_ResourceCreationLimitPolicyFields,
    },
    runtimeConfiguration: {
      wireName: "runtime_configuration",
      kind: "object",
      fields: LiftFleet_RuntimeConfigurationFields,
    },
    scalingPolicies: {
      wireName: "scaling_policies",
      kind: "list",
      fields: LiftFleet_ScalingPoliciesFields,
    },
    scriptId: "script_id",
    serverLaunchParameters: "server_launch_parameters",
    serverLaunchPath: "server_launch_path",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: LiftFleet_TagsFields,
    },
  },
};
