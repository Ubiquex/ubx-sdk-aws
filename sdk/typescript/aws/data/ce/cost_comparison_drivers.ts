// Code generated by `ubx sdk gen` from aws_ce_cost_comparison_drivers. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface CostComparisonDrivers_BaselineTimePeriod {
  end: string | Computed<string>;
  start: string | Computed<string>;
}

export interface CostComparisonDrivers_CostComparisonDrivers_CostDrivers_Metrics {
  baselineTimePeriodAmount?: string | Computed<string>;
  comparisonTimePeriodAmount?: string | Computed<string>;
  difference?: string | Computed<string>;
  unit?: string | Computed<string>;
}

export interface CostComparisonDrivers_CostComparisonDrivers_CostDrivers {
  metrics?: Record<string, CostComparisonDrivers_CostComparisonDrivers_CostDrivers_Metrics> | Computed<Record<string, CostComparisonDrivers_CostComparisonDrivers_CostDrivers_Metrics>>;
  name?: string | Computed<string>;
  type?: string | Computed<string>;
}

export interface CostComparisonDrivers_CostComparisonDrivers_CostSelector_And {
}

export interface CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategories {
  key?: string | Computed<string>;
  matchOptions?: string[] | Computed<string[]>;
  values?: string[] | Computed<string[]>;
}

export interface CostComparisonDrivers_CostComparisonDrivers_CostSelector {
  and?: CostComparisonDrivers_CostComparisonDrivers_CostSelector_And[] | Computed<CostComparisonDrivers_CostComparisonDrivers_CostSelector_And[]>;
  costCategories?: CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategories | Computed<CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategories>;
  dimensions?: CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategories | Computed<CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategories>;
  not?: unknown | Computed<unknown>;
  or?: CostComparisonDrivers_CostComparisonDrivers_CostSelector_And[] | Computed<CostComparisonDrivers_CostComparisonDrivers_CostSelector_And[]>;
  tags?: CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategories | Computed<CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategories>;
}

export interface CostComparisonDrivers_CostComparisonDrivers {
  costDrivers?: CostComparisonDrivers_CostComparisonDrivers_CostDrivers[] | Computed<CostComparisonDrivers_CostComparisonDrivers_CostDrivers[]>;
  costSelector?: CostComparisonDrivers_CostComparisonDrivers_CostSelector | Computed<CostComparisonDrivers_CostComparisonDrivers_CostSelector>;
  metrics?: Record<string, CostComparisonDrivers_CostComparisonDrivers_CostDrivers_Metrics> | Computed<Record<string, CostComparisonDrivers_CostComparisonDrivers_CostDrivers_Metrics>>;
}

export interface CostComparisonDrivers_GroupBy {
  key?: string | Computed<string>;
  type?: string | Computed<string>;
}

const CostComparisonDrivers_BaselineTimePeriodFields: FieldMap = {
  end: "end",
  start: "start",
};

const CostComparisonDrivers_CostComparisonDrivers_CostSelector_AndFields: FieldMap = {
};

const CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategoriesFields: FieldMap = {
  key: "key",
  matchOptions: "match_options",
  values: "values",
};

const CostComparisonDrivers_CostComparisonDrivers_CostSelectorFields: FieldMap = {
  and: {
    wireName: "and",
    kind: "list",
    fields: CostComparisonDrivers_CostComparisonDrivers_CostSelector_AndFields,
  },
  costCategories: {
    wireName: "cost_categories",
    kind: "object",
    fields: CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategoriesFields,
  },
  dimensions: {
    wireName: "dimensions",
    kind: "object",
    fields: CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategoriesFields,
  },
  not: "not",
  or: {
    wireName: "or",
    kind: "list",
    fields: CostComparisonDrivers_CostComparisonDrivers_CostSelector_AndFields,
  },
  tags: {
    wireName: "tags",
    kind: "object",
    fields: CostComparisonDrivers_CostComparisonDrivers_CostSelector_CostCategoriesFields,
  },
};

const CostComparisonDrivers_GroupByFields: FieldMap = {
  key: "key",
  type: "type",
};

export interface CostComparisonDriversConfig {
  /** <p>The time period of the request. </p> */
  baselineTimePeriod: CostComparisonDrivers_BaselineTimePeriod | Computed<CostComparisonDrivers_BaselineTimePeriod>;
  billingViewArn?: string | Computed<string>;
  /** <p>The time period of the request. </p> */
  comparisonTimePeriod: CostComparisonDrivers_BaselineTimePeriod | Computed<CostComparisonDrivers_BaselineTimePeriod>;
  /** <p>Use <code>Expression</code> to filter in various Cost Explorer APIs.</p> <p>Not all <code>Expression</code> types are supported in each API. Refer to the documentation for each specific API to see what is supported.</p> <p>There are two patterns:</p> <ul> <li> <p>Simple dimension values.</p> <ul> <li> <p>There are three types of simple dimension values: <code>CostCategories</code>, <code>Tags</code>, and <code>Dimensions</code>.</p> <ul> <li> <p>Specify the <code>CostCategories</code> field to define a filter that acts on Cost Categories.</p> </li> <li> <p>Specify the <code>Tags</code> field to define a filter that acts on Cost Allocation Tags.</p> </li> <li> <p>Specify the <code>Dimensions</code> field to define a filter that acts on the <a href="https://docs.aws.amazon.com/aws-cost-management/latest/APIReference/API_DimensionValues.html"> <code>DimensionValues</code> </a>.</p> </li> </ul> </li> <li> <p>For each filter type, you can set the dimension name and values for the filters that you plan to use.</p> <ul> <li> <p>For example, you can filter for <code>REGION==us-east-1 OR REGION==us-west-1</code>. For <code>GetRightsizingRecommendation</code>, the Region is a full name (for example, <code>REGION==US East (N. Virginia)</code>.</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ] } }</code> </p> </li> <li> <p>As shown in the previous example, lists of dimension values are combined with <code>OR</code> when applying the filter.</p> </li> </ul> </li> <li> <p>You can also set different match options to further control how the filter behaves. Not all APIs support match options. Refer to the documentation for each specific API to see what is supported.</p> <ul> <li> <p>For example, you can filter for linked account names that start with "a".</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "Dimensions": { "Key": "LINKED_ACCOUNT_NAME", "MatchOptions": [ "STARTS_WITH" ], "Values": [ "a" ] } }</code> </p> </li> </ul> </li> </ul> </li> <li> <p>Compound <code>Expression</code> types with logical operations.</p> <ul> <li> <p>You can use multiple <code>Expression</code> types and the logical operators <code>AND/OR/NOT</code> to create a list of one or more <code>Expression</code> objects. By doing this, you can filter by more advanced options.</p> </li> <li> <p>For example, you can filter by <code>((REGION == us-east-1 OR REGION == us-west-1) OR (TAG.Type == Type1)) AND (USAGE_TYPE != DataTransfer)</code>.</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "And": [ {"Or": [ {"Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ] }}, {"Tags": { "Key": "TagName", "Values": ["Value1"] } } ]}, {"Not": {"Dimensions": { "Key": "USAGE_TYPE", "Values": ["DataTransfer"] }}} ] } </code> </p> </li> </ul> <note> <p>Because each <code>Expression</code> can have only one operator, the service returns an error if more than one is specified. The following example shows an <code>Expression</code> object that creates an error: <code> { "And": [ ... ], "Dimensions": { "Key": "USAGE_TYPE", "Values": [ "DataTransfer" ] } } </code> </p> <p>The following is an example of the corresponding error message: <code>"Expression has more than one roots. Only one root operator is allowed for each expression: And, Or, Not, Dimensions, Tags, CostCategories"</code> </p> </note> </li> </ul> <note> <p>For the <code>GetRightsizingRecommendation</code> action, a combination of OR and NOT isn't supported. OR isn't supported between different dimensions, or dimensions and tags. NOT operators aren't supported. Dimensions are also limited to <code>LINKED_ACCOUNT</code>, <code>REGION</code>, or <code>RIGHTSIZING_TYPE</code>.</p> <p>For the <code>GetReservationPurchaseRecommendation</code> action, only NOT is supported. AND and OR aren't supported. Dimensions are limited to <code>LINKED_ACCOUNT</code>.</p> </note> */
  filter?: CostComparisonDrivers_CostComparisonDrivers_CostSelector | Computed<CostComparisonDrivers_CostComparisonDrivers_CostSelector>;
  groupBy?: CostComparisonDrivers_GroupBy[] | Computed<CostComparisonDrivers_GroupBy[]>;
  maxResults?: number | Computed<number>;
  metricForComparison: string | Computed<string>;
  nextPageToken?: string | Computed<string>;
}

export interface CostComparisonDriversAttrs {
  /** <p>The time period of the request. </p> */
  baselineTimePeriod: CostComparisonDrivers_BaselineTimePeriod;
  billingViewArn: string;
  /** <p>The time period of the request. </p> */
  comparisonTimePeriod: CostComparisonDrivers_BaselineTimePeriod;
  costComparisonDrivers: CostComparisonDrivers_CostComparisonDrivers[];
  /** <p>Use <code>Expression</code> to filter in various Cost Explorer APIs.</p> <p>Not all <code>Expression</code> types are supported in each API. Refer to the documentation for each specific API to see what is supported.</p> <p>There are two patterns:</p> <ul> <li> <p>Simple dimension values.</p> <ul> <li> <p>There are three types of simple dimension values: <code>CostCategories</code>, <code>Tags</code>, and <code>Dimensions</code>.</p> <ul> <li> <p>Specify the <code>CostCategories</code> field to define a filter that acts on Cost Categories.</p> </li> <li> <p>Specify the <code>Tags</code> field to define a filter that acts on Cost Allocation Tags.</p> </li> <li> <p>Specify the <code>Dimensions</code> field to define a filter that acts on the <a href="https://docs.aws.amazon.com/aws-cost-management/latest/APIReference/API_DimensionValues.html"> <code>DimensionValues</code> </a>.</p> </li> </ul> </li> <li> <p>For each filter type, you can set the dimension name and values for the filters that you plan to use.</p> <ul> <li> <p>For example, you can filter for <code>REGION==us-east-1 OR REGION==us-west-1</code>. For <code>GetRightsizingRecommendation</code>, the Region is a full name (for example, <code>REGION==US East (N. Virginia)</code>.</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ] } }</code> </p> </li> <li> <p>As shown in the previous example, lists of dimension values are combined with <code>OR</code> when applying the filter.</p> </li> </ul> </li> <li> <p>You can also set different match options to further control how the filter behaves. Not all APIs support match options. Refer to the documentation for each specific API to see what is supported.</p> <ul> <li> <p>For example, you can filter for linked account names that start with "a".</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "Dimensions": { "Key": "LINKED_ACCOUNT_NAME", "MatchOptions": [ "STARTS_WITH" ], "Values": [ "a" ] } }</code> </p> </li> </ul> </li> </ul> </li> <li> <p>Compound <code>Expression</code> types with logical operations.</p> <ul> <li> <p>You can use multiple <code>Expression</code> types and the logical operators <code>AND/OR/NOT</code> to create a list of one or more <code>Expression</code> objects. By doing this, you can filter by more advanced options.</p> </li> <li> <p>For example, you can filter by <code>((REGION == us-east-1 OR REGION == us-west-1) OR (TAG.Type == Type1)) AND (USAGE_TYPE != DataTransfer)</code>.</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "And": [ {"Or": [ {"Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ] }}, {"Tags": { "Key": "TagName", "Values": ["Value1"] } } ]}, {"Not": {"Dimensions": { "Key": "USAGE_TYPE", "Values": ["DataTransfer"] }}} ] } </code> </p> </li> </ul> <note> <p>Because each <code>Expression</code> can have only one operator, the service returns an error if more than one is specified. The following example shows an <code>Expression</code> object that creates an error: <code> { "And": [ ... ], "Dimensions": { "Key": "USAGE_TYPE", "Values": [ "DataTransfer" ] } } </code> </p> <p>The following is an example of the corresponding error message: <code>"Expression has more than one roots. Only one root operator is allowed for each expression: And, Or, Not, Dimensions, Tags, CostCategories"</code> </p> </note> </li> </ul> <note> <p>For the <code>GetRightsizingRecommendation</code> action, a combination of OR and NOT isn't supported. OR isn't supported between different dimensions, or dimensions and tags. NOT operators aren't supported. Dimensions are also limited to <code>LINKED_ACCOUNT</code>, <code>REGION</code>, or <code>RIGHTSIZING_TYPE</code>.</p> <p>For the <code>GetReservationPurchaseRecommendation</code> action, only NOT is supported. AND and OR aren't supported. Dimensions are limited to <code>LINKED_ACCOUNT</code>.</p> </note> */
  filter: CostComparisonDrivers_CostComparisonDrivers_CostSelector;
  groupBy: CostComparisonDrivers_GroupBy[];
  maxResults: number;
  metricForComparison: string;
  nextPageToken: string;
}

export const CostComparisonDrivers: DataSourceBinding<CostComparisonDriversConfig, CostComparisonDriversAttrs> = {
  wireType: "aws_ce_cost_comparison_drivers",
  fields: {
    baselineTimePeriod: {
      wireName: "baseline_time_period",
      kind: "object",
      fields: CostComparisonDrivers_BaselineTimePeriodFields,
    },
    billingViewArn: "billing_view_arn",
    comparisonTimePeriod: {
      wireName: "comparison_time_period",
      kind: "object",
      fields: CostComparisonDrivers_BaselineTimePeriodFields,
    },
    filter: {
      wireName: "filter",
      kind: "object",
      fields: CostComparisonDrivers_CostComparisonDrivers_CostSelectorFields,
    },
    groupBy: {
      wireName: "group_by",
      kind: "list",
      fields: CostComparisonDrivers_GroupByFields,
    },
    maxResults: "max_results",
    metricForComparison: "metric_for_comparison",
    nextPageToken: "next_page_token",
  },
};
