// Code generated by `ubx sdk gen` from aws_ce_savings_plans_purchase_recommendation. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface SavingsPlansPurchaseRecommendation_Filter_And {
}

export interface SavingsPlansPurchaseRecommendation_Filter_CostCategories {
  /** <p>The unique name of the cost category.</p> */
  key?: string | Computed<string>;
  matchOptions?: string[] | Computed<string[]>;
  values?: string[] | Computed<string[]>;
}

export interface SavingsPlansPurchaseRecommendation_Filter {
  and?: SavingsPlansPurchaseRecommendation_Filter_And[] | Computed<SavingsPlansPurchaseRecommendation_Filter_And[]>;
  /** <p>The Cost Categories values used for filtering the costs.</p> <p>If <code>Values</code> and <code>Key</code> are not specified, the <code>ABSENT</code> <code>MatchOption</code> is applied to all Cost Categories. That is, it filters on resources that aren't mapped to any Cost Categories.</p> <p>If <code>Values</code> is provided and <code>Key</code> isn't specified, the <code>ABSENT</code> <code>MatchOption</code> is applied to the Cost Categories <code>Key</code> only. That is, it filters on resources without the given Cost Categories key.</p> */
  costCategories?: SavingsPlansPurchaseRecommendation_Filter_CostCategories | Computed<SavingsPlansPurchaseRecommendation_Filter_CostCategories>;
  /** <p>The metadata that you can use to filter and group your results. You can use <code>GetDimensionValues</code> to find specific values.</p> */
  dimensions?: SavingsPlansPurchaseRecommendation_Filter_CostCategories | Computed<SavingsPlansPurchaseRecommendation_Filter_CostCategories>;
  /** <p>Use <code>Expression</code> to filter in various Cost Explorer APIs.</p> <p>Not all <code>Expression</code> types are supported in each API. Refer to the documentation for each specific API to see what is supported.</p> <p>There are two patterns:</p> <ul> <li> <p>Simple dimension values.</p> <ul> <li> <p>There are three types of simple dimension values: <code>CostCategories</code>, <code>Tags</code>, and <code>Dimensions</code>.</p> <ul> <li> <p>Specify the <code>CostCategories</code> field to define a filter that acts on Cost Categories.</p> </li> <li> <p>Specify the <code>Tags</code> field to define a filter that acts on Cost Allocation Tags.</p> </li> <li> <p>Specify the <code>Dimensions</code> field to define a filter that acts on the <a href="https://docs.aws.amazon.com/aws-cost-management/latest/APIReference/API_DimensionValues.html"> <code>DimensionValues</code> </a>.</p> </li> </ul> </li> <li> <p>For each filter type, you can set the dimension name and values for the filters that you plan to use.</p> <ul> <li> <p>For example, you can filter for <code>REGION==us-east-1 OR REGION==us-west-1</code>. For <code>GetRightsizingRecommendation</code>, the Region is a full name (for example, <code>REGION==US East (N. Virginia)</code>.</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ] } }</code> </p> </li> <li> <p>As shown in the previous example, lists of dimension values are combined with <code>OR</code> when applying the filter.</p> </li> </ul> </li> <li> <p>You can also set different match options to further control how the filter behaves. Not all APIs support match options. Refer to the documentation for each specific API to see what is supported.</p> <ul> <li> <p>For example, you can filter for linked account names that start with "a".</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "Dimensions": { "Key": "LINKED_ACCOUNT_NAME", "MatchOptions": [ "STARTS_WITH" ], "Values": [ "a" ] } }</code> </p> </li> </ul> </li> </ul> </li> <li> <p>Compound <code>Expression</code> types with logical operations.</p> <ul> <li> <p>You can use multiple <code>Expression</code> types and the logical operators <code>AND/OR/NOT</code> to create a list of one or more <code>Expression</code> objects. By doing this, you can filter by more advanced options.</p> </li> <li> <p>For example, you can filter by <code>((REGION == us-east-1 OR REGION == us-west-1) OR (TAG.Type == Type1)) AND (USAGE_TYPE != DataTransfer)</code>.</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "And": [ {"Or": [ {"Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ] }}, {"Tags": { "Key": "TagName", "Values": ["Value1"] } } ]}, {"Not": {"Dimensions": { "Key": "USAGE_TYPE", "Values": ["DataTransfer"] }}} ] } </code> </p> </li> </ul> <note> <p>Because each <code>Expression</code> can have only one operator, the service returns an error if more than one is specified. The following example shows an <code>Expression</code> object that creates an error: <code> { "And": [ ... ], "Dimensions": { "Key": "USAGE_TYPE", "Values": [ "DataTransfer" ] } } </code> </p> <p>The following is an example of the corresponding error message: <code>"Expression has more than one roots. Only one root operator is allowed for each expression: And, Or, Not, Dimensions, Tags, CostCategories"</code> </p> </note> </li> </ul> <note> <p>For the <code>GetRightsizingRecommendation</code> action, a combination of OR and NOT isn't supported. OR isn't supported between different dimensions, or dimensions and tags. NOT operators aren't supported. Dimensions are also limited to <code>LINKED_ACCOUNT</code>, <code>REGION</code>, or <code>RIGHTSIZING_TYPE</code>.</p> <p>For the <code>GetReservationPurchaseRecommendation</code> action, only NOT is supported. AND and OR aren't supported. Dimensions are limited to <code>LINKED_ACCOUNT</code>.</p> </note> */
  not?: unknown | Computed<unknown>;
  or?: SavingsPlansPurchaseRecommendation_Filter_And[] | Computed<SavingsPlansPurchaseRecommendation_Filter_And[]>;
  /** <p>The values that are available for a tag.</p> <p>If <code>Values</code> and <code>Key</code> aren't specified, the <code>ABSENT</code> <code>MatchOption</code> is applied to all tags. That is, it's filtered on resources with no tags.</p> <p>If <code>Key</code> is provided and <code>Values</code> isn't specified, the <code>ABSENT</code> <code>MatchOption</code> is applied to the tag <code>Key</code> only. That is, it's filtered on resources without the given tag key.</p> */
  tags?: SavingsPlansPurchaseRecommendation_Filter_CostCategories | Computed<SavingsPlansPurchaseRecommendation_Filter_CostCategories>;
}

export interface SavingsPlansPurchaseRecommendation_Metadata {
  additionalMetadata?: string | Computed<string>;
  generationTimestamp?: string | Computed<string>;
  recommendationId?: string | Computed<string>;
}

export interface SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendationDetails_SavingsPlansDetails {
  instanceFamily?: string | Computed<string>;
  offeringId?: string | Computed<string>;
  region?: string | Computed<string>;
}

export interface SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendationDetails {
  accountId?: string | Computed<string>;
  currencyCode?: string | Computed<string>;
  currentAverageHourlyOnDemandSpend?: string | Computed<string>;
  currentMaximumHourlyOnDemandSpend?: string | Computed<string>;
  currentMinimumHourlyOnDemandSpend?: string | Computed<string>;
  estimatedAverageUtilization?: string | Computed<string>;
  estimatedMonthlySavingsAmount?: string | Computed<string>;
  estimatedOnDemandCost?: string | Computed<string>;
  estimatedOnDemandCostWithCurrentCommitment?: string | Computed<string>;
  estimatedRoi?: string | Computed<string>;
  estimatedSavingsAmount?: string | Computed<string>;
  estimatedSavingsPercentage?: string | Computed<string>;
  estimatedSpcost?: string | Computed<string>;
  hourlyCommitmentToPurchase?: string | Computed<string>;
  recommendationDetailId?: string | Computed<string>;
  savingsPlansDetails?: SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendationDetails_SavingsPlansDetails | Computed<SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendationDetails_SavingsPlansDetails>;
  upfrontCost?: string | Computed<string>;
}

export interface SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendationSummary {
  currencyCode?: string | Computed<string>;
  currentOnDemandSpend?: string | Computed<string>;
  dailyCommitmentToPurchase?: string | Computed<string>;
  estimatedMonthlySavingsAmount?: string | Computed<string>;
  estimatedOnDemandCostWithCurrentCommitment?: string | Computed<string>;
  estimatedRoi?: string | Computed<string>;
  estimatedSavingsAmount?: string | Computed<string>;
  estimatedSavingsPercentage?: string | Computed<string>;
  estimatedTotalCost?: string | Computed<string>;
  hourlyCommitmentToPurchase?: string | Computed<string>;
  totalRecommendationCount?: string | Computed<string>;
}

export interface SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation {
  accountScope?: string | Computed<string>;
  lookbackPeriodInDays?: string | Computed<string>;
  paymentOption?: string | Computed<string>;
  savingsPlansPurchaseRecommendationDetails?: SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendationDetails[] | Computed<SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendationDetails[]>;
  /** <p>Summary metrics for your Savings Plans Purchase Recommendations.</p> */
  savingsPlansPurchaseRecommendationSummary?: SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendationSummary | Computed<SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendationSummary>;
  savingsPlansType?: string | Computed<string>;
  termInYears?: string | Computed<string>;
}

const SavingsPlansPurchaseRecommendation_Filter_AndFields: FieldMap = {
};

const SavingsPlansPurchaseRecommendation_Filter_CostCategoriesFields: FieldMap = {
  key: "key",
  matchOptions: "match_options",
  values: "values",
};

const SavingsPlansPurchaseRecommendation_FilterFields: FieldMap = {
  and: {
    wireName: "and",
    kind: "list",
    fields: SavingsPlansPurchaseRecommendation_Filter_AndFields,
  },
  costCategories: {
    wireName: "cost_categories",
    kind: "object",
    fields: SavingsPlansPurchaseRecommendation_Filter_CostCategoriesFields,
  },
  dimensions: {
    wireName: "dimensions",
    kind: "object",
    fields: SavingsPlansPurchaseRecommendation_Filter_CostCategoriesFields,
  },
  not: "not",
  or: {
    wireName: "or",
    kind: "list",
    fields: SavingsPlansPurchaseRecommendation_Filter_AndFields,
  },
  tags: {
    wireName: "tags",
    kind: "object",
    fields: SavingsPlansPurchaseRecommendation_Filter_CostCategoriesFields,
  },
};

export interface SavingsPlansPurchaseRecommendationConfig {
  accountScope?: string | Computed<string>;
  /** <p>Use <code>Expression</code> to filter in various Cost Explorer APIs.</p> <p>Not all <code>Expression</code> types are supported in each API. Refer to the documentation for each specific API to see what is supported.</p> <p>There are two patterns:</p> <ul> <li> <p>Simple dimension values.</p> <ul> <li> <p>There are three types of simple dimension values: <code>CostCategories</code>, <code>Tags</code>, and <code>Dimensions</code>.</p> <ul> <li> <p>Specify the <code>CostCategories</code> field to define a filter that acts on Cost Categories.</p> </li> <li> <p>Specify the <code>Tags</code> field to define a filter that acts on Cost Allocation Tags.</p> </li> <li> <p>Specify the <code>Dimensions</code> field to define a filter that acts on the <a href="https://docs.aws.amazon.com/aws-cost-management/latest/APIReference/API_DimensionValues.html"> <code>DimensionValues</code> </a>.</p> </li> </ul> </li> <li> <p>For each filter type, you can set the dimension name and values for the filters that you plan to use.</p> <ul> <li> <p>For example, you can filter for <code>REGION==us-east-1 OR REGION==us-west-1</code>. For <code>GetRightsizingRecommendation</code>, the Region is a full name (for example, <code>REGION==US East (N. Virginia)</code>.</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ] } }</code> </p> </li> <li> <p>As shown in the previous example, lists of dimension values are combined with <code>OR</code> when applying the filter.</p> </li> </ul> </li> <li> <p>You can also set different match options to further control how the filter behaves. Not all APIs support match options. Refer to the documentation for each specific API to see what is supported.</p> <ul> <li> <p>For example, you can filter for linked account names that start with "a".</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "Dimensions": { "Key": "LINKED_ACCOUNT_NAME", "MatchOptions": [ "STARTS_WITH" ], "Values": [ "a" ] } }</code> </p> </li> </ul> </li> </ul> </li> <li> <p>Compound <code>Expression</code> types with logical operations.</p> <ul> <li> <p>You can use multiple <code>Expression</code> types and the logical operators <code>AND/OR/NOT</code> to create a list of one or more <code>Expression</code> objects. By doing this, you can filter by more advanced options.</p> </li> <li> <p>For example, you can filter by <code>((REGION == us-east-1 OR REGION == us-west-1) OR (TAG.Type == Type1)) AND (USAGE_TYPE != DataTransfer)</code>.</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "And": [ {"Or": [ {"Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ] }}, {"Tags": { "Key": "TagName", "Values": ["Value1"] } } ]}, {"Not": {"Dimensions": { "Key": "USAGE_TYPE", "Values": ["DataTransfer"] }}} ] } </code> </p> </li> </ul> <note> <p>Because each <code>Expression</code> can have only one operator, the service returns an error if more than one is specified. The following example shows an <code>Expression</code> object that creates an error: <code> { "And": [ ... ], "Dimensions": { "Key": "USAGE_TYPE", "Values": [ "DataTransfer" ] } } </code> </p> <p>The following is an example of the corresponding error message: <code>"Expression has more than one roots. Only one root operator is allowed for each expression: And, Or, Not, Dimensions, Tags, CostCategories"</code> </p> </note> </li> </ul> <note> <p>For the <code>GetRightsizingRecommendation</code> action, a combination of OR and NOT isn't supported. OR isn't supported between different dimensions, or dimensions and tags. NOT operators aren't supported. Dimensions are also limited to <code>LINKED_ACCOUNT</code>, <code>REGION</code>, or <code>RIGHTSIZING_TYPE</code>.</p> <p>For the <code>GetReservationPurchaseRecommendation</code> action, only NOT is supported. AND and OR aren't supported. Dimensions are limited to <code>LINKED_ACCOUNT</code>.</p> </note> */
  filter?: SavingsPlansPurchaseRecommendation_Filter | Computed<SavingsPlansPurchaseRecommendation_Filter>;
  lookbackPeriodInDays: string | Computed<string>;
  nextPageToken?: string | Computed<string>;
  pageSize?: number | Computed<number>;
  paymentOption: string | Computed<string>;
  savingsPlansType: string | Computed<string>;
  termInYears: string | Computed<string>;
}

export interface SavingsPlansPurchaseRecommendationAttrs {
  accountScope: string;
  /** <p>Use <code>Expression</code> to filter in various Cost Explorer APIs.</p> <p>Not all <code>Expression</code> types are supported in each API. Refer to the documentation for each specific API to see what is supported.</p> <p>There are two patterns:</p> <ul> <li> <p>Simple dimension values.</p> <ul> <li> <p>There are three types of simple dimension values: <code>CostCategories</code>, <code>Tags</code>, and <code>Dimensions</code>.</p> <ul> <li> <p>Specify the <code>CostCategories</code> field to define a filter that acts on Cost Categories.</p> </li> <li> <p>Specify the <code>Tags</code> field to define a filter that acts on Cost Allocation Tags.</p> </li> <li> <p>Specify the <code>Dimensions</code> field to define a filter that acts on the <a href="https://docs.aws.amazon.com/aws-cost-management/latest/APIReference/API_DimensionValues.html"> <code>DimensionValues</code> </a>.</p> </li> </ul> </li> <li> <p>For each filter type, you can set the dimension name and values for the filters that you plan to use.</p> <ul> <li> <p>For example, you can filter for <code>REGION==us-east-1 OR REGION==us-west-1</code>. For <code>GetRightsizingRecommendation</code>, the Region is a full name (for example, <code>REGION==US East (N. Virginia)</code>.</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ] } }</code> </p> </li> <li> <p>As shown in the previous example, lists of dimension values are combined with <code>OR</code> when applying the filter.</p> </li> </ul> </li> <li> <p>You can also set different match options to further control how the filter behaves. Not all APIs support match options. Refer to the documentation for each specific API to see what is supported.</p> <ul> <li> <p>For example, you can filter for linked account names that start with "a".</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "Dimensions": { "Key": "LINKED_ACCOUNT_NAME", "MatchOptions": [ "STARTS_WITH" ], "Values": [ "a" ] } }</code> </p> </li> </ul> </li> </ul> </li> <li> <p>Compound <code>Expression</code> types with logical operations.</p> <ul> <li> <p>You can use multiple <code>Expression</code> types and the logical operators <code>AND/OR/NOT</code> to create a list of one or more <code>Expression</code> objects. By doing this, you can filter by more advanced options.</p> </li> <li> <p>For example, you can filter by <code>((REGION == us-east-1 OR REGION == us-west-1) OR (TAG.Type == Type1)) AND (USAGE_TYPE != DataTransfer)</code>.</p> </li> <li> <p>The corresponding <code>Expression</code> for this example is as follows: <code>{ "And": [ {"Or": [ {"Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ] }}, {"Tags": { "Key": "TagName", "Values": ["Value1"] } } ]}, {"Not": {"Dimensions": { "Key": "USAGE_TYPE", "Values": ["DataTransfer"] }}} ] } </code> </p> </li> </ul> <note> <p>Because each <code>Expression</code> can have only one operator, the service returns an error if more than one is specified. The following example shows an <code>Expression</code> object that creates an error: <code> { "And": [ ... ], "Dimensions": { "Key": "USAGE_TYPE", "Values": [ "DataTransfer" ] } } </code> </p> <p>The following is an example of the corresponding error message: <code>"Expression has more than one roots. Only one root operator is allowed for each expression: And, Or, Not, Dimensions, Tags, CostCategories"</code> </p> </note> </li> </ul> <note> <p>For the <code>GetRightsizingRecommendation</code> action, a combination of OR and NOT isn't supported. OR isn't supported between different dimensions, or dimensions and tags. NOT operators aren't supported. Dimensions are also limited to <code>LINKED_ACCOUNT</code>, <code>REGION</code>, or <code>RIGHTSIZING_TYPE</code>.</p> <p>For the <code>GetReservationPurchaseRecommendation</code> action, only NOT is supported. AND and OR aren't supported. Dimensions are limited to <code>LINKED_ACCOUNT</code>.</p> </note> */
  filter: SavingsPlansPurchaseRecommendation_Filter;
  lookbackPeriodInDays: string;
  /** <p>Metadata about your Savings Plans Purchase Recommendations.</p> */
  metadata: SavingsPlansPurchaseRecommendation_Metadata;
  nextPageToken: string;
  pageSize: number;
  paymentOption: string;
  /** <p>Contains your request parameters, Savings Plan Recommendations Summary, and Details.</p> */
  savingsPlansPurchaseRecommendation: SavingsPlansPurchaseRecommendation_SavingsPlansPurchaseRecommendation;
  savingsPlansType: string;
  termInYears: string;
}

export const SavingsPlansPurchaseRecommendation: DataSourceBinding<SavingsPlansPurchaseRecommendationConfig, SavingsPlansPurchaseRecommendationAttrs> = {
  wireType: "aws_ce_savings_plans_purchase_recommendation",
  fields: {
    accountScope: "account_scope",
    filter: {
      wireName: "filter",
      kind: "object",
      fields: SavingsPlansPurchaseRecommendation_FilterFields,
    },
    lookbackPeriodInDays: "lookback_period_in_days",
    nextPageToken: "next_page_token",
    pageSize: "page_size",
    paymentOption: "payment_option",
    savingsPlansType: "savings_plans_type",
    termInYears: "term_in_years",
  },
};
