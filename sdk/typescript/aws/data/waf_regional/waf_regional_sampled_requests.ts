// Code generated by `ubx sdk gen` from aws_waf_regional_sampled_requests. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface WafRegionalSampledRequests_SampledRequests_Request_Headers {
  name?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface WafRegionalSampledRequests_SampledRequests_Request {
  clientIp?: string | Computed<string>;
  country?: string | Computed<string>;
  headers?: WafRegionalSampledRequests_SampledRequests_Request_Headers[] | Computed<WafRegionalSampledRequests_SampledRequests_Request_Headers[]>;
  httpversion?: string | Computed<string>;
  method?: string | Computed<string>;
  uri?: string | Computed<string>;
}

export interface WafRegionalSampledRequests_SampledRequests {
  action?: string | Computed<string>;
  request?: WafRegionalSampledRequests_SampledRequests_Request | Computed<WafRegionalSampledRequests_SampledRequests_Request>;
  ruleWithinRuleGroup?: string | Computed<string>;
  timestamp?: string | Computed<string>;
  weight?: number | Computed<number>;
}

export interface WafRegionalSampledRequests_TimeWindow {
  endTime: string | Computed<string>;
  startTime: string | Computed<string>;
}

const WafRegionalSampledRequests_TimeWindowFields: FieldMap = {
  endTime: "end_time",
  startTime: "start_time",
};

export interface WafRegionalSampledRequestsConfig {
  maxItems: number | Computed<number>;
  ruleId: string | Computed<string>;
  /** <note> <p>This is <b>AWS WAF Classic</b> documentation. For more information, see <a href="https://docs.aws.amazon.com/waf/latest/developerguide/classic-waf-chapter.html">AWS WAF Classic</a> in the developer guide.</p> <p> <b>For the latest version of AWS WAF</b>, use the AWS WAFV2 API and see the <a href="https://docs.aws.amazon.com/waf/latest/developerguide/waf-chapter.html">AWS WAF Developer Guide</a>. With the latest version, AWS WAF has a single set of endpoints for regional and global use. </p> </note> <p>In a <a>GetSampledRequests</a> request, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which you want AWS WAF to return a sample of web requests.</p> <p>You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, <code>Z</code>. For example, <code>"2016-09-27T14:50Z"</code>. </p> <p>In a <a>GetSampledRequests</a> response, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which AWS WAF actually returned a sample of web requests. AWS WAF gets the specified number of requests from among the first 5,000 requests that your AWS resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, AWS WAF stops sampling after the 5,000th request. In that case, <code>EndTime</code> is the time that AWS WAF received the 5,000th request. </p> */
  timeWindow: WafRegionalSampledRequests_TimeWindow | Computed<WafRegionalSampledRequests_TimeWindow>;
  webAclId: string | Computed<string>;
}

export interface WafRegionalSampledRequestsAttrs {
  maxItems: number;
  populationSize: number;
  ruleId: string;
  sampledRequests: WafRegionalSampledRequests_SampledRequests[];
  /** <note> <p>This is <b>AWS WAF Classic</b> documentation. For more information, see <a href="https://docs.aws.amazon.com/waf/latest/developerguide/classic-waf-chapter.html">AWS WAF Classic</a> in the developer guide.</p> <p> <b>For the latest version of AWS WAF</b>, use the AWS WAFV2 API and see the <a href="https://docs.aws.amazon.com/waf/latest/developerguide/waf-chapter.html">AWS WAF Developer Guide</a>. With the latest version, AWS WAF has a single set of endpoints for regional and global use. </p> </note> <p>In a <a>GetSampledRequests</a> request, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which you want AWS WAF to return a sample of web requests.</p> <p>You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, <code>Z</code>. For example, <code>"2016-09-27T14:50Z"</code>. </p> <p>In a <a>GetSampledRequests</a> response, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which AWS WAF actually returned a sample of web requests. AWS WAF gets the specified number of requests from among the first 5,000 requests that your AWS resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, AWS WAF stops sampling after the 5,000th request. In that case, <code>EndTime</code> is the time that AWS WAF received the 5,000th request. </p> */
  timeWindow: WafRegionalSampledRequests_TimeWindow;
  webAclId: string;
}

export const WafRegionalSampledRequests: DataSourceBinding<WafRegionalSampledRequestsConfig, WafRegionalSampledRequestsAttrs> = {
  wireType: "aws_waf_regional_sampled_requests",
  fields: {
    maxItems: "max_items",
    ruleId: "rule_id",
    timeWindow: {
      wireName: "time_window",
      kind: "object",
      fields: WafRegionalSampledRequests_TimeWindowFields,
    },
    webAclId: "web_acl_id",
  },
};
