// Code generated by `ubx sdk gen` from aws_wellarchitected_lens_review. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface LensReview_LensReview_JiraConfiguration_SelectedPillars {
  pillarId?: string | Computed<string>;
  selectedQuestionIds?: string[] | Computed<string[]>;
}

export interface LensReview_LensReview_JiraConfiguration {
  selectedPillars?: LensReview_LensReview_JiraConfiguration_SelectedPillars[] | Computed<LensReview_LensReview_JiraConfiguration_SelectedPillars[]>;
}

export interface LensReview_LensReview_PillarReviewSummaries {
  notes?: string | Computed<string>;
  pillarId?: string | Computed<string>;
  pillarName?: string | Computed<string>;
  prioritizedRiskCounts?: Record<string, number> | Computed<Record<string, number>>;
  riskCounts?: Record<string, number> | Computed<Record<string, number>>;
}

export interface LensReview_LensReview_Profiles {
  profileArn?: string | Computed<string>;
  profileVersion?: string | Computed<string>;
}

export interface LensReview_LensReview {
  /** <p>Selected questions in the workload.</p> */
  jiraConfiguration?: LensReview_LensReview_JiraConfiguration | Computed<LensReview_LensReview_JiraConfiguration>;
  /** <p>The alias of the lens.</p> <p>For Amazon Web Services official lenses, this is either the lens alias, such as <code>serverless</code>, or the lens ARN, such as <code>arn:aws:wellarchitected:us-east-1::lens/serverless</code>. Note that some operations (such as ExportLens and CreateLensShare) are not permitted on Amazon Web Services official lenses.</p> <p>For custom lenses, this is the lens ARN, such as <code>arn:aws:wellarchitected:us-west-2:123456789012:lens/0123456789abcdef01234567890abcdef</code>. </p> <p>Each lens is identified by its <a>LensSummary$LensAlias</a>.</p> */
  lensAlias?: string | Computed<string>;
  lensArn?: string | Computed<string>;
  /** <p>The full name of the lens.</p> */
  lensName?: string | Computed<string>;
  lensStatus?: string | Computed<string>;
  lensVersion?: string | Computed<string>;
  /** <p>The token to use to retrieve the next set of results.</p> */
  nextToken?: string | Computed<string>;
  /** <p>The notes associated with the workload.</p> <p>For a review template, these are the notes that will be associated with the workload when the template is applied.</p> */
  notes?: string | Computed<string>;
  /** <p>List of pillar review summaries of lens review in a workload.</p> */
  pillarReviewSummaries?: LensReview_LensReview_PillarReviewSummaries[] | Computed<LensReview_LensReview_PillarReviewSummaries[]>;
  /** <p>A map from risk names to the count of how many questions have that rating.</p> */
  prioritizedRiskCounts?: Record<string, number> | Computed<Record<string, number>>;
  profiles?: LensReview_LensReview_Profiles[] | Computed<LensReview_LensReview_Profiles[]>;
  /** <p>A map from risk names to the count of how many questions have that rating.</p> */
  riskCounts?: Record<string, number> | Computed<Record<string, number>>;
  updatedAt?: string | Computed<string>;
}

export interface LensReviewConfig {
  /** <p>The alias of the lens.</p> <p>For Amazon Web Services official lenses, this is either the lens alias, such as <code>serverless</code>, or the lens ARN, such as <code>arn:aws:wellarchitected:us-east-1::lens/serverless</code>. Note that some operations (such as ExportLens and CreateLensShare) are not permitted on Amazon Web Services official lenses.</p> <p>For custom lenses, this is the lens ARN, such as <code>arn:aws:wellarchitected:us-west-2:123456789012:lens/0123456789abcdef01234567890abcdef</code>. </p> <p>Each lens is identified by its <a>LensSummary$LensAlias</a>.</p> */
  lensAlias: string | Computed<string>;
  /** <p>The milestone number.</p> <p>A workload can have a maximum of 100 milestones.</p> */
  milestoneNumber?: number | Computed<number>;
  /** <p>The ID assigned to the workload. This ID is unique within an Amazon Web Services Region.</p> */
  workloadId: string | Computed<string>;
}

export interface LensReviewAttrs {
  /** <p>The alias of the lens.</p> <p>For Amazon Web Services official lenses, this is either the lens alias, such as <code>serverless</code>, or the lens ARN, such as <code>arn:aws:wellarchitected:us-east-1::lens/serverless</code>. Note that some operations (such as ExportLens and CreateLensShare) are not permitted on Amazon Web Services official lenses.</p> <p>For custom lenses, this is the lens ARN, such as <code>arn:aws:wellarchitected:us-west-2:123456789012:lens/0123456789abcdef01234567890abcdef</code>. </p> <p>Each lens is identified by its <a>LensSummary$LensAlias</a>.</p> */
  lensAlias: string;
  /** <p>A lens review of a question.</p> */
  lensReview: LensReview_LensReview;
  /** <p>The milestone number.</p> <p>A workload can have a maximum of 100 milestones.</p> */
  milestoneNumber: number;
  /** <p>The ID assigned to the workload. This ID is unique within an Amazon Web Services Region.</p> */
  workloadId: string;
}

export const LensReview: DataSourceBinding<LensReviewConfig, LensReviewAttrs> = {
  wireType: "aws_wellarchitected_lens_review",
  fields: {
    lensAlias: "lens_alias",
    milestoneNumber: "milestone_number",
    workloadId: "workload_id",
  },
};
