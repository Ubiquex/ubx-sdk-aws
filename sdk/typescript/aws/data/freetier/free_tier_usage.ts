// Code generated by `ubx sdk gen` from aws_freetier_free_tier_usage. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface FreeTierUsage_Filter_And {
}

export interface FreeTierUsage_Filter_Dimensions {
  key: string | Computed<string>;
  matchOptions: string[] | Computed<string[]>;
  values: string[] | Computed<string[]>;
}

export interface FreeTierUsage_Filter {
  and?: FreeTierUsage_Filter_And[] | Computed<FreeTierUsage_Filter_And[]>;
  /** <p>Contains the specifications for the filters to use for your request.</p> */
  dimensions?: FreeTierUsage_Filter_Dimensions | Computed<FreeTierUsage_Filter_Dimensions>;
  /** <p>Use <code>Expression</code> to filter in the <code>GetFreeTierUsage</code> API operation.</p> <p>You can use the following patterns:</p> <ul> <li> <p>Simple dimension values (<code>Dimensions</code> root operator)</p> </li> <li> <p>Complex expressions with logical operators (<code>AND</code>, <code>NOT</code>, and <code>OR</code> root operators).</p> </li> </ul> <p>For <i>simple dimension values</i>, you can set the dimension name, values, and match type for the filters that you plan to use.</p> <p> <b>Example for simple dimension values</b> </p> <p>You can filter to match exactly for <code>REGION==us-east-1 OR REGION==us-west-1</code>.</p> <p>The corresponding <code>Expression</code> appears like the following: <code>{ "Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ], "MatchOptions": ["EQUALS"] } }</code> </p> <p>As shown in the previous example, lists of dimension values are combined with <code>OR</code> when you apply the filter.</p> <p>For <i>complex expressions with logical operators</i>, you can have nested expressions to use the logical operators and specify advanced filtering.</p> <p> <b>Example for complex expressions with logical operators</b> </p> <p>You can filter by <code>((REGION == us-east-1 OR REGION == us-west-1) OR (SERVICE CONTAINS AWSLambda)) AND (USAGE_TYPE !CONTAINS DataTransfer)</code>.</p> <p>The corresponding <code>Expression</code> appears like the following: <code>{ "And": [ {"Or": [ {"Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ], "MatchOptions": ["EQUALS"] }}, {"Dimensions": { "Key": "SERVICE", "Values": ["AWSLambda"], "MatchOptions": ["CONTAINS"] } } ]}, {"Not": {"Dimensions": { "Key": "USAGE_TYPE", "Values": ["DataTransfer"], "MatchOptions": ["CONTAINS"] }}} ] }</code> </p> <p>In the following <b>Contents</b>, you must specify exactly one of the following root operators.</p> */
  not?: unknown | Computed<unknown>;
  or?: FreeTierUsage_Filter_And[] | Computed<FreeTierUsage_Filter_And[]>;
}

export interface FreeTierUsage_FreeTierUsages {
  actualUsageAmount?: number | Computed<number>;
  description?: string | Computed<string>;
  forecastedUsageAmount?: number | Computed<number>;
  freeTierType?: string | Computed<string>;
  limit?: number | Computed<number>;
  operation?: string | Computed<string>;
  region?: string | Computed<string>;
  service?: string | Computed<string>;
  unit?: string | Computed<string>;
  usageType?: string | Computed<string>;
}

const FreeTierUsage_Filter_AndFields: FieldMap = {
};

const FreeTierUsage_Filter_DimensionsFields: FieldMap = {
  key: "key",
  matchOptions: "match_options",
  values: "values",
};

const FreeTierUsage_FilterFields: FieldMap = {
  and: {
    wireName: "and",
    kind: "list",
    fields: FreeTierUsage_Filter_AndFields,
  },
  dimensions: {
    wireName: "dimensions",
    kind: "object",
    fields: FreeTierUsage_Filter_DimensionsFields,
  },
  not: "not",
  or: {
    wireName: "or",
    kind: "list",
    fields: FreeTierUsage_Filter_AndFields,
  },
};

export interface FreeTierUsageConfig {
  /** <p>Use <code>Expression</code> to filter in the <code>GetFreeTierUsage</code> API operation.</p> <p>You can use the following patterns:</p> <ul> <li> <p>Simple dimension values (<code>Dimensions</code> root operator)</p> </li> <li> <p>Complex expressions with logical operators (<code>AND</code>, <code>NOT</code>, and <code>OR</code> root operators).</p> </li> </ul> <p>For <i>simple dimension values</i>, you can set the dimension name, values, and match type for the filters that you plan to use.</p> <p> <b>Example for simple dimension values</b> </p> <p>You can filter to match exactly for <code>REGION==us-east-1 OR REGION==us-west-1</code>.</p> <p>The corresponding <code>Expression</code> appears like the following: <code>{ "Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ], "MatchOptions": ["EQUALS"] } }</code> </p> <p>As shown in the previous example, lists of dimension values are combined with <code>OR</code> when you apply the filter.</p> <p>For <i>complex expressions with logical operators</i>, you can have nested expressions to use the logical operators and specify advanced filtering.</p> <p> <b>Example for complex expressions with logical operators</b> </p> <p>You can filter by <code>((REGION == us-east-1 OR REGION == us-west-1) OR (SERVICE CONTAINS AWSLambda)) AND (USAGE_TYPE !CONTAINS DataTransfer)</code>.</p> <p>The corresponding <code>Expression</code> appears like the following: <code>{ "And": [ {"Or": [ {"Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ], "MatchOptions": ["EQUALS"] }}, {"Dimensions": { "Key": "SERVICE", "Values": ["AWSLambda"], "MatchOptions": ["CONTAINS"] } } ]}, {"Not": {"Dimensions": { "Key": "USAGE_TYPE", "Values": ["DataTransfer"], "MatchOptions": ["CONTAINS"] }}} ] }</code> </p> <p>In the following <b>Contents</b>, you must specify exactly one of the following root operators.</p> */
  filter?: FreeTierUsage_Filter | Computed<FreeTierUsage_Filter>;
  maxResults?: number | Computed<number>;
  nextToken?: string | Computed<string>;
}

export interface FreeTierUsageAttrs {
  /** <p>Use <code>Expression</code> to filter in the <code>GetFreeTierUsage</code> API operation.</p> <p>You can use the following patterns:</p> <ul> <li> <p>Simple dimension values (<code>Dimensions</code> root operator)</p> </li> <li> <p>Complex expressions with logical operators (<code>AND</code>, <code>NOT</code>, and <code>OR</code> root operators).</p> </li> </ul> <p>For <i>simple dimension values</i>, you can set the dimension name, values, and match type for the filters that you plan to use.</p> <p> <b>Example for simple dimension values</b> </p> <p>You can filter to match exactly for <code>REGION==us-east-1 OR REGION==us-west-1</code>.</p> <p>The corresponding <code>Expression</code> appears like the following: <code>{ "Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ], "MatchOptions": ["EQUALS"] } }</code> </p> <p>As shown in the previous example, lists of dimension values are combined with <code>OR</code> when you apply the filter.</p> <p>For <i>complex expressions with logical operators</i>, you can have nested expressions to use the logical operators and specify advanced filtering.</p> <p> <b>Example for complex expressions with logical operators</b> </p> <p>You can filter by <code>((REGION == us-east-1 OR REGION == us-west-1) OR (SERVICE CONTAINS AWSLambda)) AND (USAGE_TYPE !CONTAINS DataTransfer)</code>.</p> <p>The corresponding <code>Expression</code> appears like the following: <code>{ "And": [ {"Or": [ {"Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ], "MatchOptions": ["EQUALS"] }}, {"Dimensions": { "Key": "SERVICE", "Values": ["AWSLambda"], "MatchOptions": ["CONTAINS"] } } ]}, {"Not": {"Dimensions": { "Key": "USAGE_TYPE", "Values": ["DataTransfer"], "MatchOptions": ["CONTAINS"] }}} ] }</code> </p> <p>In the following <b>Contents</b>, you must specify exactly one of the following root operators.</p> */
  filter: FreeTierUsage_Filter;
  freeTierUsages: FreeTierUsage_FreeTierUsages[];
  maxResults: number;
  nextToken: string;
}

export const FreeTierUsage: DataSourceBinding<FreeTierUsageConfig, FreeTierUsageAttrs> = {
  wireType: "aws_freetier_free_tier_usage",
  fields: {
    filter: {
      wireName: "filter",
      kind: "object",
      fields: FreeTierUsage_FilterFields,
    },
    maxResults: "max_results",
    nextToken: "next_token",
  },
};
