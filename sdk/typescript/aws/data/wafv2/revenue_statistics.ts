// Code generated by `ubx sdk gen` from aws_wafv2_revenue_statistics. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface RevenueStatistics_Filters {
  name?: string | Computed<string>;
  values?: string[] | Computed<string[]>;
}

export interface RevenueStatistics_RevenuePathStatistics {
  amount?: string | Computed<string>;
  path?: string | Computed<string>;
  percentage?: number | Computed<number>;
  requestCount?: number | Computed<number>;
}

export interface RevenueStatistics_SourceStatistics {
  amount?: string | Computed<string>;
  groupByValue?: string | Computed<string>;
  intent?: string | Computed<string>;
  organization?: string | Computed<string>;
  percentage?: number | Computed<number>;
  requestCount?: number | Computed<number>;
  sourceCategory?: string | Computed<string>;
  sourceName?: string | Computed<string>;
  verified?: boolean | Computed<boolean>;
}

export interface RevenueStatistics_TimeWindow {
  endTime: string | Computed<string>;
  startTime: string | Computed<string>;
}

const RevenueStatistics_FiltersFields: FieldMap = {
  name: "name",
  values: "values",
};

const RevenueStatistics_TimeWindowFields: FieldMap = {
  endTime: "end_time",
  startTime: "start_time",
};

export interface RevenueStatisticsConfig {
  currency: string | Computed<string>;
  filters?: RevenueStatistics_Filters[] | Computed<RevenueStatistics_Filters[]>;
  groupBy?: string | Computed<string>;
  limit?: number | Computed<number>;
  nextMarker?: string | Computed<string>;
  scope: string | Computed<string>;
  sortBy?: string | Computed<string>;
  sortOrder?: string | Computed<string>;
  statisticType: string | Computed<string>;
  /** <p>In a <a>GetSampledRequests</a> request, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which you want WAF to return a sample of web requests.</p> <p>You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, <code>Z</code>. For example, <code>"2016-09-27T14:50Z"</code>. You can specify any time range in the previous three hours.</p> <p>In a <a>GetSampledRequests</a> response, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which WAF actually returned a sample of web requests. WAF gets the specified number of requests from among the first 5,000 requests that your Amazon Web Services resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, WAF stops sampling after the 5,000th request. In that case, <code>EndTime</code> is the time that WAF received the 5,000th request.</p> */
  timeWindow: RevenueStatistics_TimeWindow | Computed<RevenueStatistics_TimeWindow>;
}

export interface RevenueStatisticsAttrs {
  currency: string;
  filters: RevenueStatistics_Filters[];
  groupBy: string;
  limit: number;
  nextMarker: string;
  revenuePathStatistics: RevenueStatistics_RevenuePathStatistics[];
  scope: string;
  sortBy: string;
  sortOrder: string;
  sourceStatistics: RevenueStatistics_SourceStatistics[];
  statisticType: string;
  /** <p>In a <a>GetSampledRequests</a> request, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which you want WAF to return a sample of web requests.</p> <p>You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, <code>Z</code>. For example, <code>"2016-09-27T14:50Z"</code>. You can specify any time range in the previous three hours.</p> <p>In a <a>GetSampledRequests</a> response, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which WAF actually returned a sample of web requests. WAF gets the specified number of requests from among the first 5,000 requests that your Amazon Web Services resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, WAF stops sampling after the 5,000th request. In that case, <code>EndTime</code> is the time that WAF received the 5,000th request.</p> */
  timeWindow: RevenueStatistics_TimeWindow;
}

export const RevenueStatistics: DataSourceBinding<RevenueStatisticsConfig, RevenueStatisticsAttrs> = {
  wireType: "aws_wafv2_revenue_statistics",
  fields: {
    currency: "currency",
    filters: {
      wireName: "filters",
      kind: "list",
      fields: RevenueStatistics_FiltersFields,
    },
    groupBy: "group_by",
    limit: "limit",
    nextMarker: "next_marker",
    scope: "scope",
    sortBy: "sort_by",
    sortOrder: "sort_order",
    statisticType: "statistic_type",
    timeWindow: {
      wireName: "time_window",
      kind: "object",
      fields: RevenueStatistics_TimeWindowFields,
    },
  },
};
