// Code generated by `ubx sdk gen` from aws_wafv2_sampled_requests. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface SampledRequests_SampledRequests_CaptchaResponse {
  failureReason?: string | Computed<string>;
  responseCode?: number | Computed<number>;
  solveTimestamp?: number | Computed<number>;
}

export interface SampledRequests_SampledRequests_Labels {
  name?: string | Computed<string>;
}

export interface SampledRequests_SampledRequests_Request_Headers {
  name?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface SampledRequests_SampledRequests_Request {
  clientIp?: string | Computed<string>;
  country?: string | Computed<string>;
  headers?: SampledRequests_SampledRequests_Request_Headers[] | Computed<SampledRequests_SampledRequests_Request_Headers[]>;
  httpversion?: string | Computed<string>;
  method?: string | Computed<string>;
  uri?: string | Computed<string>;
}

export interface SampledRequests_SampledRequests {
  action?: string | Computed<string>;
  captchaResponse?: SampledRequests_SampledRequests_CaptchaResponse | Computed<SampledRequests_SampledRequests_CaptchaResponse>;
  challengeResponse?: SampledRequests_SampledRequests_CaptchaResponse | Computed<SampledRequests_SampledRequests_CaptchaResponse>;
  labels?: SampledRequests_SampledRequests_Labels[] | Computed<SampledRequests_SampledRequests_Labels[]>;
  overriddenAction?: string | Computed<string>;
  request?: SampledRequests_SampledRequests_Request | Computed<SampledRequests_SampledRequests_Request>;
  requestHeadersInserted?: SampledRequests_SampledRequests_Request_Headers[] | Computed<SampledRequests_SampledRequests_Request_Headers[]>;
  responseCodeSent?: number | Computed<number>;
  ruleNameWithinRuleGroup?: string | Computed<string>;
  timestamp?: string | Computed<string>;
  weight?: number | Computed<number>;
}

export interface SampledRequests_TimeWindow {
  endTime: string | Computed<string>;
  startTime: string | Computed<string>;
}

const SampledRequests_TimeWindowFields: FieldMap = {
  endTime: "end_time",
  startTime: "start_time",
};

export interface SampledRequestsConfig {
  maxItems: number | Computed<number>;
  ruleMetricName: string | Computed<string>;
  scope: string | Computed<string>;
  /** <p>In a <a>GetSampledRequests</a> request, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which you want WAF to return a sample of web requests.</p> <p>You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, <code>Z</code>. For example, <code>"2016-09-27T14:50Z"</code>. You can specify any time range in the previous three hours.</p> <p>In a <a>GetSampledRequests</a> response, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which WAF actually returned a sample of web requests. WAF gets the specified number of requests from among the first 5,000 requests that your Amazon Web Services resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, WAF stops sampling after the 5,000th request. In that case, <code>EndTime</code> is the time that WAF received the 5,000th request.</p> */
  timeWindow: SampledRequests_TimeWindow | Computed<SampledRequests_TimeWindow>;
  webAclArn: string | Computed<string>;
}

export interface SampledRequestsAttrs {
  maxItems: number;
  populationSize: number;
  ruleMetricName: string;
  sampledRequests: SampledRequests_SampledRequests[];
  scope: string;
  /** <p>In a <a>GetSampledRequests</a> request, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which you want WAF to return a sample of web requests.</p> <p>You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, <code>Z</code>. For example, <code>"2016-09-27T14:50Z"</code>. You can specify any time range in the previous three hours.</p> <p>In a <a>GetSampledRequests</a> response, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which WAF actually returned a sample of web requests. WAF gets the specified number of requests from among the first 5,000 requests that your Amazon Web Services resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, WAF stops sampling after the 5,000th request. In that case, <code>EndTime</code> is the time that WAF received the 5,000th request.</p> */
  timeWindow: SampledRequests_TimeWindow;
  webAclArn: string;
}

export const SampledRequests: DataSourceBinding<SampledRequestsConfig, SampledRequestsAttrs> = {
  wireType: "aws_wafv2_sampled_requests",
  fields: {
    maxItems: "max_items",
    ruleMetricName: "rule_metric_name",
    scope: "scope",
    timeWindow: {
      wireName: "time_window",
      kind: "object",
      fields: SampledRequests_TimeWindowFields,
    },
    webAclArn: "web_acl_arn",
  },
};
