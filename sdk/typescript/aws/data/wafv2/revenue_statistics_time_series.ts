// Code generated by `ubx sdk gen` from aws_wafv2_revenue_statistics_time_series. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface RevenueStatisticsTimeSeries_DataPoints {
  category?: string | Computed<string>;
  date?: string | Computed<string>;
  groupByValue?: string | Computed<string>;
  intent?: string | Computed<string>;
  monetizeServedCount?: number | Computed<number>;
  settledCount?: number | Computed<number>;
  totalAmount?: string | Computed<string>;
}

export interface RevenueStatisticsTimeSeries_Filters {
  name?: string | Computed<string>;
  values?: string[] | Computed<string[]>;
}

export interface RevenueStatisticsTimeSeries_TimeWindow {
  endTime: string | Computed<string>;
  startTime: string | Computed<string>;
}

const RevenueStatisticsTimeSeries_FiltersFields: FieldMap = {
  name: "name",
  values: "values",
};

const RevenueStatisticsTimeSeries_TimeWindowFields: FieldMap = {
  endTime: "end_time",
  startTime: "start_time",
};

export interface RevenueStatisticsTimeSeriesConfig {
  currency: string | Computed<string>;
  filters?: RevenueStatisticsTimeSeries_Filters[] | Computed<RevenueStatisticsTimeSeries_Filters[]>;
  groupBy?: string | Computed<string>;
  interval: string | Computed<string>;
  limit?: number | Computed<number>;
  nextMarker?: string | Computed<string>;
  scope: string | Computed<string>;
  statisticType: string | Computed<string>;
  /** <p>In a <a>GetSampledRequests</a> request, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which you want WAF to return a sample of web requests.</p> <p>You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, <code>Z</code>. For example, <code>"2016-09-27T14:50Z"</code>. You can specify any time range in the previous three hours.</p> <p>In a <a>GetSampledRequests</a> response, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which WAF actually returned a sample of web requests. WAF gets the specified number of requests from among the first 5,000 requests that your Amazon Web Services resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, WAF stops sampling after the 5,000th request. In that case, <code>EndTime</code> is the time that WAF received the 5,000th request.</p> */
  timeWindow: RevenueStatisticsTimeSeries_TimeWindow | Computed<RevenueStatisticsTimeSeries_TimeWindow>;
}

export interface RevenueStatisticsTimeSeriesAttrs {
  currency: string;
  dataPoints: RevenueStatisticsTimeSeries_DataPoints[];
  filters: RevenueStatisticsTimeSeries_Filters[];
  groupBy: string;
  interval: string;
  limit: number;
  nextMarker: string;
  scope: string;
  statisticType: string;
  /** <p>In a <a>GetSampledRequests</a> request, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which you want WAF to return a sample of web requests.</p> <p>You must specify the times in Coordinated Universal Time (UTC) format. UTC format includes the special designator, <code>Z</code>. For example, <code>"2016-09-27T14:50Z"</code>. You can specify any time range in the previous three hours.</p> <p>In a <a>GetSampledRequests</a> response, the <code>StartTime</code> and <code>EndTime</code> objects specify the time range for which WAF actually returned a sample of web requests. WAF gets the specified number of requests from among the first 5,000 requests that your Amazon Web Services resource receives during the specified time period. If your resource receives more than 5,000 requests during that period, WAF stops sampling after the 5,000th request. In that case, <code>EndTime</code> is the time that WAF received the 5,000th request.</p> */
  timeWindow: RevenueStatisticsTimeSeries_TimeWindow;
}

export const RevenueStatisticsTimeSeries: DataSourceBinding<RevenueStatisticsTimeSeriesConfig, RevenueStatisticsTimeSeriesAttrs> = {
  wireType: "aws_wafv2_revenue_statistics_time_series",
  fields: {
    currency: "currency",
    filters: {
      wireName: "filters",
      kind: "list",
      fields: RevenueStatisticsTimeSeries_FiltersFields,
    },
    groupBy: "group_by",
    interval: "interval",
    limit: "limit",
    nextMarker: "next_marker",
    scope: "scope",
    statisticType: "statistic_type",
    timeWindow: {
      wireName: "time_window",
      kind: "object",
      fields: RevenueStatisticsTimeSeries_TimeWindowFields,
    },
  },
};
