# Code generated by `ubx sdk gen` from aws_cloud_front_origin_request_policy. DO NOT EDIT.
from __future__ import annotations

import dataclasses
from typing import Any

import ubx_sdk as ubx

@dataclasses.dataclass
class FrontOriginRequestPolicy_OriginRequestPolicyConfig_CookiesConfig:
    # Determines whether cookies in viewer requests are included in requests that CloudFront sends to the origin. Valid values are: + ``none`` – No cookies in viewer requests are included in requests that CloudFront sends to the origin. Even when this field is set to ``none``, any cookies that are listed in a ``CachePolicy``*are* included in origin requests. + ``whitelist`` – Only the cookies in viewer requests that are listed in the ``CookieNames`` type are included in requests that CloudFront sends to the origin. + ``all`` – All cookies in viewer requests are included in requests that CloudFront sends to the origin. + ``allExcept`` – All cookies in viewer requests are included in requests that CloudFront sends to the origin, *except* for those listed in the ``CookieNames`` type, which are not included.
    cookie_behavior: Any = None
    # Contains a list of cookie names.
    cookies: Any = None

@dataclasses.dataclass
class FrontOriginRequestPolicy_OriginRequestPolicyConfig_HeadersConfig:
    # Determines whether any HTTP headers are included in requests that CloudFront sends to the origin. Valid values are: + ``none`` – No HTTP headers in viewer requests are included in requests that CloudFront sends to the origin. Even when this field is set to ``none``, any headers that are listed in a ``CachePolicy``*are* included in origin requests. + ``whitelist`` – Only the HTTP headers that are listed in the ``Headers`` type are included in requests that CloudFront sends to the origin. + ``allViewer`` – All HTTP headers in viewer requests are included in requests that CloudFront sends to the origin. + ``allViewerAndWhitelistCloudFront`` – All HTTP headers in viewer requests and the additional CloudFront headers that are listed in the ``Headers`` type are included in requests that CloudFront sends to the origin. The additional headers are added by CloudFront. + ``allExcept`` – All HTTP headers in viewer requests are included in requests that CloudFront sends to the origin, *except* for those listed in the ``Headers`` type, which are not included.
    header_behavior: Any = None
    # Contains a list of HTTP header names.
    headers: Any = None

@dataclasses.dataclass
class FrontOriginRequestPolicy_OriginRequestPolicyConfig_QueryStringsConfig:
    # Determines whether any URL query strings in viewer requests are included in requests that CloudFront sends to the origin. Valid values are: + ``none`` – No query strings in viewer requests are included in requests that CloudFront sends to the origin. Even when this field is set to ``none``, any query strings that are listed in a ``CachePolicy``*are* included in origin requests. + ``whitelist`` – Only the query strings in viewer requests that are listed in the ``QueryStringNames`` type are included in requests that CloudFront sends to the origin. + ``all`` – All query strings in viewer requests are included in requests that CloudFront sends to the origin. + ``allExcept`` – All query strings in viewer requests are included in requests that CloudFront sends to the origin, *except* for those listed in the ``QueryStringNames`` type, which are not included.
    query_string_behavior: Any = None
    # Contains a list of query string names.
    query_strings: Any = None

@dataclasses.dataclass
class FrontOriginRequestPolicy_OriginRequestPolicyConfig:
    # A comment to describe the origin request policy. The comment cannot be longer than 128 characters.
    comment: Any = None
    # An object that determines whether any cookies in viewer requests (and if so, which cookies) are included in requests that CloudFront sends to the origin.
    cookies_config: Any = None
    # An object that determines whether any HTTP headers (and if so, which headers) are included in requests that CloudFront sends to the origin.
    headers_config: Any = None
    # A unique name to identify the origin request policy.
    name: Any = None
    # An object that determines whether any URL query strings in viewer requests (and if so, which query strings) are included in requests that CloudFront sends to the origin.
    query_strings_config: Any = None

_FrontOriginRequestPolicy_OriginRequestPolicyConfig_CookiesConfigFields = {
    "cookie_behavior": ubx.FieldSpec(wire_name="cookie_behavior"),
    "cookies": ubx.FieldSpec(wire_name="cookies"),
}

_FrontOriginRequestPolicy_OriginRequestPolicyConfig_HeadersConfigFields = {
    "header_behavior": ubx.FieldSpec(wire_name="header_behavior"),
    "headers": ubx.FieldSpec(wire_name="headers"),
}

_FrontOriginRequestPolicy_OriginRequestPolicyConfig_QueryStringsConfigFields = {
    "query_string_behavior": ubx.FieldSpec(wire_name="query_string_behavior"),
    "query_strings": ubx.FieldSpec(wire_name="query_strings"),
}

_FrontOriginRequestPolicy_OriginRequestPolicyConfigFields = {
    "comment": ubx.FieldSpec(wire_name="comment"),
    "cookies_config": ubx.FieldSpec(
        wire_name="cookies_config",
        kind="object",
        fields=_FrontOriginRequestPolicy_OriginRequestPolicyConfig_CookiesConfigFields,
    ),
    "headers_config": ubx.FieldSpec(
        wire_name="headers_config",
        kind="object",
        fields=_FrontOriginRequestPolicy_OriginRequestPolicyConfig_HeadersConfigFields,
    ),
    "name": ubx.FieldSpec(wire_name="name"),
    "query_strings_config": ubx.FieldSpec(
        wire_name="query_strings_config",
        kind="object",
        fields=_FrontOriginRequestPolicy_OriginRequestPolicyConfig_QueryStringsConfigFields,
    ),
}

@dataclasses.dataclass
class FrontOriginRequestPolicyConfig:
    # An origin request policy configuration. This configuration determines the values that CloudFront includes in requests that it sends to the origin. Each request that CloudFront sends to the origin includes the following: + The request body and the URL path (without the domain name) from the viewer request. + The headers that CloudFront automatically includes in every origin request, including ``Host``, ``User-Agent``, and ``X-Amz-Cf-Id``. + All HTTP headers, cookies, and URL query strings that are specified in the cache policy or the origin request policy. These can include items from the viewer request and, in the case of headers, additional ones that are added by CloudFront. CloudFront sends a request when it can't find an object in its cache that matches the request. If you want to send values to the origin and also include them in the cache key, use ``CachePolicy``.
    origin_request_policy_config: Any = None

@dataclasses.dataclass
class FrontOriginRequestPolicyAttrs:
    id: Any = None
    last_modified_time: Any = None
    # An origin request policy configuration. This configuration determines the values that CloudFront includes in requests that it sends to the origin. Each request that CloudFront sends to the origin includes the following: + The request body and the URL path (without the domain name) from the viewer request. + The headers that CloudFront automatically includes in every origin request, including ``Host``, ``User-Agent``, and ``X-Amz-Cf-Id``. + All HTTP headers, cookies, and URL query strings that are specified in the cache policy or the origin request policy. These can include items from the viewer request and, in the case of headers, additional ones that are added by CloudFront. CloudFront sends a request when it can't find an object in its cache that matches the request. If you want to send values to the origin and also include them in the cache key, use ``CachePolicy``.
    origin_request_policy_config: Any = None

FrontOriginRequestPolicy = ubx.ResourceBinding(
    wire_type="aws_cloud_front_origin_request_policy",
    fields={
        "origin_request_policy_config": ubx.FieldSpec(
            wire_name="origin_request_policy_config",
            kind="object",
            fields=_FrontOriginRequestPolicy_OriginRequestPolicyConfigFields,
        ),
    },
)
