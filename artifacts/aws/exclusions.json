{
 "skip_descriptions": {
  "aws_quick_sight_analysis": {
   "reason": "AWS::QuickSight::Dashboard/Analysis/Template are real, deeply-nested visualization-definition schemas (707 real \"definitions\" in Dashboard's own 336KB raw schema file) that expand to 77,457 of the registry's 126,624 real fields at a real ~0.1% sourced rate -- deeply-nested visual/chart configuration nobody hand-authors. Migrated verbatim from ubiquex's own sdk/providers/.ubx/config describe_exclude declaration (cli/describeexclude.go). Two real, distinct naming conventions apply to the SAME real resource depending on schema source: \"aws_quick_sight_analysis\" (CFN, underscore-split service name) and \"aws_quicksight_analysis\" (HashiCorp/live docs corpus, compact service name) -- both keyed here so the exclusion actually applies regardless of which source a future regeneration uses."
  },
  "aws_quick_sight_dashboard": {
   "reason": "AWS::QuickSight::Dashboard/Analysis/Template are real, deeply-nested visualization-definition schemas (707 real \"definitions\" in Dashboard's own 336KB raw schema file) that expand to 77,457 of the registry's 126,624 real fields at a real ~0.1% sourced rate -- deeply-nested visual/chart configuration nobody hand-authors. Migrated verbatim from ubiquex's own sdk/providers/.ubx/config describe_exclude declaration (cli/describeexclude.go). Two real, distinct naming conventions apply to the SAME real resource depending on schema source: \"aws_quick_sight_dashboard\" (CFN, underscore-split service name) and \"aws_quicksight_dashboard\" (HashiCorp/live docs corpus, compact service name) -- both keyed here so the exclusion actually applies regardless of which source a future regeneration uses."
  },
  "aws_quick_sight_template": {
   "reason": "AWS::QuickSight::Dashboard/Analysis/Template are real, deeply-nested visualization-definition schemas (707 real \"definitions\" in Dashboard's own 336KB raw schema file) that expand to 77,457 of the registry's 126,624 real fields at a real ~0.1% sourced rate -- deeply-nested visual/chart configuration nobody hand-authors. Migrated verbatim from ubiquex's own sdk/providers/.ubx/config describe_exclude declaration (cli/describeexclude.go). Two real, distinct naming conventions apply to the SAME real resource depending on schema source: \"aws_quick_sight_template\" (CFN, underscore-split service name) and \"aws_quicksight_template\" (HashiCorp/live docs corpus, compact service name) -- both keyed here so the exclusion actually applies regardless of which source a future regeneration uses."
  },
  "aws_quicksight_analysis": {
   "reason": "AWS::QuickSight::Dashboard/Analysis/Template are real, deeply-nested visualization-definition schemas (707 real \"definitions\" in Dashboard's own 336KB raw schema file) that expand to 77,457 of the registry's 126,624 real fields at a real ~0.1% sourced rate -- deeply-nested visual/chart configuration nobody hand-authors. Migrated verbatim from ubiquex's own sdk/providers/.ubx/config describe_exclude declaration (cli/describeexclude.go). Two real, distinct naming conventions apply to the SAME real resource depending on schema source: \"aws_quick_sight_analysis\" (CFN, underscore-split service name) and \"aws_quicksight_analysis\" (HashiCorp/live docs corpus, compact service name) -- both keyed here so the exclusion actually applies regardless of which source a future regeneration uses."
  },
  "aws_quicksight_dashboard": {
   "reason": "AWS::QuickSight::Dashboard/Analysis/Template are real, deeply-nested visualization-definition schemas (707 real \"definitions\" in Dashboard's own 336KB raw schema file) that expand to 77,457 of the registry's 126,624 real fields at a real ~0.1% sourced rate -- deeply-nested visual/chart configuration nobody hand-authors. Migrated verbatim from ubiquex's own sdk/providers/.ubx/config describe_exclude declaration (cli/describeexclude.go). Two real, distinct naming conventions apply to the SAME real resource depending on schema source: \"aws_quick_sight_dashboard\" (CFN, underscore-split service name) and \"aws_quicksight_dashboard\" (HashiCorp/live docs corpus, compact service name) -- both keyed here so the exclusion actually applies regardless of which source a future regeneration uses."
  },
  "aws_quicksight_template": {
   "reason": "AWS::QuickSight::Dashboard/Analysis/Template are real, deeply-nested visualization-definition schemas (707 real \"definitions\" in Dashboard's own 336KB raw schema file) that expand to 77,457 of the registry's 126,624 real fields at a real ~0.1% sourced rate -- deeply-nested visual/chart configuration nobody hand-authors. Migrated verbatim from ubiquex's own sdk/providers/.ubx/config describe_exclude declaration (cli/describeexclude.go). Two real, distinct naming conventions apply to the SAME real resource depending on schema source: \"aws_quick_sight_template\" (CFN, underscore-split service name) and \"aws_quicksight_template\" (HashiCorp/live docs corpus, compact service name) -- both keyed here so the exclusion actually applies regardless of which source a future regeneration uses."
  }
 },
 "skip_page": {}
}